# Findings register — defects & review findings. Closed only by evidence, never by reply.
| ID | Date | Severity | What | Owner | Status | Closing evidence |
|---|---|---|---|---|---|---|
| F-003 | 2026-08-16 | low (cosmetic, verified harmless) | `kubectl apply -f infra/manifests/postgres.yaml` reports `statefulset.apps/postgres configured` on EVERY run, never `unchanged` — which reads like a recipe that mutates the cluster each time it is run. Verified it does not: `kubectl diff -f …` prints nothing, `metadata.generation` stays 1 = `status.observedGeneration`, the pod keeps its original creationTimestamp and 0 restarts across three applies. Believed to be kubectl's apply-patch bookkeeping for StatefulSets carrying `volumeClaimTemplates`. | MLOps | open | Closes when either (a) a session identifies the exact field and the manifest stops provoking the patch, or (b) a later kubectl reports `unchanged`. Do NOT "fix" it by removing the volumeClaimTemplate — the noise is cheaper than losing the PVC story |
| F-002 | 2026-08-16 | low (known limitation, documented) | The gotcha #10 port pre-check reads `ss` INSIDE the WSL VM, so it cannot see a Windows-native process holding a family port — yet Docker Desktop publishes container ports on the Windows host too, so such a conflict is real and would surface as an opaque bind failure. Observed as sound for our own stack: after cluster-up, `ss -tlnp` in WSL does show kind's 8081/8443, so anything inside the VM (incl. all of Docker) IS covered. | MLOps | open | Closes when either (a) a session observes the failure mode and the script gains a Windows-side probe, or (b) M0-S3/S4 run the full platform with no unexplained bind failure and the limitation is downgraded to a documented note in `scripts/port_precheck.sh` (where it already is) |
| F-001 | 2026-08-16 | low (friction, non-blocking) | Session allowlist omits the ordinary verbs the allowed tools depend on (`chmod`, `ls`, `mkdir`, `tar`, `printenv`); agent cannot self-widen (harness refuses writes to `.claude/settings*.json`); `~/.local` outside the file-tool sandbox. M0-S1 completed via allowlisted `python3` workarounds. | PO | open | Closes when AWAITING_PO 2026-08-16-2 is answered and a session runs `chmod`/`ls` without an approval prompt (gotcha #27) |
