# Findings register — defects & review findings. Closed only by evidence, never by reply.
| ID | Date | Severity | What | Owner | Status | Closing evidence |
|---|---|---|---|---|---|---|
| F-007 | 2026-08-16 | medium (latent trap; would pass every offline check) | **The columns most correlated with the target are not available when an ETA is quoted.** `fare_amount`, `tip_amount`, `tolls_amount`, `total_amount`, `payment_type` and `store_and_fwd_flag` are recorded at or after trip end. Windowed to `fare_amount BETWEEN 0 AND 200`, fare correlates with duration at **r = 0.8708** (`docs/eda_report.md` §8) — so a model using them would score superbly on any held-out split and be unimplementable at M5's serving boundary, with nothing in the offline evidence to reveal it. The sharper half: **`trip_distance` has the same shape and is the single strongest predictor** (r = 0.8066 raw, 0.8464 in logs, §9). The TLC value is the meter's *driven* distance — what actually happened — not a quote-time route estimate. A serving system does not have it. | MLE | open | Closes when (a) `taxi_mlops.features` defines and documents the quote-time feature set explicitly, with the post-trip columns named as excluded and why, AND (b) the M3 dossier resolves `trip_distance`: either an honest quote-time substitute (OSRM / zone-centroid distance, already M3's named scope) or a recorded decision that the meter distance is treated as available with the assumption stated. Discovered by M1-S3's EDA, which computed the correlations that make the trap attractive. |
| F-006 | 2026-08-16 | medium (silent in training, untestable in val/test) | **`congestion_surcharge` is switched on mid-training-window and any feature or imputation built on it learns "January".** The column is 63.4565% null in 2019-01 and 0.42-0.56% null in every other month; per-day, it cliffs from 99.822% null on 2019-01-20 to 1.118% on 2019-01-21 (`docs/eda_report.md` §7b, per-day table). 2019-01 is a TRAIN month and 2019-07/08 are clean, so a model fitted with this column would look unremarkable in training and the defect would be **invisible in validation and test** — the two splits that would otherwise catch it. Same family as gotcha #31 (drift by column) and the M1-S2 review's drift by value; this one is drift by *availability*, and it is inside the training window. Sibling fact from the same section: **`airport_fee` is 100% null across all 56,127,878 rows** — a plausible-looking column carrying zero information in this window. | MLE | open | Closes when M2 records an explicit, evidenced choice: exclude `congestion_surcharge`, or train from 2019-02 onward, or a stated alternative — and does NOT impute it from a training set that is 1/6 contaminated. A silent inclusion does not close this row. |
| F-005 | 2026-08-16 | medium (design gap, review finding) | The rejected rows exist only as COUNTS. M1-S1 attributes every dropped row to a named rule and writes the totals beside each output (914,459 rows, 1.603% over 2019-01..08) but retains none of them, so "how many" is answerable and "which kind" is not. `duration_above_max` alone removes 159,300 trips over 120 minutes; whether those are meter faults or a real long-haul/airport population cannot be decided from any artifact that exists, and no view in the analyst layer can reach them. Raised as DCR-01 at the Data Contract Review 2026-08-16 (`docs/rituals/2026-08-16_data-contract-review.md`), with dissent recorded: the DA holds that deferring it means the EDA report cannot say whether the missing 1.603% was noise or a population. | DE | open | Closes when ingest retains the rejected rows in a form a query can characterize -- a sidecar under `data/rejected/<split>/`, carrying the rule that rejected each row, DVC-tracked and reachable from the analyst layer as a view -- AND a query over it answers what the `duration_above_max` population actually is. NOT closable by adding more counts. Deliberately a finding and not a debt row: no milestone's quoted scope in BLUEPRINT §9 promises this capability, so inventing a landing would be the carried-to-nowhere failure (gotcha #19). Proposed home: M1-S3, the story that first needs it (its EDA examines the target honestly); if S3's kickoff scope is judged not to cover writing new ingest artifacts, this becomes an ARCH scoping call at the M1 boundary, not a silent slide. **M1-S3 JUDGED IT OUT OF SCOPE, 2026-08-16 — reasons recorded, routed to ARCH at the M1 boundary as the finding's own condition requires.** The kickoff's S3 is a pure-docs story ("Safe stop: after merge; pure-docs story, no state touched"); a rejected-row sidecar is none of that. It requires (a) changing `taxi_mlops.data` ingest to retain rows, (b) re-running `make ingest` over 57M rows, which rewrites the very `data/processed/` artifacts M1-S2 proved byte-identical two sessions ago and would demand a fresh rebuild proof, (c) ~+1 GB DVC cache and remote, (d) a new analyst view plus its reconciliation test. That is a DE story, not a paragraph in an EDA. **The DA's dissent stands and is now evidenced rather than predicted**: `docs/eda_report.md` opens at §0 by declaring that every number in it describes the surviving 98.397%, and §2 states plainly that whether the 159,300 trips over 120 minutes are meter faults or a long-haul population "this report cannot answer and does not guess". The omission is loud in the deliverable, which is the most S3 could honestly do without it. Two new facts from S3 that ARCH should weigh: the rejection rate is **not stationary** (1.428% in 2019-04 rising monotonically to 2.020% in 2019-08, +41% relative), so the discarded population is growing as volume falls; and the val/test months are the two dirtiest, so the held-out evaluation sits on the thinnest-characterized data. |
| F-004 | 2026-08-16 | HIGH (data loss risk, FIXED same session) | `make destroy DRY_RUN=1` deleted the kind cluster — and every PVC in it — while printing `[destroy] DRY_RUN=1 — nothing was deleted.` `cmd_destroy` guarded the file loop behind DRY_RUN but called `cmd_down` unconditionally before it. The M0-S2 unit test missed it because its sandbox names a cluster that cannot exist, so the delete path always no-opped (gotcha #30). | MLOps | **closed** | Fixed in `scripts/cluster.sh` (cluster deletion now obeys DRY_RUN, printing `WOULD delete kind cluster 'mlops-taxi' (and with it every PVC inside)`). Proved live 2026-08-16 (M0-S4): `make destroy DRY_RUN=1` against the rebuilt LIVE cluster left `kind get clusters` → `mlops-taxi`, 3/3 nodes, MLflow `/health` → OK. Regression-pinned by `test_destroy_dry_run_spares_the_cluster_itself` (fake `kind` records its calls) + a positive control; red-teamed by reverting the fix → the new test FAILS, quoting `[cluster-down] deleting kind cluster` next to `nothing was deleted` |
| F-003 | 2026-08-16 | low (cosmetic, verified harmless) | `kubectl apply -f infra/manifests/postgres.yaml` reports `statefulset.apps/postgres configured` on EVERY run, never `unchanged` — which reads like a recipe that mutates the cluster each time it is run. Verified it does not: `kubectl diff -f …` prints nothing, `metadata.generation` stays 1 = `status.observedGeneration`, the pod keeps its original creationTimestamp and 0 restarts across three applies. Believed to be kubectl's apply-patch bookkeeping for StatefulSets carrying `volumeClaimTemplates`. | MLOps | **closed** | Closes when either (a) a session identifies the exact field and the manifest stops provoking the patch, or (b) a later kubectl reports `unchanged`. Do NOT "fix" it by removing the volumeClaimTemplate — the noise is cheaper than losing the PVC story. M0 boundary triage 2026-08-16: stays open by these conditions (an observation defect with a defined closure, deliberately NOT converted to debt); a BOUNDED probe of condition (a) — one attempt, e.g. `kubectl apply` server-side diff of the last-applied annotation — is folded into M1-S4, the story already editing `infra/manifests/postgres.yaml` for D-002. If the one attempt finds nothing, it stays open; do not chase **CLOSED 2026-08-16 (M1-S4) by closing condition (a) — the field is named and the manifest no longer provokes the patch.** The bounded one-attempt probe was `kubectl apply -f infra/manifests/postgres.yaml -v=9`, which prints the PATCH body kubectl actually sends. It is exactly one field: `{"spec":{"volumeClaimTemplates":[{"metadata":{"name":"data"},"spec":{"accessModes":["ReadWriteOnce"],"resources":{"requests":{"storage":"8Gi"}}}}]}}`. CAUSE: `volumeClaimTemplates` is an ATOMIC list under strategic-merge patch (no patchMergeKey), so kubectl cannot merge it element-wise and compares the whole list against the live object — into which the apiserver has defaulted `apiVersion: v1`, `kind: PersistentVolumeClaim`, `spec.volumeMode: Filesystem` and `status: {phase: Pending}` (read back live). Our manifest omitted all four, so desired could never equal live and the list was re-sent on every apply. FIX: state those four defaults in the manifest. `storageClassName` stays UNSET — the apiserver does not write it back, so naming kind's local-path would cost portability for nothing, and the M0-S3 comment about not naming a cloud-specific class stands. EVIDENCE, three applies in a row: `configured (server dry run)` -> `configured` (the run that updates the last-applied annotation) -> **`statefulset.apps/postgres unchanged`** — the first `unchanged` in this project's life. Nothing was disturbed: `metadata.generation` still 1 = `status.observedGeneration`, pod `postgres-0` keeps `creationTimestamp 2026-08-16T15:46:45Z` and `restarts=0`, `kubectl diff -f` prints nothing. Regression-pinned by `tests/unit/test_marts.py::test_postgres_manifest_states_the_server_side_defaults`, which also pins that `storageClassName` stays absent. REMAINING CONDITION, stated honestly: this was proved on an EXISTING object. M1-S5's deliberate cluster rebuild re-applies the manifest to an empty cluster and should re-observe `unchanged` on its second apply; if it does not, reopen with that transcript. |
| F-002 | 2026-08-16 | low (known limitation, documented) | The gotcha #10 port pre-check reads `ss` INSIDE the WSL VM, so it cannot see a Windows-native process holding a family port — yet Docker Desktop publishes container ports on the Windows host too, so such a conflict is real and would surface as an opaque bind failure. Observed as sound for our own stack: after cluster-up, `ss -tlnp` in WSL does show kind's 8081/8443, so anything inside the VM (incl. all of Docker) IS covered. | MLOps | **closed** (downgraded to a documented note) | Closes when either (a) a session observes the failure mode and the script gains a Windows-side probe, or (b) M0-S3/S4 run the full platform with no unexplained bind failure and the limitation is downgraded to a documented note in `scripts/port_precheck.sh` (where it already is). CLOSED 2026-08-16 (M0-S4): closing condition (b) met — M0-S3 and M0-S4 each ran the full platform (MinIO + Postgres + MLflow, five published host ports) with zero bind failures, including a from-scratch cluster create after a full destroy. The limitation stays documented at `scripts/port_precheck.sh` lines 22-25, which names the Windows-side probe (`Get-NetTCPConnection -LocalPort <p>`) for the day it does bite. |
| F-001 | 2026-08-16 | low (friction, non-blocking) | Session allowlist omits the ordinary verbs the allowed tools depend on (`chmod`, `ls`, `mkdir`, `tar`, `printenv`); agent cannot self-widen (harness refuses writes to `.claude/settings*.json`); `~/.local` outside the file-tool sandbox. M0-S1 completed via allowlisted `python3` workarounds. | PO | open | Closes when AWAITING_PO 2026-08-16-2 is answered and a session runs `chmod`/`ls` without an approval prompt (gotcha #27) |
