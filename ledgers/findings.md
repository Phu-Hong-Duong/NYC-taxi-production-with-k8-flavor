# Findings register — defects & review findings. Closed only by evidence, never by reply.
| ID | Date | Severity | What | Owner | Status | Closing evidence |
|---|---|---|---|---|---|---|
| F-029 | 2026-08-19 | low (no number is wrong and no gate is currently lying: on THIS machine every record `verify-m3` and `verify-m4` read is present and was written by the drill that claims it. The defect is that the evidence base of two gates is outside review and outside the clone) | **Two milestone gates replay records that are gitignored, and one of them says the opposite in its own header.** `automation/runs/` is ignored wholesale (`.gitignore:43`) and `git ls-files automation/runs/` is EMPTY — yet `verify-m3` §5 replays `automation/runs/m3s5/bakeoff.json` and §4 reads `automation/runs/m3s4/*.json`, and `verify-m4` (this story) reads `m4-cache/cache_drill.json`, `m4-kill/kill_drill.json`, `m4-kill/retry_probe.actions.json`, `m4-image/image.json` and the recorded run actions. Three consequences, in increasing order of interest: (a) a fresh clone runs those legs RED for a reason that is not a defect; (b) the records are MACHINE state, so an edit to one — the exact fault both red teams simulate — leaves no diff for a reviewer to see, and the only thing standing between a rewritten number and a green gate is that nobody rewrites it; (c) two artifacts already state the opposite in writing. `scripts/verify_m3.sh`'s header lists what it reads as "committed docs, **committed JSON**, the Optuna storage, the registry", and `scripts/verify_m3_redteam.sh`'s failure path advises `Run this by hand: git checkout -- automation/runs/m3s5/bakeoff.json` — a command that cannot restore an untracked file, i.e. the drill's own recovery instruction was written in the belief the record was tracked. Found at M4-S5 leg 3 while deciding what `verify-m4` was allowed to depend on; checked with `git check-ignore -v` before being written down. Same family as #51 — a component asserting something about itself that it has no mechanism to keep true — asked of a gate's inputs rather than of its outputs. | ARCH (policy) · MLOps (mechanics) | **CLOSED 2026-08-19 (M5-S1) — policy DECIDED 2026-08-19 (M4 boundary, ARCH): Option A; mechanics LANDED M5-S1.** Track the record JSONs: `automation/runs/**/*.json` un-ignored; logs, `.log.N` rotations and `.status` files stay ignored. Why A: C's copy step is the twin this program refuses everywhere else (a copy somebody must remember is a copy that drifts), and B leaves consequence (b) — a tampered record with no diff — completely open, while the records ARE the evidence base of two gates: what a gate reads must be what review can see. Churn cost accepted and bounded: records change only when a drill deliberately re-runs (rare, and every re-run is itself a reviewable event), the JSONs total a fraction of 496K today, and the M5 kickoff instructs future drills to keep verdict JSONs small (logs stay ignored). **Mechanics INTAKEN → M5-S1** (`docs/milestones/M5_KICKOFF.md`), one PR: un-ignore + `git add` every record both gates read · correct the three lines the decision makes stale (`verify_m4.sh`'s header and closing `(gitignored: F-029)` print, `verify_m3.sh`'s header note, CLAUDE.md's rows) · re-run `verify-m3` + `verify-m4` + BOTH red teams, which now edit TRACKED files (the EXIT-trap restore is byte-identical so a clean drill leaves a clean tree, and a crashed drill now recovers with exactly the `git checkout --` that used to be a false belief) · show `git ls-files automation/runs` covering every path both gates read. **Deliberately NOT executed at the boundary itself**: half the mechanics — tracked files under headers still saying "gitignored" — would be a #51-class false statement, so the move ships as one unit. Two false statements WERE corrected in place (M4-S5 leg 3): `verify_m3.sh`'s header now says **recorded** JSON and names this finding, its red team's recovery line now names the byte copy it actually kept instead of `git checkout`, and CLAUDE.md's `verify-m3` row was corrected the same way. `verify-m4`'s header states the dependency and this finding id up front rather than letting a reader discover it on a fresh clone. | **CLOSING EVIDENCE (M5-S1, 2026-08-19).** `git ls-files automation/runs \| wc -l` → **32** (was 0) — every path both gates read, 236 KB total, largest record under 100 KB, including `m4-kill/attempt1-prediction-wrong/` (a record of a mistake must not vanish from a clone). The gitignore is PATTERN-BASED and had to be: a bare `automation/runs/` exclusion makes git stop descending, so a `!` rule beneath it is silently inert — landed as `automation/runs/**` + `!automation/runs/**/` + `!automation/runs/**/*.json`, verified BOTH directions with `git check-ignore -v` (the four JSONs match the negation at `.gitignore:59`; `.log`/`.status` still match the exclusion at `.gitignore:57`). Logs and `.status` stay ignored — they are transcripts and no gate reads them for a verdict. Stale statements corrected AT SOURCE: `verify_m4.sh`'s header and its closing print (now `(tracked: F-029 closed)`), `verify_m3.sh`'s header, CLAUDE.md's verify-m3 row + the F-029 bullet + gotcha #69's summary, `docs/gotchas.md` #69 (resolved-note; the gotcha is NOT retired — it is about the check, not this directory). Two the kickoff did not enumerate, found by grepping the CLAIM rather than the file list: both red-team headers now state the new regime, and `tests/unit/test_bakeoff.py`'s skip-when-absent became an ASSERTION that the record exists (strictly stronger; the host suite now reports **544 passed, no skips**). Verbatim transcripts NOT edited (`docs/verify_m4_transcripts.md`) — they carry dated notes instead, because a transcript edited to match today's code is not a transcript. **Re-run over the moved files: `make verify-m3` GREEN 46/46 · `make verify-m4` GREEN 39/39 · `make verify-m3-redteam` PASSED (RED with 2 FAILs, 44 sub-checks still passing, sha256 `c4a323ea072a…` before and after) · `make verify-m4-redteam` PASSED (RED with 2 FAILs including the cross-system leg, sha256 `beb10ab49fb0…` before and after)** — and the property the move creates, asked after each drill and answered by silence: `git status --porcelain` empty, i.e. **a clean drill leaves a clean tree**, which was not a checkable statement while the files were invisible to git. Narrative + all four transcripts: `docs/serving_m5.md` §1. What this does NOT claim, stated because the distinction is the whole finding: the records are now REVIEWABLE, not verified — a tampered record is now a **diff**, and what used to stand between a rewritten number and a green gate was only that nobody rewrote it. **Original condition: closes when the policy is decided, not when a file moves. AMENDED at decision time (2026-08-19, ARCH) to the stricter form: closes when M5-S1's mechanics land** — a row closed on the decision alone would leave two gates printing "gitignored" over tracked files, i.e. would close onto the same class of false self-statement this finding is about. The fork as it stood, kept for the record: **(A)** track the record JSONs by un-ignoring them (`!automation/runs/**/*.json`, logs and `.status` still ignored) — the records enter review, a tampered number becomes a diff, a clone can run the gates; honest cost is that machine-produced artifacts start churning in PRs, both red teams then edit TRACKED files (a crashed drill leaves a dirty tree, which is recoverable but noisy), and `bakeoff.json` alone is 4 KB while nothing bounds what a future drill writes. **(B)** leave them ignored and make both gate headers say plainly that these legs are machine-local, which is what this session did as an interim — honest, cheap, and leaves (b) above completely unaddressed. **(C)** copy only the small verdict-bearing summaries into `docs/` beside the prose that already cites them, and have the gates read from there — the review benefit of (A) without the churn, at the cost of a copy step somebody must remember, which is the twin this program refuses everywhere else. No recommendation is recorded here: the choice is about what belongs under review, which is ARCH's call, and this row exists so it is made on purpose rather than inherited. |
| F-028 | 2026-08-18 | low (a transcript defect, not a numbers defect — every number the run produced was correct and correctly recorded elsewhere; but the line a human reads to confirm what happened described a graph that no longer existed) | **`scripts/run_pipeline.sh` printed `six stages on-cluster` after the pipeline grew a seventh.** The summary line was a literal, typed once at M4-S4 when the graph had six stages, and M4-S5 leg 2 added `publish_marts` without it changing — so the runner's own final word on a green seven-stage run (`rw98pj84z4jh5ldqrxqp`) undercounted the work by one stage and said nothing about whether the marts tail had run at all, which is the one thing about that run a reader would want confirmed. It survived because nothing reads a summary line for information: the assertion above it is POSITIVE and about the run's outputs (gotcha #59's fix), so the check was correct while its report was not. Same family as #51 asked of a transcript rather than a checker — a component stating something it has no mechanism to keep true. Found by reading this story's own transcript rather than by any test. | MLOps | **CLOSED 2026-08-18 (M4-S5 leg 2), same session it was found.** | The count is DERIVED, not typed: the line reads `pipelines.tasks.STAGES` — the graph's one declaration, already pinned by `tests/unit/test_pipeline_tasks.py::test_the_graph_is_the_one_the_blueprint_names` — subtracts one when the tail is off, and names the tail's state explicitly (`marts tail ON`/`OFF`). Verified live on the same run's re-read and on `PUBLISH_MARTS=0` (both drills). The CLAUDE.md command row was updated to name seven stages; the M4 KICKOFF was deliberately NOT edited — it says "all six stages" because that is the graph ARCH chartered, with the marts tail chartered separately in M4-S5's own Do list, and a kickoff rewritten after the fact stops being the record the work was judged against (the same rule that keeps M2/M3's promotion transcripts unedited). Deliberately NOT given a test of its own: a test asserting "the transcript says seven" is the same literal one layer out, and the derivation removes the failure mode rather than watching for it (gotcha #52 — the fix that changes the MECHANISM beats the fix that changes the value). Transcript: `docs/pipeline_m4.md` §16.7 |
| F-027 | 2026-08-18 | low-medium (no KPI, model or gate number is affected — the field is orchestration metadata only. But it is metadata `verify-m4` was explicitly told to reuse, and it was recorded as a confident zero in the drill evidence a gate is meant to read: every `attempts` value in `automation/runs/m4-cache/cache_drill.json` is a default, not a measurement) | **`scripts/flyte_run_actions.py` read a protobuf field that does not exist, and got a plausible zero instead of an error.** The reader collected `int(getattr(status, "attempt", 0) or 0)`; the message is `flyteidl2.workflow.run_definition_pb2.ActionStatus` and its field is **`attempts`**, plural. A protobuf message answers `getattr` for its own fields only, so the misspelling fell through to the supplied default — which means the reader has reported `attempts: 0` for **every action of every run it has ever been pointed at**, and nothing looked wrong, because 0 is exactly what an un-retried action should say. Found at M4-S5 by the retry probe: a task with `retries=2` that raises on its first line settled at `FAILED` while the reader still said `attempts: 0`, and the kubernetes pod for the same action was named `…-a0-2`. Asking the server directly showed `attempts: 3`. The class is the one gotcha #59 names — a signal that is consistent with success no matter what happened — one layer down, in a getter rather than an exit code. | MLOps | **CLOSED 2026-08-18 (M4-S5), same session it was found.** | One-word fix in `scripts/flyte_run_actions.py` (`attempt` → `attempts`) with the reason written beside it. Verified live: the retry probe's action now reads **`attempts: 3`** for `retries=2` (three attempts: the try plus two retries), and phase 0 of `make pipeline-kill-drill` asserts it inside the range the declared budget allows. Pinned by `tests/unit/test_flyte_task_wiring.py::test_the_action_reader_asks_for_fields_the_proto_actually_has`, which reads the field names **off `ActionStatus.DESCRIPTOR`** and requires every name the reader asks for to be one of them — a test written against the literal `"attempts"` would go green on the next typo in the next field. **Honest consequence, stated rather than quietly corrected**: M4-S4's `cache_drill.json` carries `attempts: 0` on every action. Those runs genuinely were not retried (their pods are all `…-0`), so no claim made from that file is wrong — but the field was not evidence when it was written, and `verify-m4` must not treat the historical values as if it were. Transcript: `docs/pipeline_m4.md` §13 |
| F-026 | 2026-08-18 | medium (no number recorded so far is wrong — `git diff fb57324 HEAD -- src` was EMPTY when this was found, checked before writing this row — but it is a silent-wrong-numbers hazard the moment M5+ edits `src/` and reruns the pipeline) | **A pipeline run can execute a version of `src/taxi_mlops` that is not the one in the working tree, and nothing says so.** `flyte run` defaults to `--copy-style loaded_modules`, so the code bundle carries only what is imported AT REGISTRATION — 22 files, observed, against 36 `.py` files in `src/taxi_mlops` alone. The stage bodies import `pipelines.tasks` (and through it `taxi_mlops`) INSIDE the function, so the modules that actually compute every number come from the **task image**, whose tag is read out of `automation/runs/m4-image/image.json` and is only ever rewritten by `make image-load`. Consequence: edit `src/`, commit, run `make pipeline` — and the pod runs the PREVIOUS code with a green transcript. `scripts/run_pipeline.sh` prints `a pull error here means the tree moved — re-run make image-load`, which describes a protection that does not exist: M4-S3's loud-`ImagePullBackOff` property only fires for an image tag no node HOLDS, and a stale manifest names a tag every node holds. Found at M4-S4's cache leg while checking whether the drill's two runs were running the same code — which is a question the drill has to be able to answer before its result means anything. | MLOps | **CLOSED 2026-08-18 (M4-S4), same session it was found.** | `scripts/run_pipeline.sh` now REFUSES to launch when the image's source differs from the tree's: it diffs the image manifest's git sha against `HEAD` over exactly the paths that reach a task pod ONLY through the image — `src/`, `pyproject.toml`, `uv.lock`, `docker/`. **`pipelines/` is deliberately NOT in that list**: it is what the code bundle carries, so an edit there DOES reach the pod and must not refuse a run (that scoping is the difference between a guard and an obstacle, and getting it wrong would have refused this story's own cache drill) and exits 3 naming the changed files and the remedy (`make image-load`). It is scoped to those paths ON PURPOSE — refusing on any commit would make the pipeline unrunnable after a docs commit, and a guard that fires when nothing is wrong is a guard that gets deleted (gotcha #50). `IMAGE_DRIFT_OK=1` exists for the one honest case, an untracked/dirty tree mid-debug, and it prints what it is waiving. Verified two ways live: with the tree matching the image the drill's two runs launched normally, and a deliberate one-line edit to `src/taxi_mlops/training/evaluate.py` produced `FAIL: the task image predates the tree` naming that file, exit 3, with nothing launched — restored and re-verified green. Pinned by `tests/unit/test_flyte_task_wiring.py::test_the_runner_refuses_an_image_older_than_the_source_it_would_run`. Transcript: `docs/pipeline_m4.md` §11 |
| F-025 | 2026-08-18 | medium (blocked every in-cluster MLflow client, i.e. the whole M4 train stage; nothing already recorded is wrong, and the host-side path was never affected) | **The MLflow server refused every request from inside the cluster as a DNS-rebinding attack.** The first Flyte task to reach the train stage got `MlflowException: API request to endpoint /api/2.0/mlflow/experiments/get-by-name failed with error code 403 != 200. Response body: 'Invalid Host header - possible DNS rebinding attack detected'` — AFTER the fit had begun, from an endpoint that reads like an MLflow problem and is a network-policy one. MLflow 3.x serves under uvicorn with host-header protection whose allow-list is `MLFLOW_SERVER_ALLOWED_HOSTS`; the community-charts chart derives that list from an ingress, and this release deliberately has none (the host route is a NodePort, M0-S3), so the effective list covered the loopback names and nothing else. **Latent since M0.** Every MLflow client this program had for four milestones was host-side (`localhost:5000`), so nothing could address the server by its in-cluster name until a pod tried — and a pod cannot use `localhost`. | MLOps | **CLOSED 2026-08-18 (M4-S4), same session it was found.** | Fixed in `infra/helm/mlflow/values.yaml` with the chart's own `serverAllowedHosts`, listing the four names by which this Service is addressable **explicitly** — `["*"]` would have deleted the protection rather than configured it. **The first version of the fix was wrong in an instructive way and that is part of the evidence**: setting the value at all REPLACES MLflow's default list, and the middleware compares the WHOLE host header, port included — so a list of bare hostnames fixed the pod and simultaneously gave every host-side client the same 403. Identified by a two-line experiment (`curl localhost:5000/api/...` -> 403 while `curl -H 'Host: localhost' 127.0.0.1:5000/health` -> OK) and fixed by listing both forms. Verified live after the second deploy: host route `GET /api/2.0/mlflow/experiments/search -> 200`, and a busybox pod in the `flyte` namespace `wget http://mlflow.mlflow.svc.cluster.local:5000/health -> OK`. The train stage then ran to completion on-cluster and logged its run to MLflow (`e17ce5846aaf4f90bee8a2609b208c94`). Transcript: `docs/pipeline_m4.md` §7 |
| F-024 | 2026-08-18 | low (ad-hoc probes only; every real entry point is unaffected — but the failure LOOKS like a success) | **The OpenMP shim announces that it has re-executed and then hands the interpreter a command line it cannot parse, whenever the process was started with `python -c`.** Under `python -c "<code>"` CPython sets `sys.argv[0]` to the literal `"-c"` and keeps the source string nowhere reachable, so `openmp._relaunch_argv()` returned `["-c"]` and `os.execv` ran `python -c` with no code. Observed output: `[openmp] no system libgomp.so.1; linked … and re-executing once with LD_LIBRARY_PATH set` immediately followed by `Argument expected for the -c option` and a usage block — i.e. a message about argument parsing for a problem about a shared library, on the heels of a line claiming the fix worked. Present since M2-S2 (2026-08-17) and reproduced ON THE HOST, so it is not a container artefact: `uv run python -c "from taxi_mlops.training.openmp import ensure_openmp; ensure_openmp()"` shows it directly. Blast radius is genuinely small — `python -m taxi_mlops…` and `.py` files both reconstruct correctly and are pinned by tests from M2-S2 — which is why it survived four milestones: nothing the program actually runs takes this path. Found by M4-S3's D-004 sensor drill, which had to make the shim FIRE inside the task image to prove the smoke's 'the shim never ran' assertion measures something, and used `-c` to do it. | MLE | **CLOSED 2026-08-18 (M4-S3), same session it was found.** | Fixed by REFUSING the unreconstructable form rather than guessing at it: `openmp._invoked_with_dash_c()` gates `ensure_openmp`, which now raises `OpenMPUnavailableError` **before any mutation** (no mkdir, no symlink) with a message naming the situation and the three ways out — `python -m <module>`, a `.py` file, or `sudo apt install libgomp1` as the M4 image does. The replacement probe is a MODULE, `src/taxi_mlops/training/openmp_probe.py`, run as `python -m taxi_mlops.training.openmp_probe`, so it takes the same re-exec path a real task takes; `scripts/image_smoke.sh` check 3 and `scripts/image_smoke_redteam.sh` step 2 both use it. Three tests pin it: `tests/unit/test_training_setup.py::test_a_dash_c_invocation_is_refused_instead_of_re_exec_d` (raises, AND asserts the shim directory was not created), `::test_the_dash_m_and_script_forms_are_still_allowed_through` (the refusal stays narrow), and `tests/unit/test_task_image.py::test_the_openmp_probe_module_exists_and_is_the_dash_m_form`. Observed live in the drill: `ok    the -c path raises OpenMPUnavailableError naming the situation (F-024 fixed)` — asserted three ways, including that the old `Argument expected for the -c option` string is ABSENT. Transcript: `docs/task_image_m4.md` §7. |
| F-023 | 2026-08-18 | medium (blocks M4-S2's last accept-when leg and M4-S4's whole premise; the control plane itself is healthy, so nothing already built is at risk) | **The Flyte CLI cannot upload a run's code bundle: it resolves the blob store by a name that only exists inside the cluster.** `make flyte-hello` reaches the control plane and gets far — project created, task image resolved (`ghcr.io/flyteorg/flyte:py3.12-v2.6.1`, no build needed), code bundle built (1 file, 0.0012 MB compressed) — then dies at `Uploading code bundle...` with `Failed to upload …fast….tar.gz after 3 retries: ConnectError: [Errno -2] Name or service not known`. The cause is split horizon, not a fault: the blob store is ONE MinIO with TWO names. Pods reach it as `minio.platform.svc.cluster.local:9000` (what `infra/helm/flyte/values.yaml` configures, correctly — a pod cannot use `localhost:9000`); this host reaches the same server as `localhost:9000` via the kind hostPort -> nodePort 30900 route from M0-S3. The CLI uploads **directly** to the object store, so it inherits an endpoint that does not resolve on its side of the boundary. **Tried and did NOT fix it**: exporting the SDK's own client-side settings `FLYTE_AWS_ENDPOINT` / `FLYTE_AWS_ACCESS_KEY_ID` / `FLYTE_AWS_SECRET_ACCESS_KEY` (which `flyte.storage.S3` maps onto its fields) — identical symptom, so the endpoint arrives from elsewhere (server-advertised storage config, or a dataproxy-issued URL) and finding out where is the next question. **Not ADR-002's wall**: that fallback triggers on 'Flyte 2.x fights on deployment or MLflow interop', and deployment SUCCEEDED — 3 pods Running, helm `deployed` at revision 2, `/healthz` 200 from the host, CLI demonstrably talking to the control plane. One client-side data path fails. Datum for whoever picks it up: Flyte **1.x** ships `storage.signedUrl.stowConfigOverride` for exactly this sandbox/split-horizon case and the 2.x chart renders no equivalent — which is either the reason the fallback is right or the hint for where 2.x hides it. | MLOps | **CLOSED 2026-08-18 (M4-S4)** by the finding's own condition. | `make flyte-hello` completes: `ActionOutputs(o0="HELLO CROSSTOWN FROM A FLYTE TASK")`, three pods `Completed` (`a0` plus two child actions), the second task's input being the first's output through the `flyte-data` bucket — the seam test, not a pod-ran test. **The diagnosis was right and one of the recorded probes was impossible.** Probe 1 paid: the client never builds an upload URL, it is HANDED one — `CreateUploadLocationResponse` carries `signed_url`, minted by the server from ITS storage endpoint, which is why M4-S2's `FLYTE_AWS_ENDPOINT` attempts could not have worked. Probe 2 (`<node-ip>:30900`, "one name both sides can resolve") **does not exist on this machine**: measured from WSL, `172.19.0.3:30900` -> 000 and even the apiserver at `172.19.0.3:6443` -> 000, because kubectl reaches the cluster through a docker-PUBLISHED port on loopback (`127.0.0.1:35553`) and the docker bridge is not routable from this side under Docker Desktop/WSL2. Probe 3 (ADR-002's fallback) was NOT needed and stays armed: the 2.x line does carry flytestdlib's lever, the CHART just renders no value for it — `grep -ao "stowConfigOverride" /usr/local/bin/flyte` hits inside the running image. Fixed by `configuration.inline.storage.signedUrl.stowConfigOverride.endpoint: http://localhost:9000` in `infra/helm/flyte/values.yaml`, which changes the endpoint used for SIGNING only; task pods keep the in-cluster name (read back live: `003-storage.yaml` and `100-inline-config.yaml` both present, neither clobbered). **Two latent defects in the check itself were fixed with it**: `flyte run` returns at LAUNCH (so the greeting could never have matched) and `--follow` streams LOGS while these tasks RETURN a value — the assertion now reads the run's outputs. Full trail: `docs/pipeline_m4.md` §1-§2 |
| F-022 | 2026-08-18 | low-medium (a script that cannot be re-run, disabled by the very promotion it performed; nothing depends on running it today, and M7's retrain loop is the next caller of the shape) | **`scripts/bakeoff_m3.py` has been un-runnable since M3-S5 moved the alias, and nothing noticed because nothing re-runs it.** The `champion v1` contender is resolved by ALIAS on purpose — `_resolve`'s docstring argues it, "the champion comes from the ALIAS (so this bake-off judges what is actually serving)" — while its `Spec` pre-registers `feature_set="v1"`. At M3-S5 run time `@champion` was version 1 (a v1 model) and both were true. The bake-off's own `--promote-winner` then moved the alias to version 2, a **v2** model, so the spec and the alias have disagreed ever since. Observed live 2026-08-18 (M4-S1) attempting a smoke run: `make bakeoff BAKEOFF_ARGS="--smoke-rows 20000"` → **exit 1** at `_load_booster`, `champion v1 eats [24 v2 columns] but feature set 'v1' is [5 v1 columns]. Scoring it would silently reorder columns` — the refusal working exactly as designed, one layer too late to be useful. **Pre-existing and NOT caused by M4-S1**: the failure is in contender resolution, before any line this story changed. **Why nothing caught it**: `verify-m3` §5 *replays* the recorded verdicts through `gate.decide` (correctly — M3 cost 12,447 s of fitting and the gate must not re-derive it), so a re-run of the script is checked by nobody; and a bake-off is, by construction, a once-per-milestone command. **The tension is real, not a typo**: judging "what is actually serving" and pre-registering "which cell of the 2×2 this row is" are both right, and they cannot both be resolved from a moving pointer. Options are (a) resolve the incumbent cell by alias but read its feature set from the loaded model rather than from the Spec — the row becomes "champion, whatever it is now", which is what the 2×2's incumbent cell actually means; (b) pin the incumbent row to a version/run id once a milestone's contenders are declared, losing the "judges what is serving" property; (c) keep the bake-off a one-shot per milestone and say so, with M7's retrain building its own contender set. | MLE → ARCH at the M4 boundary | **OPEN — direction DECIDED 2026-08-19 (M4 boundary, ARCH): option (a).** The incumbent cell resolves by ALIAS and reads its feature set off the LOADED model rather than off its `Spec`, so the row means what the 2×2's incumbent cell has always meant: "the champion, whatever it is now." The four fixed contenders keep their pre-registered Specs — pre-registration is the point for arms that were declared before their numbers existed, and it is exactly wrong for a pointer that is DESIGNED to move. **CARRY → M7**, quoted landing (gotcha #19): §9/M7 *"scheduled Flyte retrain landing a challenger"* — the retrain loop is the next thing that must build a contender set and compare it against what serves, so the repair belongs to its builder, now with the direction in hand rather than the open question. Originally: filed 2026-08-18 (M4-S1), deliberately not fixed there. Out of this story's scope (F-018's closing conditions are (a) rank on val + (b) correct the sentence; both are met and neither needs the script to execute), and the choice between (a)/(b)/(c) is a design call about what the incumbent row MEANS, which belongs with whoever next builds a contender set. **Nothing waits on it in M4**: no M4 story runs the bake-off, and the M3 record stands as measured. It becomes blocking at **M7's first retrain**, which is the next thing that must produce a challenger and compare it against what serves. **Honest consequence for M4-S1, stated rather than hidden**: F-018's repair could not be demonstrated by executing the script end to end. Its evidence is the unit tests instead — a behavioural test whose two splits DISAGREE, and an AST test proving `_select_winner` is called inside the val pass — plus `verify-m3`'s five replayed verdicts still green | Open. Closable by a recorded decision among (a)/(b)/(c) WITH the change, plus one execution of the resulting script that gets past contender resolution (a `--smoke-rows` run is enough — the defect is in resolution, not in scale) |
| F-021 | 2026-08-18 | low-medium (a correct check giving DANGEROUS advice in the one state the machine is usually in; found at the M3 boundary by running it against the live cluster) | **`make ports` goes RED against OUR OWN running cluster and its message tells the reader to stop it.** Observed at the M3→M4 boundary: with kind `mlops-taxi` up, `scripts/port_precheck.sh` refuses on 6 of 10 ports (3030/5000/8081/8443/9000/9001 — exactly the hostPorts `infra/kind/kind-config.yaml` publishes) and prints *"another stack on this machine owns a port we need. Free it (stop that stack)"*. The check was designed as a PRE-cluster-up guard (gotcha #10, foreign stacks) and is correct in that window — but nothing in its output distinguishes "a foreign stack squats on 5000" from "the port is held by the very cluster this repo built", and the advice is wrong in the second case in the worst possible way: **since M2 the cluster is STATEFUL** (the MLflow registry with both champion versions, all run artifacts in MinIO, the Metabase app-db, both Optuna studies live on PVCs, and PVCs die with the cluster), so a 3am session that obeys the message destroys the registry that every verify target reads live. The failure is the port-precheck sibling of gotcha #50: a guard firing on the program's own correct behaviour trains the reader to distrust or obey it wrongly. | MLOps | **CLOSED 2026-08-18 (M4-S2)** by this row's own conditions — intaken at the M4 kickoff 2026-08-18 (ARCH triage, same session it was found): absorbed into M4-S2 (quoted landing: §9/M4 is the milestone whose stories run against the LIVE cluster daily, and S2 is its platform-plumbing story) | **CLOSED by both conditions, and the foreign refusal was NOT softened.** `scripts/port_precheck.sh` now resolves the HOLDER before it judges: `ss` cannot answer "whose?" (a published port is held by docker-proxy, not by the workload), so the script reads `docker ps --format '{{.Names}}\t{{.Ports}}'`, keeps the containers whose name starts with the cluster name parsed out of `infra/kind/kind-config.yaml`, and extracts their host-side port numbers. A busy port in that set is OURS; anything else is foreign. **Observed live 2026-08-18 against the running cluster**: `make ports` -> `6 port(s) held by US — the 'mlops-taxi' cluster is up, which is expected`, each line naming the port, its purpose and `-> container mlops-taxi-control-plane`, then `[ports] OK — 10 required port(s): 4 free, 6 held by us, 0 foreign.` **exit 0**, plus two lines telling the reader NOT to free them and why (the PVCs hold the only copy of the registry, the studies and the app-db). The gotcha #10 refusal is unchanged for a genuinely foreign holder and now also says which containers are exempt. **Both states pinned by tests** (`tests/unit/test_cluster_scripts.py`): the same bound ephemeral port, the same fake `docker ps`, differing ONLY in the container NAME — `mlops-taxi-control-plane` -> exit 0 with `held by US`, `somebody-elses-stack-web-1` -> exit 2 with `REFUSING`. A fix that stopped reading the name would fail one of the two. The pre-existing fake-listener red-team (`test_port_precheck_refuses_a_busy_port_and_names_it`, no docker shim) still goes red. If `docker` is absent or silent the OURS set is empty and every busy port reads foreign — the pre-F-021 behaviour, which is the safe direction to fail in | 2026-08-18 | **S3 — low-medium** (a caveat on the milestone's headline conclusion, not a wrong decision; it recurs at every retrain) | **The tuned configuration is chosen at 15% of train and applied unchanged at 100%, including knobs whose meaning is dataset-size dependent — so "tuning bought +0.07 points" measures a *sample-optimal* configuration at full scale, not a full-scale-optimal one.** `scripts/optuna_sniper.py:50` searches on `DEFAULT_SAMPLE = 0.15`; the winning trial for v2 (`automation/runs/m3s4/refit-v2.json`) carries `min_data_in_leaf: 1293`, which on the 6,598,113-row sample is a leaf floor of **1 row in 5,105** and on the 43,987,422-row refit is **1 in 34,020** — the same integer is ~6.7× *less* regularising at the scale it was finally fitted at, and nothing re-derives it. The rounds budget travels the same way and by construction, not by choice: `scripts/automl_refit.py:102` reads `num_boost_round` from `verdict["max_rounds"]`, which the sniper wrote as its own **per-trial cap at 15%** (800), and both full-data refits then ran into it (800/800 and 791/800). `docs/automation_track_m3.md` §7 discloses the cap as a DR-01 *budget* decision and F-015 records the v1 truncation it caused; what is nowhere is the transfer question itself — the sign of the bias is not obvious (looser leaves may help or hurt at 44M rows), which is exactly why it should be measured rather than assumed. It is the largest unstated caveat on §6.4's "+0.24% val / +0.07 pts test", and M7's retrain loop calls the same scout→sniper→refit path. | MLE → ARCH/PO at the M3 boundary (M7 owns the recurrence) | OPEN — **CARRY 2026-08-18 (M3 boundary, ARCH triage): lands M7**, quoted covering scope (gotcha #19): §9/M7 *"scheduled Flyte retrain landing a challenger"* — the retrain loop re-runs the same scout→sniper→refit path, so the scale-transfer rule (row option (b)) or the one measured re-fit (option (a)) belongs in M7's kickoff intake, where REV's condition placed it. Deliberately NOT pulled into M4: re-fitting the v2 winner now would spend ~35 min of compute to re-litigate a bake-off whose verdicts stand, and DR-01's post-hoc-refit prohibition still applies to M3's budget | Open. Closable by either (a) one full-data re-fit of the v2 winner with the count-scaled knobs rescaled to the refit's row count, reported through `taxi_mlops.training.evaluate` beside the 3.2403 that stands, or (b) a recorded decision that the sniper's verdict must carry a scale-transfer rule and the refit must re-derive its own round budget. REV closes nothing itself |
| F-019 | 2026-08-18 | **S2 — medium** (latent in M3, a hard failure on the first request M5 serves; created by this milestone's alias move) | **The champion M3 promoted cannot build features for any request dated outside 2019 — it raises, per request.** Feature set v2 admitted g1, which carries `is_holiday`/`is_near_holiday`/`is_business_day`; those are built from the committed lookup `data/reference/us_federal_holidays.csv`, which holds **10 rows, all in 2019**. `taxi_mlops.features.calendar.flags` calls `assert_covers`, which **raises `ValueError`** on an uncovered year — a deliberate and correct choice for training (a silent "not a holiday" would decay the feature into a constant), but `features/` is the ONE transform path for training AND serving (CLAUDE.md), so at M5 the same line is a 500 on every quote. Reproduced live 2026-08-18 by REV against `configs/train.yaml`'s configured set: a single-row request at `2026-08-18 09:15` → `ValueError: data/reference/us_federal_holidays.csv covers [2019] but the frame carries [2026]`; the same request at `2019-08-18 09:15` builds all 24 features. Champion **v1 had none of these columns**, so the dependency entered the served model at M3-S5 and did not exist before it. Nothing in `ledgers/`, `docs/gotchas.md` or the dossier records it; `docs/feature_dossier.md` row 4 records the opposite intuition — leakage risk "none — a calendar is knowable years ahead" — while the shipped table stops at the end of the training year. M7's drift months are the second caller. | MLE/MLOps → M5 entry (a kickoff §0 intake, not an M3 re-open) | **CLOSED 2026-08-19 (M5-S2)** — the decision recorded WITH the fix, per the row's own condition. Previously: OPEN — **CARRY 2026-08-18 (M3 boundary, ARCH triage): lands M5**, quoted covering scope (gotcha #19): §9/M5 *"v1's M4 (KServe Standard, mlserver, storage-config, THE parity test 1e-6, p95 measured, self-heal under load) + the Production Readiness Review: SRE walks a written checklist (runbook exists, rollback typed, alerts planned, capacity sanity) BEFORE the champion serves"* — the serving story that would eat the 500 is the story that decides extend-vs-policy, and the PRR is where the SRE half (wrong quote vs outage) is minuted. M5's kickoff intake is mandatory (Prompt E); NOT closable before then because the policy choice is the serving story's to make with the runbook in hand. **M4 is unaffected**: its pipeline is parametrized over 2019 months only | Open. Closable by a decision recorded WITH the fix, because the two options differ in kind: extend the table (cheap, and the raise stays the guard it is) versus a serving-time policy for an uncovered date (degrade-and-flag vs refuse-the-request — an SRE call, since one is a wrong quote and the other is an outage). Evidence to re-run: build `sets.resolve(train.features)` features for one request with a non-2019 timestamp. **TRIPWIRE ADDED 2026-08-18 (M4-S1), the fix deliberately NOT taken**: `tests/unit/test_features_v2.py::test_the_configured_feature_set_refuses_a_request_dated_outside_the_table` builds the CONFIGURED set for a 2026-dated request and asserts the raise names `data/reference/us_federal_holidays.csv` and both years, then builds the same frame inside 2019 to prove the test is about the DATE. It pins the CURRENT behaviour so M5 changes it on purpose rather than meeting it as a 500 from a KServe pod; the table was not extended and no policy was chosen, because that decision is M5's with the runbook in hand  **CLOSING EVIDENCE (M5-S2, 2026-08-19). DECIDED: BOTH halves, because each alone is unshippable.** (1) The table is EXTENDED to **2019..2030** — 146 rows, derived from 5 U.S.C. §6103 by the committed `scripts/derive_us_federal_holidays.py` (`make holidays`, horizon moved by `HOLIDAYS_TO=`). Extending alone is not a fix: a table always ends, and the day it ends the failure is exactly the one this row is about — only later, and to somebody who was not told it could happen. (2) The serving boundary is TYPED: an uncovered date raises `taxi_mlops.serving.client.UncoveredDateError`, a `QuoteRefused` carrying `http_status = 422`, before a request is built and before anything reaches the wire; `make quote` exits **2** on it, its own code, because "I will not answer that" and "I could not answer" are different facts. **REFUSE, not degrade-and-flag** — the SRE half, minuted in `docs/champion_on_the_wire_m5.md` §4.2 and cross-referenced by M5-S5's PRR: degrading returns a WRONG QUOTE whose size depends on how holiday-sensitive the route is and which nothing in the system can see, while refusing is a visible, countable, alertable failure confined to dates nobody has entered yet, with its fix named in the error text. It is also what `features/calendar.py`'s own raise was written to prevent. **M6's alert plan gains a named signal from this decision: the count of 422 refusals per window — a leading indicator with a fixed, one-command remedy.** **Evidence, all live 2026-08-19 against the deployed champion (version 2, unmoved).** A 2026-dated request through `localhost:8081` now returns a quote (`2026-08-19T09:15:00, zone 132 -> 48 -> 63.7881 minutes`) where it previously raised; a 2031-dated request returns `REFUSED (422) … covers through 2030 … Extend the table: make holidays HOLIDAYS_TO=2031`, exit 2. **No measured number moved, and that is checked twice rather than eyeballed**: re-deriving 2019 alone reproduces the ten hand-written rows BYTE FOR BYTE (they were written by a human from the published federal calendar at M3-S3, months before the deriver existed — so agreement is evidence about the RULES, including Juneteenth, federal only from 2021 and correctly ABSENT from 2019), and `tests/unit/test_holidays.py` asserts the holiday AND near-holiday SETS inside 2019-01-01..2019-08-31 are unchanged — asserted directly because a near-holiday day can be introduced by a row in a different year (2019-12-31's neighbour is 2020-01-01). The diff is 136 insertions, 0 deletions. **Two deriver decisions recorded rather than assumed**: a weekend holiday emits TWO rows (the statutory date AND the observed day off — both are anomalous for a duration model, and no 2019 holiday falls on a weekend so nothing measured moves), and the horizon is 2030 because a longer default is a bigger claim about a statute that changed as recently as 2021. **THE M4-S1 TRIPWIRE WAS UPDATED IN THE SAME PR**, per the intake's condition: it pinned the BROKEN behaviour on purpose and is replaced by two assertions that fail if EITHER half is undone — a request dated today builds the full matrix (the table half) and a request past the horizon still RAISES in `features/` (the guard half, because the typed 422 lives one layer up and translating a raise that no longer happens would silently make every quote answerable). The horizon is READ from the table, never typed (F-017's rule), so moving it does not orphan the test. 16 tests in `tests/unit/test_holidays.py`, 14 in `tests/unit/test_serving_client.py`; host suite **610 passed**. Narrative: `docs/champion_on_the_wire_m5.md` §4 |
| F-018 | 2026-08-18 | **S2 — medium** (method defect in the code that decided which model serves; the decision itself survives it, the pattern is inherited by M7) | **The bake-off SELECTS its winner on the holdout month and the gate then judges that winner on the same month, and the memo states the opposite.** `scripts/bakeoff_m3.py:276` is `winner = min(loaded[1:], key=lambda item: item.metrics[holdout].mae)` with `holdout = "test"` — five contenders are read on 2019-08 and the champion is whichever one comes lowest there. `docs/bakeoff_m3.md:87` describes those same rows as "**5,950,708 test rows, untouched by training and by selection**", and `gate.verdict_lines` prints that sentence on every verdict; after line 276 it is not true of selection. The structure is the one `gate.py` refuses by name one level up — "judging on val would score a model against a month it has already been fitted to" — and a max-of-five on the holdout biases the promoted verdict (+3.33% over the floor) upward by an amount nobody measured. It decided a real identity: the two v2 arms are **0.0022 min apart** (0.069%), far below any selection-free resolution, so which of them serves was settled by the test month. **What makes it cheap rather than fatal**: every contender already had a val number in hand — the script re-scores val and asserts each contender reproduces its recorded val MAE to 1e-9 *before* any test number is taken — and `docs/bakeoff_m3.md` §3 records that the val ranking and the test ranking are identical, so selecting on val and gating only the winner on test would have cost one line, changed no outcome, and left the holdout honest. Related but distinct from **F-016** (which asks whether the incumbent condition needs a margin) and from §6 note 1 (which discloses that the runner-up wins KPI-10): those are about the *bar* and the *metric*; this is about the *split the ranking is taken on*. M7's retrains are the next caller of this shape. | MLE → ARCH/PO at the M3 boundary | **CLOSED 2026-08-18 (M4-S1)** by this row's own (a)+(b) conditions, with `make verify-m2` and `make verify-m3` re-run green after | **(a) The ranking moved to val, and it moved by ORDERING rather than by convention.** `scripts/bakeoff_m3.py` now chooses its winner in `_select_winner`, called from inside the val pass of the split loop — before the holdout split has been loaded, so there is no holdout number in existence to rank on. `SELECTION_SPLIT = "val"` carries the argument; the payload records `winner_selected_on`; the floor stays excluded from the ranking (it is the bar, not a candidate) while keeping its own holdout number and verdict. Pinned by four tests in `tests/unit/test_bakeoff.py`, the load-bearing one built so the two splits DISAGREE (`artisan v2` best on val, `auto-on-v2` best on test) — a test where they agree, which is what M3-S5 actually observed, cannot tell the two rules apart, and that is why the defect survived a bake-off everybody read. A fifth test asserts the M3 record still has NO `winner_selected_on` key: its silence is the honest marker of the run that ranked on the holdout. **(b) The false sentence is corrected at both of its sources.** `gate.verdict_lines` no longer asserts selection-purity on its own authority — it is a caller-supplied argument (`holdout_untouched_by_selection`) defaulting to the weaker, always-true sentence, written up as `gate.py` property 7; `make train` and `scripts/gate_redteam_incumbent.py` fit or construct exactly ONE challenger and so earn it, the bake-off passes False and prints its own selection basis instead. `docs/bakeoff_m3.md` §3 carries a DATED correction note beside line 87 that leaves the false five words standing, states what the defect costs the result (an unmeasured upward bias on the winner's +3.33% margin; the two v2 arms 0.0022 min apart) and what it does not (the champion's identity — §3's own paragraph records auto-on-v2 leading on val too), and says why `bakeoff.json` was NOT regenerated. §6's ranking-rule sentence carries the same dated correction. **`automation/runs/m3s5/bakeoff.json` is byte-unchanged and nothing was re-fitted.** |
| F-017 | 2026-08-18 | medium (a gate that goes red when the program behaves correctly; found by the first legitimate champion transition, three sub-checks at once) | **Three `verify-m2` §1 assertions encoded M2-era facts as literals, so the first correct champion transition turned them RED.** (1) `gate_floor` was pinned to the string `baseline-group-median` — but M3-S1 replaced the floor with a NEW name (`…-od-fallback`, F-010) *because* `configs/train.yaml` legislates that a floor change is a new name and never an edit, so the tag legitimately moved with the promotion. (2) The champion's experiment was pinned to `cfg['mlflow']['experiment']` (`m2-modeling`) — but the M3-S5 winner is M3-S4's full-data refit and legitimately lives in `m3-automl`. (3) `do_not_promote` was read by KEY PRESENCE, and every run this program writes carries that key with a `yes …`/`no …` VALUE; the four bake-off contenders carry `"no — full-data fit; the gate sees it at M3-S5"`, so the gate called the promoted champion **hobbled** (gotcha #49). §3's kept-refusal leg had the same latent false GREEN in the other direction. The severity is not the red run — it is what a red run for no reason teaches: **a guard that fails when the program does the right thing trains the next session to edit assertions**, and the session after that inherits a formality. Found by running `make verify-m2` after the champion transition, which is exactly the tripwire it was built to be — it just fired at its own literals rather than at the refresh. | MLOps | **CLOSED 2026-08-18 (M3-S5)** | Each literal replaced by the property that holds at every champion and is strictly stronger than the literal was: the floor must be a name `baselines.fit_floor` can rebuild (which also excludes the flattering constant-median floor — something the literal never checked); the run must be FINISHED and in a NAMESPACED experiment (gotcha #17's real invariant, never MLflow's `Default`); a mark counts unless its value says no. Plus one NEW sub-check the literal could not make: the version's `gate_floor` must equal the floor `data/predictions/predictions.json` actually published against — F-012's wire seen from the other end. `make verify-m2` → **GREEN 55/55** (was 54; one added, none removed). `verify-m3` was written under the same rule from the first line, and `tests/unit/test_verify_m3.py::test_the_gate_pins_no_champion_run_experiment_or_floor_name` fails if it ever pins a run id, an experiment name or a floor name. Written up as gotchas **#49** and **#50** |
| F-016 | 2026-08-18 | low-medium (latent asymmetry in the gate; it made a real promotion at M3-S5 and will make every one after) | **The incumbent condition is non-regression with no margin, so the serving alias can move on a difference smaller than the program's own keep bar.** The floor condition demands **≥2.00%** KPI-09 improvement before this program will own a booster at all, argued in `configs/train.yaml: gate` as a MAINTENANCE-COST bar (~4 s of mean error). F-011's incumbent condition, added at M3-S1, asks only that a challenger not REGRESS against what is serving. At M3-S5 that difference decided a real promotion: `auto-lgbm-v2` took the alias at **+0.63%** over incumbent version 1 (3.2403 vs 3.2608 min — **1.2 s** of mean error, +0.097 KPI-10 points), and its margin over the runner-up `artisan-v2` was **0.069%** (134 ms), with the runner-up AHEAD on KPI-10 by 0.005 points. The asymmetry is defensible on its face — the booster is already owned, so a version bump does not cost what adopting a model class costs — but a rollback rehearsal, a serving cutover and a prediction/mart/board/memo refresh are not free either, and nothing currently stops the pointer churning on sub-keep-bar deltas (M7's retrains are the next callers). **Not acted on at M3-S5 on purpose**: loosening OR tightening a gate condition after seeing the number it would have changed is the edit this program never makes on its own authority (PO fork, CLAUDE.md), and the pre-registered rule was followed as written. The question for the boundary is whether the incumbent condition should carry a margin of its own, and if so whether it is the floor bar's 2.00% or something smaller that reflects only the transition cost. | MLE → ARCH/PO at the M3 boundary | OPEN — **routed to the PO 2026-08-18 (M3 boundary, ARCH triage): AWAITING_PO 2026-08-18-1**, a genuine gate-condition fork (gates change only by PO fork). The gate stands AS PRE-REGISTERED meanwhile, and the M4 kickoff legislates that M4's orchestration demos never move the alias (`--no-promote` on every pipeline demo run; `verify-m4` asserts `@champion` unchanged), so nothing rides on the answer before M7's first retrain promotion | Open. Evidence in `docs/bakeoff_m3.md` §6 note 2 and §4's verdict table; the promotion it describes is `automation/runs/m3s5/bakeoff.json`. Closes when the PO answers 2026-08-18-1 and the chosen option lands (a config edit with re-argued comment if a margin is adopted, or a recorded keep-as-is decision in the entry itself) |
| F-015 | 2026-08-18 | medium (a bake-off row whose weakness is a budget artefact, one story before the bake-off reads it) | **The automation track's v1 contender is a TRUNCATED model, not a converged one, and the 2×2 will be read as if it were.** `auto-on-v1` (xgboost, from study `m3-sniper-v1`) refit full-data to **3.7245 val MAE / 78.003% KPI-10**, against hand-tuned v1's **3.4760 / 79.693%** — automation loses by 7.15%. The log shows validation error still falling ~0.03 MAE per 100 rounds at iteration **799 of the 800-round cap** (`[700] 3.75255 → [799] 3.72447`), and the scout had proposed `n_estimators: 1635`, i.e. more than double the ceiling. The cap is a DR-01 **budget** decision, so the row is a statement about this budget and not about xgboost or about AutoML. Compounding it: the v1 study got only **9 trials of 60 requested** before its 1,500 s ran out, over an 8-dimensional space. M3-S5's table isolates "features, or tuning, or both?" — a row that is really "tuning, truncated at 800 rounds after 9 trials" will silently answer that question wrongly unless it is labelled in the row itself. **The obvious repair is forbidden**: raising the cap and refitting *after* seeing the result is DR-01 condition 2's named prohibition, and the track has already overspent its 9,000 s share (§6.5: 8,152.3 s over five phases, ~9,400–9,700 s projected, vs the artisan's 3,313.9 s). **Addendum 2026-08-18, same story, after the sixth phase landed — the finding is confirmed and it is NARROWER than it looked.** (a) The projection above is superseded by a measurement: the track finished at **9,133.8 s**, +1.49% over its share and **2.76×** the artisan's — over, but by less than the guess, so the "budget already overspent" argument stands on a smaller number than the one written when this row was opened. (b) `auto-on-v2` reached the *same* 800-round cap (`best_iteration: 791`, `Did not meet early stopping`), which looked like it would double the caveat — it does not: v2 gained **0.00034** MAE over its last 100 rounds where v1 gained **0.02808**, an ~82× difference in slope at the cap, so v2 is capped-but-flat and v1 is capped-mid-descent. **A cap is a truncation only if the curve is still moving under it.** This finding therefore attaches to the `auto-on-v1` row and to no other row in the 2×2. | MLE | **CLOSED 2026-08-18 (M3 boundary, ARCH triage)** — closed by M3-S5's own artifacts, verified against this row's conditions | **Closed exactly as the row demanded.** `docs/bakeoff_m3.md` carries the caveat IN the row: §1 ("One row carries a caveat and one row deliberately does not… stopped by the clock, not by convergence (**F-015**)… F-015 attaches to the first row and to no other. The caveats travel *in the row*"), §4 ("Its F-015 caveat travels in its row… **the caveat explains the size of the loss; it does not convert the row into a pass**"), and §5's 2×2 states what the square can and cannot separate ("xgboost truncated mid-descent (F-015)" named in the tuning-only cell). Original closing conditions preserved for the record: bake-off table carries the caveat in the `auto-on-v1` row (cap bound / 9 trials / budget already over) and states which explanations the 2×2 can and cannot separate — or a PO fork of DR-01's budget. Evidence today: `automation/runs/m3s4/refit-v1.json` (`best_iteration: 800`, `max_rounds: 800`), `automation/runs/m3s4/sniper-v1.json` (`trials_total: 9`, `trials_requested: 60`, `stopped_on: budget`), `docs/automation_track_m3.md` §6.4/§6.5 |
| F-014 | 2026-08-18 | medium (record destroyed by the tool built to preserve it; no load-bearing data lost, by an unrelated design decision) | **`automation/run_detached.sh` truncated the log of the run it was resuming.** The launcher opened its log with `: > "${LOG}"` — right for a job that runs once, wrong for every job in this repo. `scripts/automation_track.sh` is *designed* to be relaunched under the same name (it skips any phase whose output JSON exists), so relaunch after a kill/stop/failure is the normal path. Resuming M3-S4's track on 2026-08-18 to run its one missing phase wiped **2 h 20 m** of transcript — both FLAML leaderboards, every sniper trial line, and the PO's hand-written stop note — one line before the resume logic correctly skipped the phases that transcript described. Nothing load-bearing was lost only because each phase writes a JSON verdict beside the log: the property that makes the job resumable is the property that made the loss survivable. Latent since the script was written (2026-08-17, for gotcha #45); this was the first ever relaunch under an existing name, so it was the first execution of that line in its real context. | MLOps | **CLOSED 2026-08-18 (M3-S4)** | Rotation replaces truncation: `<name>.log.1 … .log.N`, `KEEP_LOGS=5`, announced on stdout (`previous log kept as …`). Safe because the launcher already refuses to start a second job under a RUNNING name, so no live writer's file is ever renamed — and that pairing is itself pinned. Three tests in `tests/unit/test_watchdog.py`, all against a real relaunch in a sandbox repo: `test_a_resumed_job_does_not_erase_the_run_it_resumes` · `test_rotation_keeps_a_bounded_history` · `test_a_live_job_is_never_rotated_out_from_under_itself`. `uv run pytest tests/unit/test_watchdog.py -q` → **14 passed**. Written up as gotcha **#48** and as the M3-S4 (part 2) field note |
| F-013 | 2026-08-17 | low-medium (latent; two stale configs that contradict the live truth, waiting for a session to trust one) | **Two bootstrap-era config stubs survived into a repo whose real answers now live elsewhere, and both contradict the current law.** (1) `configs/promotion.yaml` defines a gate (`gate_ratio: 0.85`, i.e. "pass at 15% improvement") that is NOT the gate: the real gate lives in `configs/train.yaml: gate` (2.00% margin + KPI-10 no-regression, pinned by verify-m2 §2's replay). A future session that greps configs for "promotion" finds the wrong bar first. (2) `configs/features.yaml` names `trip_distance` inside its "v1" set — a column `taxi_mlops.features.quote_time.EXCLUSIONS` refuses by law (F-007), and its `pickup_hour`/`pu_zone` names match no real column. The port-family twins lesson (CLAUDE.md): two definitions one directory apart, one stale, is how leakage gets re-admitted "per the config" or a model gets judged against a bar nobody set. Found at the M2→M3 boundary triage by reading the configs the M3 stories are about to lean on. | MLE | open — **gate half CLOSED 2026-08-17 (M3-S1)**: `configs/promotion.yaml` DELETED, and a test (`test_no_second_config_file_defines_a_gate`) fails if any file under `configs/` other than `train.yaml` names a gate KNOB — knobs and not filenames, because the next stub will be called something else. `docs/BLUEPRINT.md`'s pointer to the deleted file was corrected in the same PR. **Features half CLOSED 2026-08-17 (M3-S3)**: `configs/features.yaml` is now the REAL registry — `base` + five named `groups` + the sets (`v1`, the five ablation sets, `v2`, the red-team set) — and `configs/train.yaml: features` holds a `version` and a `registry` pointer and NOTHING else. `taxi_mlops.features.sets.resolve` is the only expansion in the program and it RAISES on a stray column list in `train.yaml` (`test_a_second_home_is_refused_loudly`), so the second home cannot grow back. The stale `trip_distance` line is gone and a test walks every set in the registry asserting none re-admits an EXCLUSIONS column, plus a line-level check that the file does not name `trip_distance` outside a comment. `v1` still resolves to the champion's five columns in order, pinned by its own test and re-proved live: `make verify-m2` GREEN 54/54 after the refactor. **Row CLOSED.** | Closes when (1) `configs/promotion.yaml` is deleted (the gate keeps its ONE home in `train.yaml`; a test may pin that no second configs/ file names a gate knob), and (2) feature-set definitions have ONE home — `features.yaml` made real and referenced by `train.yaml`, or deleted with the version registry living in `train.yaml` — with the stale `trip_distance` line gone and the EXCLUSIONS guard still refusing at both ends. NOT closable by a comment in either stub pointing at the other: a pointer is a third copy of the truth. |
| F-012 | 2026-08-17 | **S3** (low; latent, asymmetric check) | **`make predictions` checks the challenger half of the gate's argument against the registry and leaves the FLOOR half unchecked — and every KPI-13 number the mart, the board and the memo publish is built on that unchecked half.** `taxi_mlops.training.score._check_against_registry` (src/taxi_mlops/training/score.py:157-186) compares the re-scored champion MAE against the version tag `gate_challenger_mae` and REFUSES to write on a mismatch — correctly called "the strongest thing this command does". But `score.score()` also RE-FITS the honest floor from the six train months (score.py:216-223) and writes `floor_predicted_minutes` on all 12,140,456 published rows, and that refit is never compared against the version's own `gate_floor_mae` tag, which is sitting right beside the one that IS checked (read live 2026-08-17: `gate_floor_mae = 3.5090`). Consequence: `marts.error_segments.kpi_13_margin_vs_floor_pct`, the memo's whole §1 decomposition and the error-segment board all quote a comparison against a floor that no check ties to the floor the gate actually used. `verify-m2` does not close the hole either — §5 compares only the champion's number, and §6's rollup compares only KPI-11/KPI-12 (the model's), never the floor's. Concrete failure: a future session re-runs `make predictions` after the training window has moved (M7's retrain window, or an F-008-style `--train-months` invocation) without re-training; the champion still re-scores at 3.2608 so the write proceeds, but every published `floor_predicted_minutes` now comes from a different lookup table, KPI-13 shifts across the whole mart, and `verify-m2` stays GREEN 49/49. Currently CONSISTENT — verified live this session: the published floor scores 3.5089986 on test against the tag's 3.5090, and the refit's `groups` (1,610,050) and `fallback_minutes` (11.15) are recorded in `predictions.json`. This is a latent gap, not a live defect. | MLE | **CLOSED 2026-08-17 (M3-S1)**: `score.score` now (1) fits the floor NAMED BY THE CHAMPION's `gate_floor` tag rather than by the current config — after F-010 those legitimately differ, and publishing rows compared against a bar the champion never faced would make every KPI-13 a comparison nobody made — and (2) checks its re-scored MAE against `gate_floor_mae` as a REFUSAL TO WRITE, on the same terms as the challenger check. RED TEAM (`make predictions-redteam`, transcript in `docs/promotion_gate_m3.md` §4): floor fitted on 2019-01 instead of the six configured months → write REFUSED, exit 2, and every published file byte-identical by **sha256** before and after. An untagged floor is a refusal too, deliberately asymmetric with the challenger check's note. | Closes when the floor's re-scored MAE is checked against `gate_floor_mae` on the same terms as the challenger's — a refusal to write, not a printed note — with a transcript showing the check naming both numbers, AND a red-team showing the write REFUSED when the floor is fitted on a different set of months. NOT closable by adding the floor number to a doc or a manifest field: `predictions.json` already records it and nothing reads it back. |
| F-011 | 2026-08-17 | **S2** (medium; latent, and it fires the first time there are two champions) | **The promotion gate never compares a challenger to the model that is currently serving. It compares to the floor, and `@champion` moves on any pass — so a model materially WORSE than the incumbent can take the alias, and nothing in the codebase notices.** `gate.decide()` (src/taxi_mlops/training/gate.py:112-184) reads exactly two `Metrics`: the challenger and `baseline-group-median`. The registry is not consulted. The second condition is *named* `KPI-10 ... does not regress` but line 163 measures `challenger.within_tolerance_rate >= floor.within_tolerance_rate` — a regression against the FLOOR, not against what is deployed. `registry.promote()` then moves the alias whenever `previous != version` (registry.py:135-138), with no comparison of the incoming version's tags to the outgoing one's; `tests/unit/test_training_gate.py::test_a_new_winner_moves_the_alias_and_keeps_the_old_version` pins exactly that behaviour. Concrete failure, in M3's own units: a tuned challenger scoring **3.40** min KPI-09 on test is **worse than champion v1's 3.2608**, but against the floor's 3.5090 it observes **+3.11% ≥ 2.00%** → PASS; if its KPI-10 is **80.5%** it clears `>= 80.322%` (the floor's) → PASS; the gate prints PROMOTE, `@champion` moves, and roughly **58,000 more test-month riders** are quoted wrongly than under the model it replaced. M5 will deploy that alias and M7 will retrain into it. The nearest prior mention is a *recommendation* in `docs/error_memo_m2.md` §7 row 3 ("today's gate cannot tell them apart"), which is about coverage vs accuracy and carries no landing; `docs/promotion_gate_m2.md` §6 "Limits" does not name this one. Filed as a finding rather than left as memo prose for the reason F-008 states in this same register: a limit recorded only in prose is one the next session does not run into. | MLE | **CLOSED 2026-08-17 (M3-S1)** via BOTH (a) and (b), because either alone can be walked around: `gate.decide` gained incumbent conditions on KPI-09 AND KPI-10 fed from the resolved `@champion` version's own tags (the registry read lives in `run._resolve_incumbent`, so `decide` stays pure — pinned by the existing no-side-effects test), and `registry.promote` gained a REQUIRED `incumbent_version` that it checks against the live alias, refusing any promotion decided against a champion that is not the one being replaced. WATCHED REFUSAL (`make gate-redteam`, transcript in `docs/promotion_gate_m3.md` §3): a challenger at **3.2667 min / 81.423%** cleared the floor bar at **+2.54%** and was REFUSED on both incumbent conditions against version 1's **3.2608 / 81.480%**, with the floor conditions still passing; the bypass (`incumbent_version=None`) was refused by `registry.promote`; the registry snapshot was **identical** before and after (alias 1, versions [1]). The condition has no config knob, by design. | Closes when a challenger that is worse than the resolved `@champion` on KPI-09 or on KPI-10 cannot take the alias — either (a) `gate.decide` gains an incumbent condition fed from the champion version's own tags (which is why F-012's floor check and this row are cheap together), or (b) promotion is separated into a second, explicit comparison against the incumbent that `registry.promote` refuses to bypass. Evidence must include a WATCHED refusal: a challenger that clears the floor bar and is refused for regressing against the incumbent, with both models' KPI-09 AND KPI-10 in the transcript, plus proof the alias did not move (the M2-S3 red-team's snapshot-before-and-after shape). Tightening a gate is the MLE's to argue for (CLAUDE.md), so this needs no PO fork. NOT closable by documenting the limitation. |
| F-010 | 2026-08-17 | **S2** (medium; the gate's stated headroom is 2.6× smaller than argued, and M3's bake-off inherits the bar) | **The promotion gate's "headroom, deliberately" (7.07% observed vs a 2.00% bar) is mostly an artefact of the floor's single-level fallback, not of the model. Give the SAME floor one more backoff level — the most obvious one, from the same train data, no new feature and no new model — and v1's margin collapses from +7.07% to +2.71%.** RE-DERIVED THIS SESSION from committed artifacts: for the 87,989 test rows (1.479%) where `floor_unseen_group` is true, replace the global-median guess (11.15 min) with the train median of the row's (PULocationID, DOLocationID) — 46,938 OD cells fitted over the same 43,987,422 train rows; **87,008 of the 87,989 unseen rows (98.9%) resolve to a real OD cell**. Result on the untouched test month: floor MAE **3.5090 → 3.3518**, floor KPI-10 80.322% → 80.733%, and the champion's margin **7.07% → 2.71%** against a 2.00% bar (KPI-10 margin +1.158 → +0.747 points). So the headroom `docs/promotion_gate_m2.md` §2 argues is generous — "so it is a bar and not a rubber stamp cut to fit the model we happen to have" — is **1.35×**, not 3.5×, once the floor is as good as a second `GROUP BY` makes it. The program computed the number that predicts this and did not carry it back to the gate: M2-S4's memo §1 establishes that **75.4%** of the champion's entire advantage is bought on those 1.48% fallback rows, and names "a coarser key" and "more train months" as things that move the bar — but not a better fallback, and no session re-measured the verdict against one. **v1's promotion still stands** (2.71% clears 2.00%), so nothing is being rolled back and this is not fork-class; what does not stand is the claim that the bar has room to spare, and M3's tuned challenger is judged against that same bar. `configs/train.yaml: baselines` anticipates the mechanism ("A deeper hierarchy (PU,DO -> hour,dow -> global) would predict better and would no longer be the published floor... it is a NEW baseline with a new name") but argues it only as EDA comparability — the consequence for the gate is unstated and, until now, unmeasured. | MLE | **CLOSED 2026-08-17 (M3-S1)** via option (a): `baseline-group-median-od-fallback` implemented as a NEW named baseline (never an edit — `configs/train.yaml: baselines` legislated that in M2), measured through the SAME evaluator on full data — **3.5515 val / 3.3518 test KPI-09, 80.733% test KPI-10**, 1,610,050 groups + **46,938 backoff cells** — and ADOPTED as the gate's floor in `configs/train.yaml: gate` with the reasoning beside the value and DR-06 cited (AI-4 discharged). This row's re-derivation from published rows predicted 3.3518 and +2.71%; the fit measured 3.3518 and +2.71% (transcript: `docs/promotion_gate_m3.md` §1–§2). The 2.00% bar is unchanged and re-argued as a maintenance-cost bar against the NEW floor (~4.0 s of mean error); headroom is stated as **1.35x, not 3.5x**, and +7.07% is named in the config as not quotable as headroom. | Closes when M3 does ONE of: (a) the gate's bar becomes a floor whose fallback is stated and defended AS A GATE DECISION — e.g. a new named baseline (`baseline-group-median-od-fallback` or similar, never an edit to the published one, per the config's own law) with the margin bar re-argued against the number it actually produces; or (b) the current floor is KEPT with the measurement in this row quoted in the gate document, and the 2.00% bar re-derived as a maintenance-cost bar against **2.71%** rather than 7.07% — i.e. the argument is made against the real headroom, whichever floor wins. Either way the dossier states what the booster buys over the best simple predictor, not over the first one. NOT closable by re-quoting 7.07%. Evidence available now: the query above is reproducible in one command from `data/predictions/test/*.parquet` + `data/processed/train/*.parquet`, both DVC-pinned; every input number in it (3.2608, 3.5090, 81.480%, 80.322%, 1.4786%, 87,989) was re-derived from the published rows this session and matched the run's logged metrics to 13 significant figures. |
| F-009 | 2026-08-17 | medium (blocks every consumer of the registry, with an error that blames the wrong thing) | **On MLflow 3.15.1 a registered model cannot be loaded by its alias URI — `mlflow.lightgbm.load_model("models:/nyc-taxi-eta@champion")` raises `No such artifact: 'MLmodel'`, while `mlflow.models.get_model_info()` on the SAME uri resolves happily.** Cause: MLflow 3 stores the artifacts of a logged model under `.../models/m-<logged-model-id>/artifacts`, but the registry version's `source` is still `runs:/<run_id>/model`. The registry-uri load path resolves the alias to the version, reads that `source`, and looks under the RUN's artifact prefix — where nothing was ever written — so it hands back an empty directory and the failure surfaces as a missing `MLmodel` file. The error names an artifact, so it reads as a corrupt or half-logged model; the model is fine and the promotion was fine. Found at M2-S4 by de-risking the load API before spending a scoring run on it, not by losing one. Impact beyond this story: **M5 serves this champion**, and KServe's MLflow path addresses models by exactly this kind of URI. A serving story that meets this for the first time meets it as a deployment failure with an artifact-shaped error message. | MLE | **CLOSED 2026-08-19 (M5-S2)** by the row's option (b) | **Worked around and localised at M2-S4, deliberately in ONE place**: `taxi_mlops.training.score.load_champion` resolves the alias to the logged-model uri with `mlflow.models.get_model_info(uri).model_uri` and loads THAT, printing the resolution on stdout (`[score] alias models:/nyc-taxi-eta@champion resolves to models:/m-4a4e7bdc… (F-009: load it, not the alias)`). The alias stays the entry point — this is a resolution step, not a second address for the champion, and nothing else in the codebase learns the logged-model id. Closes when M5 does ONE of: (a) the serving path loads the champion by alias without the resolution step, because the version's `source` has been made to point at the logged model (an MLflow-side fix or a change to how `registry.promote` registers it) — with a transcript showing the bare alias uri loading; or (b) the resolution step is proven to be what serving needs too, i.e. the KServe deployment resolves the alias the same way, and this becomes a documented property of MLflow 3 rather than a workaround. NOT closable by the workaround continuing to work — it already does. Evidence available now: the M2-S4 scoring transcript, and the fact that the SAME uri succeeds through `get_model_info` and fails through `load_model` on client and server both 3.15.1. **NARROWED 2026-08-17 (M2-S5), and the narrowing is what M5 needs most: F-009 has an impostor.** A first draft of `verify_m2.sh`'s registry leg reached MLflow with a bare `mlflow.set_tracking_uri(...)` instead of `taxi_mlops.training.tracking.configure()`, and `get_model_info` on the champion alias failed with `Failed to download artifacts from path 'MLmodel'` — close enough to F-009's message to read as "F-009 is worse than we recorded, it breaks `get_model_info` too". It is not F-009. Our server does not proxy artifacts (gotcha #5), so a client without the MinIO endpoint and credentials cannot read ANY artifact, and the first one a model read touches is `MLmodel`. **The discriminator, and it costs one call:** under F-009 `get_model_info` SUCCEEDS on the same uri that `load_model` fails on; under missing credentials both fail, and so does reading any unrelated artifact of any unrelated run. Recorded as gotcha #39. This does not close the row and does not change its landing — it removes the false trail M5 would otherwise walk first.  **CLOSING EVIDENCE (M5-S2, 2026-08-19) — option (b), and option (a) is UNAVAILABLE rather than unpreferred.** (a) would require the version's `source` to point at the logged model; `source` is set when a version is CREATED and MLflow exposes no way to change it, so making it right would mean registering a NEW version — exactly what M5 is legislated not to do (kickoff law 2, `@champion` is version 2 before and after this story) — and it would fix one version while leaving every earlier one, **including version 1, the rollback target M5-S5's typed rollback depends on**, pointing at the same empty prefix. (b) is discharged with a stronger statement than "the workaround still works": **serving needs the same two hops, and a deploy that trusted `source` would fail SILENTLY.** The champion's `source` is `runs:/92b73bd4f77d…/model`, which resolves to `s3://mlflow-artifacts/6/92b73bd4f77d…/artifacts/model` — a prefix with nothing under it, and MLflow says so itself while resolving (`No artifacts found to download at … Returning destination path`). KServe's storage-initializer would download zero objects and **succeed** — there is no error in "the prefix is empty" — and mlserver would then fail on a missing `MLmodel`: F-009's exact signature at the serving boundary, an artifact-shaped error about a model that is perfectly fine. **THE DOCUMENTED MLFLOW-3 PROPERTY, written so it can be checked rather than remembered:** a registered model version's `source` is a RUN uri while its artifacts live under the LOGGED MODEL's `artifact_location`; every consumer that needs bytes — loader, scorer, serving runtime — must resolve `alias -> logged model -> artifact_location`, and none of them may read `source`. The serving path does it in ONE place (`scripts/resolve_champion_storage.py`, a READER pinned by a test), exactly as the training path does in `taxi_mlops.training.score.load_champion`; a test refuses a run id, a logged-model id or a resolved bucket path as a literal in any committed serving file, so the alias stays the only address. **Gotcha #39's discriminator was run FIRST and is wired into the resolver as `--check`**, so the impostor cannot cost the next reader an hour: observed `get_model_info(models:/nyc-taxi-eta@champion)` SUCCEEDS while `load_model` on the SAME uri fails with `No such artifact: 'MLmodel'` — under missing MinIO credentials both would fail. The resolved store `s3://mlflow-artifacts/6/models/m-04478c47…/artifacts` was downloaded by the predictor and the endpoint returns a prediction that matches the locally-loaded champion BIT FOR BIT (absolute delta 0.000e+00 on one row; the 1e-6 gate over the honest hazards is M5-S3's). Narrative: `docs/champion_on_the_wire_m5.md` §3 |
| F-008 | 2026-08-17 | medium (latent; makes the gate LOOSER exactly where M3 will lean on it) | **A promotion-gate verdict from a sampled training run is not comparable to one from a full run, and the direction of the error is the dangerous one: sampling makes the gate EASIER to pass.** The bar is `baseline-group-median`, re-derived from the same training data as the challenger (deliberately — a floor quoted from a document drifts silently). So shrinking the training set degrades the FLOOR as well as the model, and the floor degrades faster: its lookup table loses whole (hour, dow, PU, DO) cells and falls back to the global median more often, while a booster keeps generalising. Measured on this story's smoke run (`--train-months 2019-01`, one month of six): the floor moved 3.5090 → **4.1138** min test MAE while the model moved 3.2608 → **3.4207** — i.e. the challenger got **worse** and its margin over the floor **rose** from 7.07% to **16.85%**. A model that would be refused on full data can therefore be promoted on a sample, and the transcript looks better, not worse. This is not a defect in the gate as specified (M2-S3's kickoff scopes it to full-data contenders and `make train` uses the configured months); it is a trap laid directly across M3's path, whose scout and sniper train on samples **by design** (ADR-007/ADR-008, `configs/automl.yaml` time budget). | MLE | **CLOSED 2026-08-17 (M3-S1)** via option (a), with (b) carried alongside for free: `gate.assert_full_train_months` refuses to issue a verdict for a run whose train months are not the configured set, raising BEFORE a row is read (the refusal costs seconds, not a training run) — observed exit **2** with the message naming both month sets and the DIRECTION of the error. `--no-gate` is the sample-first smoke path and is legal ONLY with `--train-months`, so it can never skip a gate a promotable run would have faced; it promotes nothing, exits **3** (its own code), and tags its MLflow runs `sample_run` / `do_not_promote` / `gate_verdict: NONE` — which is option (b). This session's sampled smoke run also re-measured the row's own numbers from scratch: floor **4.1138**, model **3.4207** on test, M2-S3's figures to four decimals. Transcript: `docs/promotion_gate_m3.md` §5. | Closes when M3 does ONE of: (a) the gate refuses to issue a verdict for a run whose train months are not the configured full set (the `--train-months` override becomes gate-disqualifying, matching how `gate.decide` already raises on the wrong SPLIT rather than warning), or (b) the sample is recorded ON the verdict and on the registered version's tags, so a promotion earned on a sample cannot be read as one earned on the full data, with the comparability limit stated in the dossier. NOT closable by a comment in a doc — S3's `docs/promotion_gate_m2.md` §6 already carries the observation, and this row exists precisely because a limit recorded only in prose is one the next session does not run into. Evidence available now: the smoke transcript numbers above, all four from `taxi_mlops.training.evaluate`. |
| F-007 | 2026-08-16 | medium (latent trap; would pass every offline check) | **The columns most correlated with the target are not available when an ETA is quoted.** `fare_amount`, `tip_amount`, `tolls_amount`, `total_amount`, `payment_type` and `store_and_fwd_flag` are recorded at or after trip end. Windowed to `fare_amount BETWEEN 0 AND 200`, fare correlates with duration at **r = 0.8708** (`docs/eda_report.md` §8) — so a model using them would score superbly on any held-out split and be unimplementable at M5's serving boundary, with nothing in the offline evidence to reveal it. The sharper half: **`trip_distance` has the same shape and is the single strongest predictor** (r = 0.8066 raw, 0.8464 in logs, §9). The TLC value is the meter's *driven* distance — what actually happened — not a quote-time route estimate. A serving system does not have it. | MLE | **CLOSED 2026-08-17 (M3-S2)** — (a) discharged 2026-08-17 (M2-S2); (b) resolved formally at the M3 Design Review, decision **DR-04**, minutes committed (`docs/rituals/2026-08-17_design-review-m3.md`). Both conditions now hold, so the row closes. | Closes when (a) `taxi_mlops.features` defines and documents the quote-time feature set explicitly, with the post-trip columns named as excluded and why, AND (b) the M3 dossier resolves `trip_distance`: either an honest quote-time substitute (OSRM / zone-centroid distance, already M3's named scope) or a recorded decision that the meter distance is treated as available with the assumption stated. Discovered by M1-S3's EDA, which computed the correlations that make the trap attractive. **Intaken at the M2 kickoff 2026-08-17: condition (a) lands in M2-S2 — `taxi_mlops.features` names the post-trip exclusions in code with reasons, pinned by a test. Condition (b) stays M3's dossier (quoted M3 scope already owns OSRM / zone-centroid distances). Row stays open until BOTH conditions hold.** | **Condition (a) evidence, 2026-08-17 (M2-S2):** `taxi_mlops.features.quote_time.EXCLUSIONS` names all six post-trip columns with one shared reason quoting the r = 0.8708 windowed correlation, and adds the three money columns F-007 did NOT list (`extra`, `mta_tax`, `improvement_surcharge`) under the same reason — they are recorded on the same meter at the same moment, and a registry that agreed with the finding rather than with the world would be the next trap. The guard is a raise, not a warning: `FeatureLeakageError` refuses a matrix or a config containing any of them, tested per column (`tests/unit/test_features.py::test_every_post_trip_column_f_007_names_is_excluded_with_a_reason`, `::test_no_excluded_column_can_be_configured_into_the_feature_set`, `::test_a_post_trip_column_reaching_the_matrix_by_another_route_still_fails`). `trip_distance` is excluded too, with its deferral recorded ON the exclusion (`revisit`: M3's dossier owns the quote-time substitute), which is what keeps (b) visible instead of forgotten. **Condition (b) evidence, 2026-08-17 (M3-S2):** resolved by MEASUREMENT at the M3 Design Review, decision **DR-04** — `trip_distance` stays excluded and the **zone-centroid haversine distance is the quote-time substitute**, not a recorded assumption. The number that decided it, computed this session over **43,439,267** train rows through the DuckDB analyst layer joined to the new centroid table: the meter's driven distance correlates with the target at **0.8068** (reproducing the EDA's independently computed 0.8066, which is what says the query measures what it claims), and the centroid straight-line distance — knowable from `PULocationID`/`DOLocationID` at quote time — correlates at **0.7873**, i.e. it retains **97.6%** of the excluded column's power. Supporting: centroid distance vs meter distance `r` = **0.9661** over 41,182,160 rows, straight-line ≤ driven on **81.662%**, median circuity **1.2952**. The substitute is real, not notional: `data/reference/taxi_zone_centroids.csv` (263 rows, sha256 `37910367…`) is committed, derived by `make zones` from the sha256-pinned TLC shapefile with the CRS read from the .prj, and guarded by 13 cluster-free tests including a byte-identity re-derivation twin — red-teamed by a 111 m edit to one row, which turned two legs red while every semantic check still passed. Three conditions ride on DR-04 and are M3-S3's: an explicit, tested fallback for the **1.0113–1.2462%** of rows whose zone is 264/265 ("Unknown", no geometry); circuity REFUSED as a feature because its numerator is the excluded column; and `EXCLUSIONS` keeps refusing `trip_distance` at both ends, with the `revisit` note discharged by pointing at DR-04 rather than by re-admitting the column.
| F-006 | 2026-08-16 | medium (silent in training, untestable in val/test) | **`congestion_surcharge` is switched on mid-training-window and any feature or imputation built on it learns "January".** The column is 63.4565% null in 2019-01 and 0.42-0.56% null in every other month; per-day, it cliffs from 99.822% null on 2019-01-20 to 1.118% on 2019-01-21 (`docs/eda_report.md` §7b, per-day table). 2019-01 is a TRAIN month and 2019-07/08 are clean, so a model fitted with this column would look unremarkable in training and the defect would be **invisible in validation and test** — the two splits that would otherwise catch it. Same family as gotcha #31 (drift by column) and the M1-S2 review's drift by value; this one is drift by *availability*, and it is inside the training window. Sibling fact from the same section: **`airport_fee` is 100% null across all 56,127,878 rows** — a plausible-looking column carrying zero information in this window. | MLE | **closed 2026-08-17 (M2-S2)** | **EXCLUDED from feature set v1, in code, with the reason attached to the column** — `taxi_mlops.features.quote_time.EXCLUSIONS` carries `congestion_surcharge` with the 2019-01-21 cliff quoted (63.4565% null in 2019-01, 99.822% -> 1.118% across one day, 0.42-0.56% elsewhere) and `airport_fee` with its 100%-null-over-56,127,878-rows reason. The alternative the row allowed — training from 2019-02 onward — was considered and REFUSED in writing on the exclusion itself: it would buy one surcharge for 9.3M rows (a sixth of train). Imputation was never available, the row having ruled it out, and the registry says why (the training set is 1/6 contaminated, so the imputer learns the cliff). This is not a silent inclusion and not a silent exclusion: `tests/unit/test_features.py::test_the_findings_intaken_by_this_story_each_own_an_exclusion` fails if either column loses its registry entry, and `test_no_excluded_column_can_be_configured_into_the_feature_set` fails if configs/train.yaml re-admits one. Measured consequence, for the record: v1 reaches its numbers WITHOUT this column.
| F-005 | 2026-08-16 | medium (design gap, review finding) | The rejected rows exist only as COUNTS. M1-S1 attributes every dropped row to a named rule and writes the totals beside each output (914,459 rows, 1.603% over 2019-01..08) but retains none of them, so "how many" is answerable and "which kind" is not. `duration_above_max` alone removes 159,300 trips over 120 minutes; whether those are meter faults or a real long-haul/airport population cannot be decided from any artifact that exists, and no view in the analyst layer can reach them. Raised as DCR-01 at the Data Contract Review 2026-08-16 (`docs/rituals/2026-08-16_data-contract-review.md`), with dissent recorded: the DA holds that deferring it means the EDA report cannot say whether the missing 1.603% was noise or a population. | DE | **closed** | Closes when ingest retains the rejected rows in a form a query can characterize -- a sidecar under `data/rejected/<split>/`, carrying the rule that rejected each row, DVC-tracked and reachable from the analyst layer as a view -- AND a query over it answers what the `duration_above_max` population actually is. NOT closable by adding more counts. Deliberately a finding and not a debt row: no milestone's quoted scope in BLUEPRINT §9 promises this capability, so inventing a landing would be the carried-to-nowhere failure (gotcha #19). Proposed home: M1-S3, the story that first needs it (its EDA examines the target honestly); if S3's kickoff scope is judged not to cover writing new ingest artifacts, this becomes an ARCH scoping call at the M1 boundary, not a silent slide. **M1-S3 JUDGED IT OUT OF SCOPE, 2026-08-16 — reasons recorded, routed to ARCH at the M1 boundary as the finding's own condition requires.** The kickoff's S3 is a pure-docs story ("Safe stop: after merge; pure-docs story, no state touched"); a rejected-row sidecar is none of that. It requires (a) changing `taxi_mlops.data` ingest to retain rows, (b) re-running `make ingest` over 57M rows, which rewrites the very `data/processed/` artifacts M1-S2 proved byte-identical two sessions ago and would demand a fresh rebuild proof, (c) ~+1 GB DVC cache and remote, (d) a new analyst view plus its reconciliation test. That is a DE story, not a paragraph in an EDA. **ARCH SCOPING CALL MADE AT THE M1 BOUNDARY, 2026-08-17 (the disposition this row prescribes): ABSORBED INTO M2-S1 (role:DE), the first story of the next milestone.** Landing scope quoted from BLUEPRINT §9/M2: the DA error memo must answer *"where does it fail: zones? hours? long trips?"* — the long-trips answer is bounded by rule at 120 minutes unless the discarded `duration_above_max` population becomes queryable, and the val/test months are the two dirtiest, so the memo would otherwise sit on exactly the data whose discards are least characterized. Stays a FINDING (not converted to debt); closes ONLY by the closing conditions above — sidecar carrying the rejecting rule, DVC-tracked, reachable as an analyst view, and a committed query answering what `duration_above_max` actually is. **The DA's dissent stands and is now evidenced rather than predicted**: `docs/eda_report.md` opens at §0 by declaring that every number in it describes the surviving 98.397%, and §2 states plainly that whether the 159,300 trips over 120 minutes are meter faults or a long-haul population "this report cannot answer and does not guess". The omission is loud in the deliverable, which is the most S3 could honestly do without it. Two new facts from S3 that ARCH should weigh: the rejection rate is **not stationary** (1.428% in 2019-04 rising monotonically to 2.020% in 2019-08, +41% relative), so the discarded population is growing as volume falls; and the val/test months are the two dirtiest, so the held-out evaluation sits on the thinnest-characterized data. **CLOSED 2026-08-17 (M2-S1), by every one of the conditions above, each with its evidence.** (1) *Retained in a form a query can characterize*: `make ingest` writes `data/rejected/<split>/yellow_tripdata_<month>.parquet` — 16 files, 27 MB, every contract column plus the derived target. (2) *Carrying the rule that rejected each row*: `rejection_rule` (first-match, so it reconciles to `rejected_by` exactly) and `rejection_rules` (every rule the row violates). (3) *DVC-tracked*: `data/rejected.dvc`, pinned LAST by `make data` (gotcha #33's order law) and pushed (`9 files pushed`, `Cache and remote 'localstore' are in sync`). (4) *Reachable from the analyst layer as a view*: `trips_rejected`, VIEWS now 10. (5) *NOT closable by adding more counts* — and the counts are what CHECKS it: `make duckdb` reconciles the sidecar per (month, rule) against the ingest report and exits 1 on any disagreement. Observed live 2026-08-17: **914,459 sidecar rows == 914,459 counted, 80 (month, rule) pairs checked, 0 disagreements**. Red-teamed in unit form three ways (rows removed, rows relabelled under the wrong rule, sidecar deleted) — each goes RED and the CLI exits 1. (6) *A query answers what the `duration_above_max` population actually is*: `docs/rejected_rows_appendix.md` (Appendix R), every number from a named view, SQL quoted per section. **The answer: both, in a ratio of 24 to 1.** 135,460 of the 159,300 (85.035%) are the 23-24 h band — median 2.19 miles, median fare $12.00, 98.97% dropped off the NEXT DAY and 62.64% within the same clock hour: an ordinary short trip whose session was closed a day later, whose money was never wrong, and which no ETA model is missing out on. 5,601 (3.516%) in the 120-180 min band are a REAL long-haul population: 52.78% touch an airport, 66.01% run 10+ miles, 78.41% cost $40+, and 32.87% carry an out-of-city rate code against 2.7497% of the clean data (12x). The bands between are a graded mixture and the gradient is monotone in every discriminator. **The DA's dissent from the Data Contract Review is discharged**: the EDA can now say what the missing 1.603% was, and does — `docs/eda_report.md` §0/§2 carry the answer inline and E-11 is marked ANSWERED. **No rule was changed and none is proposed**: `max_minutes: 120.0` stands (the population it removes is 85% unusable; admitting the 5,601 genuine trips would admit the wall with them), and loosening a threshold is a PO fork in any case. **Side proof, free and load-bearing**: re-running the whole path with the retaining ingest returned all 8 processed outputs BYTE-IDENTICAL — `make rebuild-proof` GREEN at **16/16** outputs across both derived trees, two witnesses (`data/processed.dvc` and `data/rejected.dvc` both `up to date`). The sidecar was folded INTO the proof rather than left beside it. Two facts S3 asked ARCH to weigh, now answered: the rising rejection rate (1.428% -> 2.020%) is **not** driven by this rule (its share is flat, 0.273%-0.299% per month), so the trend lives in `duration_below_min`/`distance_non_positive`; and the `plausible_long` count more than doubles across the window (417 -> 1,020), which is the number M2-S4's long-trip segment should quote. |
| F-004 | 2026-08-16 | HIGH (data loss risk, FIXED same session) | `make destroy DRY_RUN=1` deleted the kind cluster — and every PVC in it — while printing `[destroy] DRY_RUN=1 — nothing was deleted.` `cmd_destroy` guarded the file loop behind DRY_RUN but called `cmd_down` unconditionally before it. The M0-S2 unit test missed it because its sandbox names a cluster that cannot exist, so the delete path always no-opped (gotcha #30). | MLOps | **closed** | Fixed in `scripts/cluster.sh` (cluster deletion now obeys DRY_RUN, printing `WOULD delete kind cluster 'mlops-taxi' (and with it every PVC inside)`). Proved live 2026-08-16 (M0-S4): `make destroy DRY_RUN=1` against the rebuilt LIVE cluster left `kind get clusters` → `mlops-taxi`, 3/3 nodes, MLflow `/health` → OK. Regression-pinned by `test_destroy_dry_run_spares_the_cluster_itself` (fake `kind` records its calls) + a positive control; red-teamed by reverting the fix → the new test FAILS, quoting `[cluster-down] deleting kind cluster` next to `nothing was deleted` |
| F-003 | 2026-08-16 | low (cosmetic, verified harmless) | `kubectl apply -f infra/manifests/postgres.yaml` reports `statefulset.apps/postgres configured` on EVERY run, never `unchanged` — which reads like a recipe that mutates the cluster each time it is run. Verified it does not: `kubectl diff -f …` prints nothing, `metadata.generation` stays 1 = `status.observedGeneration`, the pod keeps its original creationTimestamp and 0 restarts across three applies. Believed to be kubectl's apply-patch bookkeeping for StatefulSets carrying `volumeClaimTemplates`. | MLOps | **closed** | Closes when either (a) a session identifies the exact field and the manifest stops provoking the patch, or (b) a later kubectl reports `unchanged`. Do NOT "fix" it by removing the volumeClaimTemplate — the noise is cheaper than losing the PVC story. M0 boundary triage 2026-08-16: stays open by these conditions (an observation defect with a defined closure, deliberately NOT converted to debt); a BOUNDED probe of condition (a) — one attempt, e.g. `kubectl apply` server-side diff of the last-applied annotation — is folded into M1-S4, the story already editing `infra/manifests/postgres.yaml` for D-002. If the one attempt finds nothing, it stays open; do not chase **CLOSED 2026-08-16 (M1-S4) by closing condition (a) — the field is named and the manifest no longer provokes the patch.** The bounded one-attempt probe was `kubectl apply -f infra/manifests/postgres.yaml -v=9`, which prints the PATCH body kubectl actually sends. It is exactly one field: `{"spec":{"volumeClaimTemplates":[{"metadata":{"name":"data"},"spec":{"accessModes":["ReadWriteOnce"],"resources":{"requests":{"storage":"8Gi"}}}}]}}`. CAUSE: `volumeClaimTemplates` is an ATOMIC list under strategic-merge patch (no patchMergeKey), so kubectl cannot merge it element-wise and compares the whole list against the live object — into which the apiserver has defaulted `apiVersion: v1`, `kind: PersistentVolumeClaim`, `spec.volumeMode: Filesystem` and `status: {phase: Pending}` (read back live). Our manifest omitted all four, so desired could never equal live and the list was re-sent on every apply. FIX: state those four defaults in the manifest. `storageClassName` stays UNSET — the apiserver does not write it back, so naming kind's local-path would cost portability for nothing, and the M0-S3 comment about not naming a cloud-specific class stands. EVIDENCE, three applies in a row: `configured (server dry run)` -> `configured` (the run that updates the last-applied annotation) -> **`statefulset.apps/postgres unchanged`** — the first `unchanged` in this project's life. Nothing was disturbed: `metadata.generation` still 1 = `status.observedGeneration`, pod `postgres-0` keeps `creationTimestamp 2026-08-16T15:46:45Z` and `restarts=0`, `kubectl diff -f` prints nothing. Regression-pinned by `tests/unit/test_marts.py::test_postgres_manifest_states_the_server_side_defaults`, which also pins that `storageClassName` stays absent. REMAINING CONDITION, stated honestly: this was proved on an EXISTING object. M1-S5's deliberate cluster rebuild re-applies the manifest to an empty cluster and should re-observe `unchanged` on its second apply; if it does not, reopen with that transcript. **REMAINING CONDITION DISCHARGED 2026-08-17 (M1-S5), and the result was better than the prediction.** After `make cluster-down && make cluster-up && make deploy-platform` created a brand-new StatefulSet on a brand-new 8Gi PVC, `kubectl apply -f infra/manifests/postgres.yaml` printed `configmap/postgres-initdb unchanged` · `service/postgres unchanged` · `statefulset.apps/postgres unchanged` on the **first** apply, and the same three lines on the second. The prediction was `configured` then `unchanged`; on a fresh object even the first apply is a no-op, because deploy_platform.sh's own apply had already written the last-applied annotation from the same manifest that now states the four server-side defaults. F-003 stays CLOSED — the fix is a property of the manifest, not of the object it was first observed on. |
| F-002 | 2026-08-16 | low (known limitation, documented) | The gotcha #10 port pre-check reads `ss` INSIDE the WSL VM, so it cannot see a Windows-native process holding a family port — yet Docker Desktop publishes container ports on the Windows host too, so such a conflict is real and would surface as an opaque bind failure. Observed as sound for our own stack: after cluster-up, `ss -tlnp` in WSL does show kind's 8081/8443, so anything inside the VM (incl. all of Docker) IS covered. | MLOps | **closed** (downgraded to a documented note) | Closes when either (a) a session observes the failure mode and the script gains a Windows-side probe, or (b) M0-S3/S4 run the full platform with no unexplained bind failure and the limitation is downgraded to a documented note in `scripts/port_precheck.sh` (where it already is). CLOSED 2026-08-16 (M0-S4): closing condition (b) met — M0-S3 and M0-S4 each ran the full platform (MinIO + Postgres + MLflow, five published host ports) with zero bind failures, including a from-scratch cluster create after a full destroy. The limitation stays documented at `scripts/port_precheck.sh` lines 22-25, which names the Windows-side probe (`Get-NetTCPConnection -LocalPort <p>`) for the day it does bite. |
| F-001 | 2026-08-16 | low (friction, non-blocking) | Session allowlist omits the ordinary verbs the allowed tools depend on (`chmod`, `ls`, `mkdir`, `tar`, `printenv`); agent cannot self-widen (harness refuses writes to `.claude/settings*.json`); `~/.local` outside the file-tool sandbox. M0-S1 completed via allowlisted `python3` workarounds. | PO | open | Closes when AWAITING_PO 2026-08-16-2 is answered and a session runs `chmod`/`ls` without an approval prompt (gotcha #27) |
