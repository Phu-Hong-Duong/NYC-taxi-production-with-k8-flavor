# Debt register — carried obligations. A carry is legal ONLY with a landing
# milestone that exists in BLUEPRINT §9 and whose quoted scope covers the need
# (gotcha #19). Intake at the landing milestone's kickoff is mandatory (Prompt E);
# a slide without an explicit re-disposition blocks closure (Prompt G §4).
| Id | Raised (Mx, date) | What is owed | Why not fixed then | Landing M | Quoted landing scope | Status |
|---|---|---|---|---|---|---|
| D-004 | M2-S2, 2026-08-17 | **The training path only imports LightGBM because of a shim.** This WSL host ships no `libgomp.so.1` (OpenMP): `find /usr /lib /opt` empty, `dpkg -l | grep gomp` empty, so `import lightgbm` raises `OSError: libgomp.so.1: cannot open shared object file`. `taxi_mlops.training.openmp` works around it without sudo — it symlinks the copy scikit-learn's wheel vendors under the SONAME the loader wants, sets `LD_LIBRARY_PATH`, and re-execs once (gotcha #37 explains why nothing simpler works). Owed: the M4 image installs `libgomp1` as a real package, so `_load()` succeeds on its first line and the shim never runs in a container. The shim itself stays as the laptop path and must be PROVEN dead in the image — a check that the image's OpenMP is the system's, not a borrowed one. | The honest fix is `sudo apt install libgomp1`, which is the PO's hands, not an unattended session's (gotcha #23: the host is the PO's). Parking the chain overnight on one apt package would have cost a session and delivered nothing; the shim is sudo-free, announced on stdout when it fires, and has a verified undo (delete the module, the import fails loudly again). | **M4** | BLUEPRINT §9/M4: "v1's M3 unchanged: Flyte 2 per docs, **containerized**, ingest->validate->features->train->evaluate->register" — the story that puts training in an image is the story that owes it a real OpenMP package. | open — **intaken at the M4 kickoff 2026-08-18 (mandatory intake honored): absorbed into M4-S3**, which builds the task image. Closes on in-container evidence: `openmp_status()` prints `(True, 'system libgomp.so.1')` on its FIRST line inside the image (the shim provably never fires there), pinned by a container smoke check in `verify-m4` . **CLOSED 2026-08-18 (M4-S3) by in-container evidence, and by evidence that the evidence can fail.** The image installs **`libgomp1` 14.2.0-19** as a real apt package (`docker/Dockerfile.pipeline`), and `make image-smoke` proves the consequence inside the container in 10 checks, of which four are this row's: (1) `dpkg-query` reports `libgomp1 14.2.0-19 install ok installed` and `/sbin/ldconfig -p` resolves `libgomp.so.1 => /lib/x86_64-linux-gnu/libgomp.so.1` — a system path, NOT a wheel's; (2) `openmp_status()` returns exactly `(True, 'system libgomp.so.1')` **as the first line, with nothing printed before it**; (3) `python -m taxi_mlops.training.openmp_probe` returns `openmp: system libgomp.so.1` and **no `[openmp]` announcement appears anywhere**, which is the only externally visible difference between the borrowed library and the system one; (8) `/app/.venv/lib/openmp` does not exist and no file in the venv carries the `libgomp.so.1` SONAME — even though both the xgboost and scikit-learn wheels still ship a vendored copy the shim COULD borrow. The row's closing condition asked for a check, and a check on a thing NOT happening is worthless unless it can flip — so `make image-smoke-redteam` bind-mounts an EMPTY FILE over `/lib/x86_64-linux-gnu/libgomp.so.1` in ONE `--rm` container (the image, the nodes and the cluster untouched; the pointer-not-the-state pattern of `verify-m2-redteam`) and asserts all three flip: the probe returns `(False, 'not loadable yet; a vendored copy exists at …')`, the shim ANNOUNCES itself, and `/app/.venv/lib/openmp` appears — then a fresh container from the same image reads `absent` again. Its exit code is inverted like `make marts-redteam`'s: a check that stays green under the mask fails the drill. The shim REMAINS in the code as the laptop path (AWAITING_PO 2026-08-17-1 is still the PO's one-line fix for this machine) and it is now also more honest than it was: the drill exposed **F-024**, a `python -c` invocation the re-exec could never reconstruct, fixed the same session. `make image-smoke` is the leg `verify-m4` calls (M4-S5's kickoff item). Transcripts: `docs/task_image_m4.md` §3 and §4. |
| D-003 | M1 boundary (ARCH), 2026-08-17 | `make marts` publishes `trips_clean` (56,127,878 rows) as a FULL REFRESH: ~13 GB steady in the Postgres volume, **~23 GB peak** mid-swap (old table + staging copy coexist, autovacuum working on the one about to drop), ~3.5 min per publish. Fine as a manual command; not fine as a scheduled monthly task. Owed: an incremental materialisation (or a recorded, evidenced decision that full-refresh stays) when the publish becomes scheduled. Numbers measured live at M1-S4 (node disk 783 G free after; deployments ledger row 2026-08-16). | M1 needed the mart live, at full grain, and honest about its cost — incremental machinery for a command run by hand a few times per milestone is complexity nothing yet justifies. Named now, while the number (23 GB) is measured rather than remembered. | **M4** | BLUEPRINT §9/M1-S6: "From M4 the build+publish runs as the tail task of the monthly Flyte pipeline" — the story that makes the publish SCHEDULED is the story that must not pay 23 GB monthly without deciding to. | open — **intaken at the M4 kickoff 2026-08-18 (mandatory intake honored): absorbed into M4-S5**, which lands the marts build+publish as the pipeline's tail task. Closes on either an incremental materialisation measured against the 23 GB peak, or a RECORDED decision that full-refresh stays, with the number re-measured and the reasoning committed (dbt model diff or decision note in the story PR). **STILL OPEN after M4-S5's FIRST session (2026-08-18) — the story was cut between legs and this is leg 2.** What that session did land is the measurement and the one design fact the tail task turns on, so the next session starts from a decision rather than a discovery: `marts.trips_clean` is **13 GB right now** (read live off `pg_total_relation_size`, so the row's number is today's rather than M1-S4's memory), and a pod built from the actual task image **can** reach the one Postgres as the `marts` user (`scripts/marts_reach_probe.py` -> `PROBE-OK ('marts', 'marts')`). That last one was not safe to assume: `make marts` publishes over `kubectl exec` because the HOST has no TCP route to 5432 at all, and a task pod cannot use that transport — it has neither kubectl nor a kubeconfig, and giving a pipeline stage cluster credentials to shell into another pod would be a worse trade than any of the three alternatives `scripts/marts.sh` already rejected. Named for the next session so it is not rediscovered: the swap SQL exists once, in `marts.sh`, and a second copy in Python for the in-pod path would be a twin of the most consequential SQL in the repo. Full note: `docs/pipeline_m4.md` §14. **CLOSED 2026-08-18 (M4-S5 leg 2) — decided by measurement, and the decision is a SPLIT.** Both candidate publishes were measured on TODAY's data before either was argued, with `make marts-peak` sampling `pg_database_size('marts')` every 5 s (`scripts/marts_peak_probe.sh`, summaries in `automation/runs/m4-marts/`): a **full refresh** of all 8 months costs **228.2 s** and takes the database **15.33 -> 27.96 -> 13.48 GiB (peak/end 2.075x)** with PGDATA peaking at 204.62 GiB; a **month-scoped** publish of 2019-03 (7,753,921 rows) costs **82.7 s** and peaks at **15.33 GiB**. So the row's remembered "~23 GB" was OPTIMISTIC — it is **27.96 GiB** now, because `error_segments` joined the marts at M2-S4 and the fact table grew; the closure quotes the measurement, not the memory. **The decision:** the four aggregates (`zone_hourly_stats` 44,792 rows, `monthly_kpis` 8, `rejections_by_rule` 80, `error_segments` 1,151) stay FULL REFRESH forever — ~46,000 rows between them, under a second, and it buys the strongest property a publish can have, that the mart IS the source with no possibility of drift; `trips_clean` is MONTH-SCOPED, because it is the entire peak, its grain IS the month (an indexed column), and a monthly pipeline re-derives ONE month, so a full refresh republishes ~7.5M changed rows by rewriting 56M. **Peak down 45.2%, wall down 63.8%.** **The honest cost, recorded rather than netted out:** (a) the STEADY STATE rises — a scoped publish leaves ~7.75M dead tuples, so `end` is 15.33 GiB where a full refresh ends at 13.48, i.e. incremental trades a lower peak for a higher floor of about one month of dead space; and (b) the mart can now drift from its source in a way a full refresh made impossible, so `marts_publish.reconcile` asks Postgres and DuckDB for the same per-month counts after every scoped publish and REFUSES to return unless every month agrees — that check is the price of the decision and it is not optional (`tests/unit/test_marts_publish.py` watches it say no). **MECHANISM LANDED AND PROVEN ON-CLUSTER, not designed:** the publish is now `scripts/marts_publish.py` — one body of SQL, two thin transports (`kubectl exec` for the host, psycopg for a pod), with the mart list, the dbt `--vars` payload and `--no-partial-parse` all moved there so neither publisher keeps a copy (§14 named that twin before it was written; `marts.sh` no longer has a `MARTS=(...)` array and a test fails if one returns). It runs as the pipeline's SEVENTH stage: run `rw98pj84z4jh5ldqrxqp`, `publish_marts` SUCCEEDED in **90.6 s of an 886.6 s run (10.2%)**, of which the publish itself was **71.9 s** (against 82.7 s host-side — a pod's direct TCP beats `kubectl exec` by ~13%), in-pod `dbt build` **PASS=57 in 9.96 s**, **2019-01 published month-scoped and all 8 months reconciled `yes`**, `@champion` **2 -> 2**. Wiring it needed a fourth `marts`-role consumer (`flyte-task-marts` Secret, referenced from the PodTemplate; the pod publishes AS `marts`, never as the superuser), `data/predictions/` as a FOURTH staged tree (the `error_segments` mart sources the analyst layer's `predictions` view, which is conditional on the tree existing), and the F-026 image guard widened to `scripts/` and `analytics/` — both now reach a pod ONLY through the image. Full note: `docs/pipeline_m4.md` §16 |
| D-002 | M0-S3, 2026-08-16 | A path for creating a NEW database/role in the one Postgres AFTER first init. Today the `mlflow` database and role are created by `docker-entrypoint-initdb.d/10-mlflow.sh` (ConfigMap in `infra/manifests/postgres.yaml`), which Postgres runs **only on an empty data directory**. Editing that file on an existing volume is a silent no-op: the manifest will say the database exists, and it will not. | M0's platform needs exactly one database, and the honest alternative (a migration Job with its own idempotence and ordering story) is machinery M0 cannot yet justify — the second consumer does not exist. Named now rather than discovered later by a database that never appeared. `verify-m0` already checks the `mlflow` database and its owner, so the failure mode is caught, not silent. | **M1** | BLUEPRINT §9/M1: "a publish step lands the marts in the one Postgres" — the marts publish is the second database consumer, and the first that arrives after the volume already exists. (M1-S7's Metabase "app-db in Postgres" is the third; same landing.) | open — **intaken at the M1 kickoff 2026-08-16 (mandatory intake honored): absorbed into M1-S4**, which must prove the mechanism on the EXISTING volume; M1-S5's deliberate cluster rebuild then exercises the fresh-volume path. Closes on M1-S4's evidence | **closed 2026-08-16 (M1-S4)** — mechanism: `scripts/postgres_databases.sh`, invoked as step [5/7] of `scripts/deploy_platform.sh` (never by hand — MLOps charter). It declares the desired state of the server as a `DATABASES` list of `db:role:passwordkey` and converges each entry: role created only if absent then ALTER'd to the .env password every run; database created only if absent via the `\gexec` + `WHERE NOT EXISTS` form (CREATE DATABASE cannot sit in a DO block or a transaction). It never drops anything — destroying is `make destroy`'s job. Credentials reach psql on **stdin only** as `\set` variables, never in argv, so no password appears in `ps` inside the pod or in a kubectl audit log; `.env` gains the keys through a new ADDITIVE branch in `scripts/platform_secrets.sh` (safe precisely because a marts password is not yet baked into any volume — volume-baked keys stay in REQUIRED and are never regenerated). PROVEN ON THE EXISTING VOLUME, which is the case initdb cannot serve: PGDATA initialised 2026-08-16T15:47:03Z, `marts` dropped and re-converged at 17:44 — run 1 printed `[pg-db] marts: before = role absent, database absent` then `[pg-db] ok  marts owner=marts`, run 2 of the same command printed `before = role present, database present` and changed nothing. `mlflow` is in the same list and printed `role present, database present` on BOTH runs — untouched, and the free proof that the guards are real no-ops. `SELECT datname || ' owner=' || pg_get_userbyid(datdba)` -> `marts owner=marts`, `mlflow owner=mlflow`. Pinned by `tests/unit/test_marts.py::test_database_recipe_is_guarded_and_never_destructive` and `::test_mlflow_is_in_the_database_recipe_too`. The fresh-volume path is exercised by M1-S5's deliberate cluster rebuild; `10-mlflow.sh` stays as the empty-volume fast path, with the script's list as the single source of truth. |
| D-001 | M0-S2, 2026-08-16 | Decide how OUR images reach the kind nodes: local-registry pattern (`containerdConfigPatches` in `infra/kind/kind-config.yaml`) **or** `kind load docker-image` (gotcha #3). The kind config carried this as an undated `TODO(M0)`. | M0 builds and runs no image of ours — the platform at M0-S3 is upstream helm charts (MinIO/Postgres/MLflow) pulled from registries. Deciding now would be a guess ratified by nothing; gotcha #3 only bites once a locally-built image must run on-cluster. Re-tagged `TODO(M4)` in the config so the note cannot drift back to "M0" silently. | **M4** | BLUEPRINT §9/M4: "v1's M3 unchanged: Flyte 2 per docs, **containerized**, ingest→validate→features→train→evaluate→register parametrized by month" — containerized tasks are exactly the first images we build that must run inside the kind nodes. | open — **intaken at the M4 kickoff 2026-08-18 (mandatory intake honored): absorbed into M4-S3**, which decides local-registry vs `kind load` as a RECORDED decision (with the honest costs of each) before the first image must reach a node. One constraint the decision must respect, new since the TODO was written: the local-registry pattern's `containerdConfigPatches` lives in the kind CONFIG, and a config change means cluster-down/up — **which the M4 kickoff forbids** (the cluster is stateful; a rebuild destroys the registry). If the registry pattern is chosen, it lands at the next PO-sanctioned rebuild and `kind load` is the M4 bridge — say so in the decision . **CLOSED 2026-08-18 (M4-S3) — decision recorded, mechanism proven.** Decision: **`kind load docker-image`** at M4, argued with both options' honest costs in `docker/DECISION-D001-image-delivery.md` (a dated decision note, not an ADR — it changes no architecture). The fact that decides it is the one this row already anticipated: the kind config is read only at cluster-CREATE, so `containerdConfigPatches` means delete+create, and this cluster's PVCs hold the only copies of the MLflow registry (versions 1 and 2, `@champion` -> 2), MinIO's artifacts, the Metabase app-db, both Optuna studies and the `marts` warehouse — the state `verify-m2` (55) and `verify-m3` (46) read live. The registry pattern is named as the better END-STATE with a trigger (**image churn**) and a landing event (the next PO-sanctioned rebuild, the same one that owes Flyte's declared 8080 route), so it is deferred with a date, not dropped. MECHANISM PROVEN, not assumed: `make image-load` built `taxi-mlops-pipeline:<git-short-sha>` and `kind load` put it on all 3 nodes, read back with the nodes' OWN tool — `docker exec <node> crictl images` on each, printing the image id BEFORE and after so an idempotent re-load reads `(unchanged)` rather than being asserted. The forgetting hazard gotcha #3 names is closed by construction: the tag is the git short sha (`-dirty` when the tree is not), so a stale node is a MISSING image (loud `ImagePullBackOff`) instead of silently older bytes under the right name; `:latest` is refused by a test. Transcript: `docs/task_image_m4.md` §2. `infra/kind/kind-config.yaml`'s `TODO(M4)` is replaced by a pointer to the decision. |
