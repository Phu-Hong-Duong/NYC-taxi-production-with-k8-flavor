# Deployments ledger — every serve, canary shift, and rollback, with exact commands.
| Date | Event | Model version | Traffic | Commands used | Outcome / evidence |
|---|---|---|---|---|---|
| 2026-08-24 | **M9-S7 — the epilogue's only wire change: a dead online store stops billing the caller (F-062, PO answer (b)).** ONE mutation, and it is on the ERROR path only: `serving.feature_store.calendar_from_store` now asks a second question before it picks a status. An unanswered date and an emptied store return the same bytes (`null`), so the store is asked for a date the committed holiday table **provably covers** — a sentinel DERIVED from that table's own first year, which is the twin of the left edge of the `date_range` `scripts/feast_sources.build_calendar_day` generates the store's calendar view with. Sentinel answers → the requested date is genuinely uncovered → **422**, F-019 untouched. Sentinel null too → **503 `FeatureStoreUnavailable`**, ours, and it SPENDS SLO-A1's availability budget. The probe runs on the failure path only and is skipped entirely when any date in the batch answered, so the boundary's measured ~18 ms p50 is untouched. **A second defect was found by the re-measurement and fixed in the same deploy — F-069**: `do_POST`'s wrong-path 404 returned without reading the request body, and with `HTTP/1.1` keep-alive plus ingress-nginx's upstream pooling the NEXT caller's good request came back `400 Bad request syntax` with its own body echoed at it. Image rebuilt twice (`taxi-mlops-pipeline:8b6d5c0` then `:4e5dd66`), loaded onto all 3 nodes and read back with each node's own `crictl`; the transformer isvc redeployed. **STATE CLASS: code in an image — ledger row YES, backup obligation NO**; losing it costs one `make image-load && make deploy-transformer`. No new dependency, no new port, no new route, no PVC; `uv.lock` **byte-identical to `m7-closed`**. | **@champion 2 / `feature_set v2` — read as an assertion before and after each deploy, never written.** No alias moved, no registry version was created (`versions ['1','2']`), nothing was fitted. The champion's OWN wire is untouched by this change: it builds its matrix from committed tables and never reads the store, and it answered **39.0019 minutes** in every phase of the drill including the total outage | **Unchanged — 0% of rider traffic moved.** The change is on the transformer's raw boundary; the champion's InferenceService, Service, Ingress and predictor pod were not touched. The transformer's own dependency went down twice on purpose (emptied, then unreachable) and what it returns is the point of the story | `make transformer-probe` · `make image-load` · `make deploy-transformer` · `make transformer-parity` · `make feast-server-parity` · `make feast-online-parity` · `make store-watch-drill DRILL_ARGS=--dry-run` · `make store-watch-drill` · `make verify-m5` · `make verify-m6` · `make verify-m7` · `make verify-m8` · `make verify-m9` | **The drill PASSED — 36 checks across FOUR phases, 0 failures**, against a prediction committed at `b89eea4` **before** the first `FLUSHDB` (checkable from git). Empty store → **HTTP 503** (predicted 503; at 422 this same request spent no availability budget). **F-019's guarantee asserted in BOTH states** — a past-horizon quote is **422** naming the year while the store answers and **503** while it does not — because a change that decides blame by asking a second question could have stopped refusing altogether. A-12a/A-12b both **FIRED at T+162.5 s** and reached Alertmanager with the failing claims read per series (`['calendar_answers','zone_answers']`); all five must-not-fire negatives inactive; refill **57,688 keys in 11.6 s**; both cleared 30.0 s / 0.0 s and the board ends carrying the truth. The **fourth phase ran for the first time**: surface deleted → **A-13 FIRED at T+630.7 s** while **A-12 stayed inactive** — the load-bearing negative. Unreachable store → 503, answering again 15.1 s after the server returned. **All three parity records re-measured at their committed EXACT bars and came back 0.000e+00** — `transformer-parity` (16 hazards), `server-parity` (6 columns / 108 comparisons, `one missing` 0), `online_parity` (16 columns / 100 declared pairs, `UNEXPLAINED` 0) — which is the assertion that the fix touched the error path and nothing else. Gates re-run at exit: **verify-m5 · m6 · m7 · m8 · m9 all GREEN**; host suite **1220 passed**; ruff clean; all 5 settled DVC pins up to date |
| 2026-08-23 | **M9-S2 — three alert rules go live, and one drill deliberately destroys the online store's entire contents.** Two mutations, both recorded. (1) **`infra/monitoring/alerting_rules.yml` gains A-12a/A-12b/A-13** (`OnlineStoreCanaryFailing`, `OnlineStoreIncomplete`, `OnlineStoreWatchdogAbsent`), validated by `render_alert_rules.py` BEFORE helm (a rule with no `annotations.why` is refused, so a threshold whose argument is not beside it cannot ship) and provisioned into the `prometheus-server` ConfigMap — **13 -> 16 rules, `health=ok`, and no pod restart** (the configmap-reload sidecar, M6-S1's measurement re-confirmed for the fourth time). **STATE CLASS: the rules are cluster CONFIG rendered from git — ledger row YES, backup obligation NO** (M6-S2/M7-S3 precedent); losing them costs one `make deploy-monitoring`. **No new image, no new dependency, no CronJob, and NO Flyte trigger** — F-058's law, this milestone's §0: the reader's cadence is whoever runs it, which is why both A-12 rules carry A-4's `< 1800` freshness clause and go **INACTIVE rather than falsely green** on a stale reading. `uv.lock` byte-identical to `m7-closed`. (2) **`make store-watch-drill` FLUSHDB'd the Feast online store — 57,688 keys -> 0 — on purpose**, then refilled it. Restored and verified: **57,688 keys at `noeviction`**, re-read live 2026-08-24 after a host restart. | **@champion 2 / `feature_set v2` — read as an assertion, never written.** No rule, no reader and no drill phase touches the registry; the drill asserts the champion's own wire answers **39.0019 minutes throughout the outage**, which is the point: the store backs the transformer's raw boundary and not the champion's 24-column wire | **Unchanged — 0% of rider traffic moved.** No InferenceService, Service, Ingress or predictor pod was touched by any phase. The transformer's dependency DID go down for the duration of the empty phase by design, and what it returned is the finding: **HTTP 422, not a wrong number** | `make store-watch-headroom` · `make store-watch` · `make alert-rules` · `make deploy-monitoring` · `make store-watch-drill DRILL_ARGS=--dry-run` · `make store-watch-drill` · `make feast-materialize --no-record` · `make verify-m5` · `make verify-m6` · `make verify-m7` · `make verify-m8` | **The drill PASSED — 28 checks across three phases, 0 failures** (`health` 5/5, `empty` 19/19, `unreachable` 4/4), prediction committed BEFORE the first mutation and pinned by a test. **A-12a and A-12b both FIRED at T+162.2 s and both reached Alertmanager**; the failing claims were read **per series** (`['calendar_answers','zone_answers']`), not per rule name; **all five must-not-fire negatives held** (A-13, A-2, A-5, A-11, A-4); refill **57,688 keys in 9.9 s**; both rules cleared 30.0 s / 0.0 s after the reader saw a healthy store, and the board ends carrying the truth rather than a silence. **Two findings raised: F-063 CLOSED** (the undo rewrote M8-S4's tracked record — `--no-record`, restored from git and the phase re-run so every committed record was produced by the committed code) and **F-062 OPEN** (an emptied store bills a dead dependency to the caller as 4xx, so SLO-A1's error budget cannot see it — routed to the program close with three costed options, recommendation (b)). Re-verified at exit on the merged tree: **`make verify-m5` GREEN · `make verify-m6` GREEN · `make verify-m7` GREEN · `make verify-m8` GREEN 51/51 · host suite 1167 passed · ruff clean · `uv.lock` byte-identical to `m7-closed` · all settled DVC pins up to date** |
| 2026-08-23 | **M9-S1 — the stakeholder demo page, and a route chosen so that nothing else had to move.** Four NEW objects in `serving`, all named `taxi-demo-*` (names KServe never generates — F-039, and `deploy_demo.sh` refuses to write to any of them if it finds `ownerReferences`): a ConfigMap rendered FROM `demo/index.html` at apply time (the file in git is the only copy), a `busybox:1.38.0` **httpd** Deployment at the digest `infra/manifests/flyte-data-stager.yaml` already pins (a test asserts the two refs are one string — two copies of a pin is a twin, and reusing the program's existing image is why the demo adds none), a ClusterIP Service, and ONE Ingress. **STATE CLASS: STATELESS — no PVC, no database, no hostPort, so ledger row YES and backup obligation NO** (M8-S4 leg 2's precedent, recorded in both directions so the absence does not read as an omission); losing every one of them costs one `make deploy-demo`. **The route rule carries NO `host:`**, which puts both paths in nginx's DEFAULT server block: the page and the model share one origin and **CORS never happens**. The refused alternative was measured first — `location /healthz` lives only in the default block, so `host: localhost` would have turned `deploy_serving.sh`'s accept RED for a correct system. `/` is deliberately unclaimed, and so is the CHAMPION's own model name. | **@champion 2 / `feature_set v2` — never read by this story at all.** No registry client is imported by any file it added (asked of the AST); the version the page shows is the one **mlserver stamps on the answer** and the transformer forwards verbatim | **Unchanged — 0% of rider traffic moved, and the proof is a pod UID.** The champion's predictor pod is still `9b1f1b03-7dfe-458f-a8b0-cd045c61b18c`, the uid M8-S4 leg 3 recorded, at 4d age; neither InferenceService, neither KServe-owned Service and no Ingress KServe generated was touched. The demo's Ingress reloads nginx and touches no pod (M6-S4's measurement) | `make demo-page` · `make deploy-demo DRY_RUN=1` · `make deploy-demo` · `make demo-accept` · `make verify-m5` · `make verify-m8` | **`make demo-accept` PASSED 9/9.** The page a browser receives is **byte-identical to git by sha256** (fetched back through the route, 47,147 bytes); the page's own default trip answers **39.00193715359812 minutes against the recorded 39.00193715359812, |delta| = 0.000e+00** on a bar of EXACT argued in `demo/README.md` §4 and committed 2 commits before the record existed; `model_version` **'2'** off the answer; `X-Taxi-Lookups` equal to the recorded string, so the store was consulted through THIS path and F-059's two committed groups did not cross; a **2031** quote **422**-refused naming the date; the no-geometry path (264 -> 264) **quoted at 8.2445 min, not broken**; the champion's own name **404** on this origin (asserted only after a real quote succeeded — F-060); `/healthz` 200 and `/` 404 still standing. `DRY_RUN=1` verified to leave the namespace with no demo object. **`make verify-m5` GREEN · `make verify-m8` GREEN 51/51 · host suite 1146 passed · `uv.lock` byte-identical to `m7-closed` · all settled DVC pins up to date.** The §9/M9 box *"one non-technical person completes a query unassisted, observed"* is **OPEN by design** and raised at AWAITING_PO 2026-08-23-3 |
|---|---|---|---|---|---|
| 2026-08-23 | **M8-S5 — ONE monitoring config change, and it removes a target rather than adding one (F-061).** The `kserve-predictors` scrape job kept every pod carrying an InferenceService label and then forced mlserver's `:8082` on it — two assumptions that were the same assumption until M8-S4 leg 3 put a **transformer** pod in `serving`. That pod belongs to an isvc, is not an mlserver, and had become a permanently-DOWN scrape target on the one signal whose value (F-043) is that `up == 0` means a predictor stopped reporting. One `relabel_config` now also requires `component=predictor` — the discriminator **KServe already sets**, so the job's selector catches up to the job's own name. **STATE CLASS: no state gained or lost**; nothing was created, nothing was deleted, and the change removes a target. **No alert rule and no threshold was touched.** | **@champion 2 / `feature_set v2`, read and never written.** Versions `['1','2']` — no version 3. The gate asserts the strong form: not one registry version was created after the `m7-closed` tag | **Unchanged — 0% of rider traffic moved.** No InferenceService, Deployment, Service or Ingress was modified; the champion's and the transformer's pods were not restarted. Prometheus picked the config up through its configmap-reload sidecar with **no pod restart** (M6-S1's measurement, re-confirmed) and the target list converged within 5 s | `make deploy-monitoring` · `make monitoring-accept` · `make verify-m8` · `make verify-m8-redteam` · `make verify-m5` · `make verify-m6` · `make verify-m7` | **`monitoring-accept` GREEN 12/12** — 3 scrape jobs up, one real quote moved the inference counter 736 → 737, all 11 board panels returned live series. Targets after: exactly two, `nyc-taxi-eta-predictor…: 1` and `nyc-taxi-eta-transformer-predictor…: 1`, the false third gone. **`make verify-m8` GREEN 51/51 across 7 sections**, incl. its five live questions (champion 10.665224 min at `model_version='2'`; the transformer answering the same hazard from four RAW fields at `|Δ| = 0.000e+00`; the feature server two-sided on zone 132 / zone 264; the online store holding **57,688** keys at `maxmemory-policy noeviction`; one PromQL query). **`make verify-m8-redteam` PASSED** — one column's `both_missing` 13 → 0 (chosen from the record, not typed) → RED exit 1 with **3 FAILs from three artifacts**, 48 sub-check lines still passing, the headline-delta leg deliberately still GREEN, sha256-identical restore (`153c4399deab…`), GREEN 51/51, clean tree. `verify-m5` GREEN · `verify-m6` GREEN · `verify-m7` GREEN 62/62 · host suite **1127 passed** · ruff clean · `uv.lock` byte-identical to `m7-closed` · all settled DVC pins up to date. Records: `docs/verify_m8_transcripts.md`, `docs/feast_side_by_side.md` |
| 2026-08-23 | **M8-S4 leg 3 — the request boundary MOVES: a transformer beside the champion, and the champion's own wire untouched.** A SECOND InferenceService `nyc-taxi-eta-transformer` in `serving`, two Deployments (its own transformer pod on OUR image `taxi-mlops-pipeline:2cdcb36`, and its own predictor holding **the same champion bytes** resolved from `models:/nyc-taxi-eta@champion` by the same F-009 two hops). It accepts a RAW quote request — `at`, `pu`, `do`, `passenger_count` — and derives the champion's 24 features **inside the pod** through `taxi_mlops.features`, with the centroids and the two calendar flags read out of the online store via the quarantined feature server (shape (i), leg 2's). **STATE CLASS: STATELESS** — no volume, no ledger state, no backup obligation; losing either pod costs one restart. **No hostPort** (M8 law 1): it rides the existing 8081 route on its own KServe-generated host `nyc-taxi-eta-transformer-serving.local`. **F-059 is now structural**: `Lookups` has exactly two fields, the borough dictionary and the airport constant read the committed artifacts unconditionally, and an AST test refuses a regression. `uv.lock` byte-identical to `m7-closed` — the transformer is stdlib (`http.server`, `urllib`, `json`). | `@champion` **2** / `feature_set v2`, read BEFORE and AFTER every mutation the deploy makes; a change exits 2. Versions `['1','2']` — **no version 3**. Nothing was fitted, no run was minted, no alias was read by anything that could move it. | **Unchanged — 0% of rider traffic moved.** The champion's own InferenceService, its Deployment, its Service, its Ingress and its POD (uid `9b1f1b03-7dfe-458f-a8b0-cd045c61b18c`, age 4d6h) were not modified, and the pod uid was re-read AFTER the teardown drill to prove it. The transformer is a second host: rider traffic reaches it only if a client asks for it by name. | `make transformer-probe` (the ~1 min probe in front of the build) · `make image-load` · `make deploy-transformer` (`DRY_RUN=1` / `TEARDOWN=1`) · `make transformer-accept` · `make transformer-parity` · `make transformer-load` · `make verify-m5` · `make verify-m7` | **Accept GREEN 6/6**, and it is the artifact not a ready-list (gotcha #59): a RAW request answered **39.0019 minutes** — the value every record in this repo already carries for that row — stamped `model_version='2'` by mlserver and forwarded VERBATIM, with `X-Taxi-Lookups` proving the store was consulted and F-059's two groups were not. **THE PARITY: `max |champion − transformer| = 0.000e+00` minutes across all 16 declared hazards, bar EXACT** (argued in `docs/transformer_m8.md` §3 and committed at `79aedb4`, before any record existed) — table at `docs/transformer_parity_table.md`. **p95 at M5-S4's shape, both arms measured back to back in the same minutes: champion 113.1 ms -> transformer 136.1 ms (+23.0), p50 32.2 -> 49.0 (+16.8), 240/240 ok on each arm, zero errors, achieved 4.01 req/s.** **TEARDOWN PROVEN** (the M6-S3 shadow precedent): `TEARDOWN=1` removed exactly the transformer's isvc and its four generated objects while the champion's Deployment/Service/Ingress stayed at 4d6h and the champion re-quoted 39.0019 from the SAME pod uid; then re-deployed, because **M8-S5's gate inherits it live**. Exit: `verify-m5` GREEN, `verify-m7` GREEN 62/62, host suite **1091 passed**, ruff clean, four DVC pins `up to date`. Records: `automation/runs/m8-transformer/{transformer-deploy,transformer-parity,transformer-load}.json` |
|---|---|---|---|---|---|
| 2026-08-23 | **M8-S4 leg 2 — the Feast FEATURE SERVER becomes a tenant, and the `retrain-schedule-proof` trigger is DEACTIVATED.** The server is the pandas-2 half of the wall in its own pod (shape (i) of the kickoff's three, 1 of the 3-attempt wall spent, worked first time): image `taxi-mlops-feast-server:feast-0.66.0-a524771`, 203 MB, `--no-deps` from `infra/feast/requirements-feast.txt` — the SAME pin file the host quarantine uses. **STATE CLASS: STATELESS — no volume, no ledger state to lose, NO backup obligation.** Its registry is derived from the `definitions.py` in its own image at every start and every value it serves lives in Redis, so losing the pod costs one restart. **No hostPort** (M8 law 1); in-cluster readers use `feast-server.feast.svc.cluster.local:6566`, a human uses `kubectl port-forward`. Separately, and NOT part of the story: the `retrain-schedule-proof` FixedRate(20) trigger was deactivated after it back-filled ~100 missed firings at host boot (**F-058**) — a reconciliation with a one-command undo, the code unchanged. | `@champion` **2** / `feature_set v2`, read at entry and at exit. **No model is deployed by this row**: the feature server serves no predictions and cannot name the champion. | Unchanged — **0% of rider traffic touches anything here.** The champion's own InferenceService, its pod and its route were not modified; `make verify-m5` GREEN at story exit. The transformer that would put this server on a request path is NOT built (leg 3). | `make feast-serve-probe` (the ~30 s probe in front of the build) · `make feast-server-image` · `make deploy-feast-server` (`DRY_RUN=1` / `TEARDOWN=1`) · `make feast-server-parity` · undo for the trigger: `make retrain-schedule` | Accept is an ANSWER, not a ready-list (gotcha #59), asked **from the Redis pod** so Service DNS is under test: zone 132 -> `(40.646985, -73.78653) is_airport=True`, zone 264 -> `null` — **the null half asserted too**, because a check that only asserts presence passes against a server answering every question with the same row. Then the seam measured: **`max \|ours − server\| = 0.000e+00` across 6 columns and 108 comparisons, bar EXACT (argued in `docs/feast_server_m8.md` §3 and committed at `91ab8a6`, before any record existed), `one missing = 0`**, and the store declines EXACTLY the zones our path has no geometry for (`[264, 265]`). Exit state: `@champion` 2, versions `['1','2']` (no version 3), `verify-m5` GREEN, `verify-m7` GREEN, `uv.lock` byte-identical to `m7-closed`, all four settled DVC pins `up to date`, host suite **1057 passed**, ruff clean. Records: `automation/runs/m8-transformer/{feast-server-image,feast-server-deploy,server-parity}.json` |
|---|---|---|---|---|---|
| 2026-08-21 | **M8-S4 leg 1 — a NEW stateful tenant on the cluster: the Feast ONLINE store, and it holds no model.** (1) `make backup` FIRST, before the tenant existed (law 1, unbroken since M4-S2): `2026-08-21T07-46-33Z`, 6 databases, **1.7 GiB**, every dump verified by gzip CRC over every byte plus pg_dump's own completion marker. (2) A new `feast` namespace holding ONE Redis (`redis:8.2-alpine@sha256:30abb90e62f1…`, TAG AND DIGEST — the Metabase precedent), a 1Gi PVC, a ClusterIP, `strategy: Recreate` (a node-local RWO volume plus RollingUpdate is F-033's deadlock, avoided by construction) and **`maxmemory-policy noeviction`, which is a correctness setting**: an evicting feature store drops the key the next request asks for and answers null, and a null reads as a feature that has no value. **NO hostPort** — the pod reaches it by Service DNS, the host through an ephemeral `kubectl port-forward` on **6380** (deliberately off 6379 so a materialization can never write into a developer's own local Redis). ADR-012 records the decision AND the state class: **materialized features are REGENERABLE, so ledger row YES and backup obligation NO** — `make backup` enumerates databases and buckets from the server and will not see Redis, which is correct and is written down so nobody later reads the absence as an omission. (3) One materialization, 7 s. **Nothing on the serving path was touched.** | **2** (`@champion`, `auto-lgbm-v2`) — **READ, never written.** Versions `['1','2']`; no version 3 exists. The store deploy cannot name the alias in code (unit-tested), and this whole story fits nothing | unchanged — 100% to the single predictor replica; no canary, no rollback, no InferenceService touched. Both serving gates were re-run afterwards and each asked the endpoint for a real prediction | `make backup` · `make deploy-feast-store` · `make feast-materialize` · `make feast-online-parity` · `make feast-online-parity-redteam` · `make verify-m5` · `make verify-m7` | **GREEN.** Store accept is an ANSWER FROM THE SERVER, not a list of ready objects (gotcha #59): `PING -> PONG` plus a real SET/GET/DEL round trip (**a write, which is what a materialization needs and a readiness probe does not prove**), with `maxmemory-policy=noeviction` read back off the running server rather than off the values submitted. Materialize: **57,688 keys / 14.32 MiB** against a 512mb cap, window **DERIVED** from the published parquet and never typed. **THE 100-pair parity: `max |online − offline| = 0.000e+00` over 16 columns, `one missing` ZERO on every one, `UNEXPLAINED 0`, no-geometry disagreements 0** — `docs/feast_online_parity_table.md` (the blueprint's named accept artifact, committed). Red team **PASSED**: one OD pair's REAL serialized bytes copied onto another's key → **RED exit 1 at 8.727e+01** naming `od_window.od_median_duration_min`, **26 sub-check lines still passing**, sha256-identical restore (`bd91004815981b5c…`), GREEN again, clean tree. `make verify-m5` GREEN and `make verify-m7` **GREEN 62/62** at exit. **The transformer is NOT built — the kickoff's own declared safe stopping point**; leg 2 inherits an unspent 3-attempt wall |
| 2026-08-21 | **M8-S1 leg 2 — the task image rebuilt once and the retrain schedule redeployed once, plus the only registry write in this program that is not a promotion.** Image `taxi-mlops-pipeline:5edf9fd` (804 MiB content, `tree dirty: no`) built and `kind load`ed onto all 3 nodes, read back with each node's own `crictl` at `sha256:8e20e0b191cd…`. Task `taxi-pipeline-train.retrain` redeployed at version `cfe8dc01a11527facc8cbc329e1df85a` with both triggers read back OFF THE SERVER (`retrain-schedule-proof` every 20 minutes, active **True**; `retrain-monthly` cron `0 3 1 * *`, active **False** — unchanged, and still a PO's call about compute). The registry write: `make backfill-provenance` set three provenance TAGS on versions 1 and 2 (F-048) | **2, unmoved** — read before and after; the backfill never reads an alias and cannot move one (AST-pinned) | none moved; no InferenceService touched, no canary, no rollback | `make image-load` · `make image-smoke` · `make retrain-schedule` · `make backfill-provenance` (dry run, then real, then again) · `uv run python scripts/retrain_proof_record.py` | GREEN. `image-smoke` **10/10** with the in-image suite at 928 passed / 19 skipped / 22 deselected (F-047's closing condition). Backfill: `2 version(s) changed; no alias was read or moved, no version was created, nothing was deleted`, and a second run `0 version(s) changed`. The host then resolved F-020's transfer FROM THE REGISTRY — factor **6.6667**, `min_data_in_leaf` 1293 -> **8620**, round cap **2400** — the same numbers M7-S4 measured from the host records, from a different authority. Honest cost, priced in the kickoff and paid here: **gotcha #66** — the image tag is the git short sha, so this commit makes the next on-cluster pipeline run start cold. One rebuild was ALSO spent on a `-dirty` tag, because `make retrain --plan-only` writes a tracked record and it had been run first. |
| 2026-08-21 | **M8-S1 leg 1 — the drift board becomes a STATEFUL tenant, and gains a rule for its own absence.** Three mutations, all in `monitoring`, none on the serving path. (1) `make backup` FIRST, before the tenant gained state (law 1, the M4-S2/M5-S1/M6-S1/M7-S3 precedent unbroken): `2026-08-21T05-00-56Z`, 6 databases + 418 objects, **1.7 GiB**, every dump verified by gzip CRC over every byte plus pg_dump's own completion marker. (2) `prometheus-pushgateway` gains a **PersistentVolume** (`persistentVolume.enabled: true`, 128Mi, `--persistence.file=/data/pushgateway.data --persistence.interval=10s`) — F-050 (a). The subchart's `strategy.type` is already `Recreate`, which a node-local RWO volume needs; read live, not assumed. **Honest cost, paid once**: the values change ROLLED the gateway and lost whatever it held (it held nothing — F-050's own empty state), which the drill then re-pushed. (3) A **new alert, A-11 `DriftMetricsAbsent`** (`absent(...)`, `for: 10m`, SLO-D4) — F-050 (b) — plus a `blind_spot` annotation on A-10 saying why it cannot do this job. 13 rules validate; A-11 read back off `/api/v1/rules` `health=ok`. | unchanged — **@champion version 2 throughout, read and never written** | unchanged — **no traffic moved, no InferenceService touched, no canary, no rollback** | `make backup` · `make deploy-monitoring` (which re-runs `deploy_serving.sh` as its own step 1 — ~1 min of no-op helm upgrades so ONE file owns the ingress values) · `make drift-persistence-drill` · `make drift DRIFT_ARGS="--months 2020-01 2020-02 2020-03"` · `make verify-m7` | **`monitoring-accept` GREEN 10/10** — 3 scrape jobs up, one real quote moved the inference counter 9 -> 10, and all 11 board panels returned live series. **`make drift-persistence-drill` PASSED 16/16** with its prediction committed BEFORE the run: 48 samples survived a `kubectl delete pod` onto a **different pod object** (uid `bf053286…` -> `2a1591bc…`, ready in **13.12 s**), the deliberate wipe made A-11 **fire at 625.1 s** against its 600 s sustain and reach **Alertmanager** while **A-10 and A-9 both stayed inactive** through a total loss of the surface, and the re-push cleared A-11 in **37.8 s** with A-9 returning for 2020-03 — the board ends carrying the truth. **`make verify-m7` GREEN 62/62** at leg exit, its §5 passing through the *present* branch for the first time (`the live gateway carries 3 volume series, one per scoring month`). Record `automation/runs/m8-drift/persistence.json`; narrative `docs/drift_surface_m8.md` |
| 2026-08-20 | **M7-S5 leg 2 — the M7 gate, and it mutates NOTHING.** `make verify-m7` ingests no month, scores no month, computes no drift, **pushes no metric**, fits nothing, deploys nothing and moves no alias — the properties are pinned by `tests/unit/test_verify_m7.py`, which forbids the producers by invocation and allows only read-only `kubectl get`/`describe` plus one `kubectl exec … psql -c SELECT` (the transport every mart read in this repo uses, because nothing of ours publishes 5432). Its entire footprint on the running system is **three questions**: one inference request, one PromQL query, one `/api/v1/rules` read. The red team's footprint is one tracked JSON file restored byte-identically. **One inherited guard was repaired**: `verify-m6` §2 had gone RED because M7-S3 CLOSED F-035 — the leg required the documented-absence list to be non-empty | **2** (`@champion`, `auto-lgbm-v2`, run `92b73bd4f77d…`) — **READ, never written.** The gate asserts the alias in its strong form: not one of the 8 runs the retrain fitted is a registry version, and `@champion`'s run is still the one the M3 bake-off recorded as its winner | unchanged — 100% to the single predictor replica. **The gate sent the endpoint exactly one request** (§7's live prediction) and the red team sent two more across its two gate runs | `make verify-m7` · `make verify-m7-redteam` · `make verify-m6` (re-run, repaired) · `uv run pytest tests/unit` | **GREEN 62/62 sub-checks in 7 sections, 5.328 s, exit 0.** Red team **PASSED**: one rewritten `volume_ratio` (0.3913 → 0.4021, a ratio of TOTALS where a ratio of RATES belongs — F-045 itself, derived from the record's own fields and still under the 0.50 bar) → **RED exit 1 with 3 FAILs from THREE artifacts** (the anchor arithmetic, the drill record that watched the live gateway, and the memo a human reads), **59 sub-check lines still passing**, the bar-daylight leg deliberately **still GREEN**, sha256-verified byte-identical restore, GREEN 62/62, clean tree. `make verify-m6` **GREEN 63/63** after the repair; unit suite **939 passed**. **One finding raised by the gate on its own first run — F-050**: the pushgateway had lost every drift series to a host-reboot restart, and A-10 cannot fire on an absent series. Routed to the boundary; the gate names it rather than hiding it. Transcripts: `docs/verify_m7_transcripts.md` |
| 2026-08-20 | **M7-S5 leg 1 — one mutation, and it is a dashboard.** `make boards` converged a **fourth** Metabase dashboard, `Predictions & drift (M7)` (id 5, 8 cards), from checked-in JSON over `marts.scoring_daily`. The three existing boards reported `card updated` and kept their ids — the converger matches by NAME and never archives or deletes (M1-S5's stated asymmetry). **No serving object, no registry object, no Prometheus rule and no cluster workload was touched**: leg 1 reads the analyst layer, the mart and the tracked drift records, and writes one document, one board and two readers | **2** (`@champion`, `auto-lgbm-v2`) — **READ once, to name the model the board describes; never written.** No run was minted, no version created, no alias moved | unchanged — 100% to the single predictor replica; the endpoint was not called at all this session | `make boards` · `uv run python scripts/metabase_boards.py --verify` · `make board-cards` · `uv run python scripts/drift_memo_numbers.py` | Board renders live: **91 rows** from `KPI-17 · trips scored per day` through Metabase's own API. **`make board-cards` executed all 36 cards across all 4 boards, 0 failures** — the new check, and it exists because `--verify` runs ONE card per dashboard while an empty panel is indistinguishable from a quiet system (gotcha #78). Transcripts: `docs/drift_memo_m7_transcripts.md`. **`make verify-m7` is NOT built — that is the declared leg boundary** (the kickoff's own cut, the M4-S5/M6-S5 precedent) |
| 2026-08-20 | **M7-S4 — the retrain becomes a SCHEDULED entity on the control plane, and the task image is re-pinned.** Two mutations, both deliberate. (1) A new deployed Flyte task `taxi-pipeline-train.retrain` (version `6d5b536b975b7814c829cb3ef2bcbaf8`, image `taxi-mlops-pipeline:72a4013`) with **two registered triggers**: `retrain-monthly` (`Cron("0 3 1 * *")` UTC, full-data, judged) **with `auto_activate: False`** and `retrain-schedule-proof` (`FixedRate(20)`, `plan_only=True`, active). Declared in CODE with their inputs, because `flyte create trigger` cannot pass task inputs and a CLI-created trigger would fire the retrain with its DEFAULTS with the cadence living in somebody's shell history. **The monthly trigger is registered and inactive ON PURPOSE**: this cluster is a laptop, the full-data fit is hours of CPU under a 6-core task limit, and a retrain firing unattended every month on a machine nobody watches would spend that budget to produce a verdict nobody reads — turning it on is one field and a PO's call about compute. (2) The task image rebuilt twice (`820adbd` then `72a4013`) and loaded to all 3 nodes; the second rebuild was forced by `retrain_schedule.sh`'s own F-026 guard firing on this story's commit, and **the image was rebuilt rather than the guard narrowed** | **2** (`@champion`, `auto-lgbm-v2`, run `92b73bd4f77d…`) — **READ, never written.** The retrain path passes `promote=False` unconditionally and `retrain()` has **no `promote` parameter** — a law with a keyword argument is a default (`tasks.train`'s rule, inherited), and this is the one job in the program designed to run unattended | unchanged — 100% to the single predictor replica; **no serving object was touched by this story at all** | `make bakeoff BAKEOFF_ARGS="--smoke-rows 20000"` · `make retrain RETRAIN_ARGS="--plan-only"` · `make image-load` (×2) · `make retrain-schedule` (×2, the first refused) | **GREEN.** F-022's execution half: exit 0 past contender resolution, `[resolve] champion (alias) auto-lgbm-v2 … features=v2 (24) (DERIVED from the artifact — F-022)`, five verdicts printed, no JSON written, nothing promoted. F-020's transfer resolved live from three tracked artifacts: `min_data_in_leaf` **1293 -> 8620** (1 row in 5,103 where it was chosen · 1 in **34,020** unchanged at the refit's scale · 1 in 5,103 after) and the round budget re-derived **800 -> 2400** with `ended_by` reported as a first-class field. Schedule: **Flyte 2.6.1 / chart v2.0.42 carries triggers natively** — the question was answered by asking the tooling (gotcha #70's family), so **the kickoff's recorded cron fallback was NOT executed and stays armed and unspent**, one attempt of a three-attempt wall. Triggers **read back off the control plane**, not off the file submitted (`deploy_serving.sh`'s ConfigMap precedent; gotcha #81 one layer up — a registered trigger and a firing trigger look identical in a configuration table): `retrain-schedule-proof … every 20 minutes starting at now … True` and `retrain-monthly … cron: 0 3 1 * * (UTC) … False`. **The cluster never went down**; no kind config edit, no new hostPort, no serving mutation. `docs/retrain_m7.md` · `docs/retrain_m7_transcripts.md` |
| 2026-08-20 | **M7-S3 — the pushgateway lands as a sixth tenant, and three new alert rules go live.** `make backup` FIRST (the standing precedent: a new tenant lands beside state with no other copy) — manifest `2026-08-20T04-07-22Z`, **6 databases, 1.6 GiB**, every dump gzip-CRC clean with pg_dump's own completion marker. Then: `prometheus-pushgateway` flipped on in the existing prometheus release (**no hostPort** — M7 law 1; 9091 stays a RESERVED NAME in the port family, reached by port-forward, the M4-S2 flyte-console precedent), its own scrape job with **`honor_labels: true`**, and the rules file grown from 7 rules / 6 signals to **12 rules / 10 signals** (A-8 input drift, A-9 volume, A-10 staleness, plus F-035's two landings). **No workload was restarted to change a rule or a scrape config** — the configmap-reload sidecar makes both free, as M6-S1 measured. The predictor was not touched at all: no deploy, no annotation, no secret, no pod deleted | `nyc-taxi-eta` **version 2** (`@champion`), read before and after every phase of the drill and asserted equal. **No version minted, no alias written**, and an AST test forbids a registry-mutating verb anywhere in this story's code (M7 law 3) | 100% champion throughout. **This story sent the endpoint exactly two real requests** — one from the deploy's own accept check and one from A-4's version pusher, which reads the served version off a real prediction because no mlserver metric carries it (F-034). No load, no canary, no shadow | `make backup` · `make deploy-monitoring` (×3) · `make alert-rules` · `make drift-headroom` · `make drift-drill` (×2) · `make drift-witness` · `make push-serving-version` | **GREEN.** `make deploy-monitoring` accept check **10/10** each time, all 11 serving-board panel queries still returning live series. The gateway's first scrape target read **`down`** — the subchart's fullname prefixes the RELEASE name, so the address is `prometheus-prometheus-pushgateway`, read off the live Service rather than guessed; a DOWN target is the *good* outcome of that mistake, and had the gateway also been annotated for the generic endpoints job it would have been scraped with its labels mangled and every drift rule would have sat quietly inactive instead. `make drift-drill` **PASSED**: A-9 `ScoringVolumeCollapse` pending T+31.5s → **FIRING T+331.5s for `month='2020-03'` only**, reaching **Alertmanager**, with A-9 for 2020-01 and 2020-02 and all seven other watched rules **inactive as predicted**; the pre-registered open question (A-8 at monthly grain) answered **correctly**. A-9 **cleared 40.0 s** after its metric was removed and the real numbers were pushed straight back — the board deliberately ends with A-9 FIRING, because March 2020 really did lose 61% of its trips and the decision it asks for is M7-S4's. F-035's two landings proved live: `taxi_serving_model_version` and `taxi_registry_champion_version` both **2** (agreeing, so A-4 correctly inactive), and **A-3's client half FIRED end to end** — the alert M6-S2 measured as impossible on this stack — from `taxi_quote_refusals_total` after two past-horizon refusals; that drill counter was deleted afterwards, since two refusals of my own making are not real traffic. **End state = M5's/M6's**: `@champion` 2, `features.version` v2, champion serving, `make verify-m5` unaffected, 18 `taxi_drift_psi` series on the board |
| 2026-08-20 | **M7-S1 — the v1 shadow torn down.** The M6 boundary's decision (M7 kickoff §0), executed as this story's first move: the shadow's evidentiary work is done — the F-043 idle-exporter comparison is committed in `automation/runs/m6-gameday/saturation.json` and `docs/gameday_m6.md` §4.2 — and keeping a second predictor as a permanent fixture would turn an M6-S3 leftover into unplanned wire state. **This is the ONLY wire mutation of M7-S1**; the rest of the story is data on the host. Nothing else in the cluster was touched: no deploy, no restart, no secret, no rule, no scrape config | `nyc-taxi-eta` **version 2** (`@champion`) — read, never written. The shadow was never aliased; deleting it removes no registry state (version 1 remains the rollback target, `feature_set=v1`) | 100% champion before and after. The shadow had taken **zero rider traffic** since M6-S3 by construction (its own KServe-generated host) | `make shadow TEARDOWN=1` · `kubectl -n serving get isvc,deploy,svc,ingress` · `make verify-m5` | `inferenceservice.serving.kserve.io "nyc-taxi-eta-shadow" deleted from serving namespace` / `ok  nyc-taxi-eta-shadow removed (the champion was not touched)`. Read back immediately: **one** isvc, Deployment, Service and Ingress remain, **ages 21 h and 23 h** — i.e. the objects the teardown did not touch are the objects it did not restart, which is the evidence that matters (M6-S3 proved the teardown removes exactly its own three objects; this run proves it again from the other side). `make verify-m5` **GREEN 49/49** afterwards, including its §2 prediction: the champion still answers with `model_version` equal to what the alias resolves to |
| 2026-08-19 | **M6-S5 — Gameday 1 and the restore rehearsal.** Four staged failures against the live serving stack, every prediction written to disk before the first injection, plus the first restore this program has ever performed. Wire mutations, all deliberate and all recorded: one predictor pod DELETED under load (scenario 1); `secret/minio-serving`'s `AWS_SECRET_ACCESS_KEY` overwritten with a wrong value and the pod deleted, a **~5 minute total outage of the only predictor** held deliberately so a `for: 3m` rule could fire (scenario 2, undo staged BEFORE the injection); ~25 minutes of a saturated container at 8 req/s (scenario 3). Postgres/MinIO: three SCRATCH databases created and dropped and one scratch bucket created and deleted — **no live database or bucket was written** | `nyc-taxi-eta` **version 2** (`@champion`), read at the start and end of every scenario and asserted equal; **no version minted, no alias written** | 100% champion throughout; no canary, no shadow traffic (the v1 shadow stayed up with zero rider traffic and served as an accidental control for F-043) | `make gameday GAMEDAY_ARGS="--scenario predict"` -> `--scenario control` -> `kill` -> `storage` -> `saturation` -> `report` · `make restore-drill` (x2) · `make serve` (the staged undo) | **Accept bar MET with two wrong predictions, neither engineered.** Control GREEN 11/11: A-3 fired T+170.5s, A-2 T+335.6s, both at Alertmanager, both cleared 330.1s after the stop. Kill: outage **13.75s** (55 of 1,200 requests, 52x503 + 3x502), a different pod uid, **nothing fired** — but the edge 5xx share **peaked at 0.5000** and A-2/A-5 both flickered `pending`, which is **F-041**: the thresholds were argued from a steady-state ratio and it is the `for:` sustain that prevents the page. Storage: **8/8 as predicted** — `403 Forbidden` on HeadBucket (M5-S2's class), A-5 firing T+150.2s and A-7 T+210.2s (**A-5 first, the OPPOSITE of A-7's own annotation — F-042**), A-2 silent through a total outage (its documented blind spot, demonstrated), the flapping rule silent because `kserve-container` never started, route 503, `make serve` exit 0 and both alerts clear within 30s. Saturation: A-6 fired at **T+844.3s = 244.1 + 600.2** (the sustain clock starts when the RATIO crosses, not when the load does), throttled fraction 0.00 -> **0.9996**, client p50 latency **94,553ms** against service p50 1,084ms at an achieved 6.775 of 8 req/s — and **125 x HTTP 502**, so the zero-errors prediction was wrong and gotcha #74 is refined by duration. **F-043 (open)**: A-1 fired at T+349.3s and cleared itself at T+514.3s mid-event because the predictor's own exporter starved (`scrape_duration` 4.613s, `up` 0, against the idle shadow's 0.004s/1 on the same job). Restore: **GREEN 17/17** — mlflow 2.34s, optuna 0.78s, metabase 7.29s into scratch databases, every counted table equal to live, the same `champion\|2` pointer, the M3-S4 trial counts, `flyte-data` restored whole (184 objects / 783,327 bytes) and one MLflow artifact byte-identical by sha256; live database and bucket lists unchanged, no scratch survived. **The label therefore moves exactly one notch: restore is SCRATCH-REHEARSED (2026-08-19) and a full restore over a DEAD platform is still NOT rehearsed** — it needs a PO-sanctioned rebuild, and until then this is a lifeboat with one oar tested. **End state = M5's**: `@champion` 2, `features.version` v2, champion serving, endpoint answering |
| 2026-08-19 | **M6-S4 — the release rehearsal: rider traffic actually split, and the rollback finally run.** Four mutations, all deliberate, all ended where they started. (1) A **canary InferenceService** `nyc-taxi-eta-canary` carrying the champion's OWN bytes (resolved from `@champion`, so a benign delta by construction — M6-S3's DA memo returned NO-GO for v1) with `MLSERVER_MODEL_NAME: nyc-taxi-eta`, **ADR-011 condition 2's named-but-unproven remedy, now PROVED**; plus its dedicated backend Service (condition 1). (2) A hand-authored **canary Ingress `nyc-taxi-eta-canary-route`** applied at weight **10**, then **100**, then **deleted** — under one continuous 6-minute load run. (3) The **alias moved twice**, v2→v1→v2, M6 law 3's sanctioned pair, with `configs/train.yaml: features.version` moving with it both times and `make serve` re-deploying twice. (4) The canary torn down at the end. **No isvc was ever annotated** (F-038). | **2 → 1 → 2.** The alias moved exactly twice and both moves are this row. `@champion` is version **2** and `features.version` is **v2** at the end, and `configs/train.yaml` is **byte-identical by git hash-object** (`4eaa1edfa5fd…` before and after). Nothing promoted; both moves are raw `set_registered_model_alias`, never `registry.promote` (the runbook's §4.3 refusal, intact) | **100% champion → 90/10 → 0/100 → 100% champion**, observed from COUNTERS and never from the annotation. Ingress `canary` label: 0 of 177 · **41 of 420 = 9.76%** · **301 of 301 = 100%** · 0 of 300. The two predictors' own `rest_server_requests_total`, a second witness from a different process: 204/0 · **379/39 = 9.33%** · **0/240 = 100%** · 300/0. End state 100% champion, canary isvc removed | `make canary-deploy` · `make canary` · `make rollback` · `make canary-deploy TEARDOWN=1` · `make verify-m5` (×3: after the canary drill, at the HALF-WAY v1 state, and at the end) | **canary drill PASS 11/11 · rollback rehearsal PASS 10/10 · verify-m5 GREEN at the end.** **Zero requests failed across the entire canary run** (1440 sent, two weight changes and a revert): an Ingress edit touches no pod, and the champion's predictor kept the same UID throughout. **The traffic revert took 0.37 s** for the controller to drop the backend, against §9/M6's 120 s budget — one `kubectl delete ingress`, no requests lost. **The alias rollback is the expensive one and now has a number: 35.35 s of moves and 27.93 s of failing requests (v2→v1) against 34.38 s and 0.501 s (v1→v2)** — F-040, gotcha #86: the config move changes the request schema while the old pod serves, and removing features refuses requests where adding them does not. `verify-m5` at the half-way state went **RED with 3 FAILs while §2's coherence check stayed GREEN at `v1`** — which is what proves that check is coherence and not the literal v2. **F-039 cost the first canary run**: the route was named `nyc-taxi-eta-canary`, KServe owns that name, and the annotations were reconciled away — 0 of 420 moved with no error anywhere, kept unedited at `automation/runs/m6-canary/attempt1-ingress-name-collision/`. Records (tracked): `automation/runs/m6-canary/release_drill.json` · `automation/runs/m6-rollback/alias_rollback.json` |
| 2026-08-19 | M6-S3 **a SECOND model on the wire, and ADR-004's canary spike** — three distinct mutations, all recorded, all ended where they started. (1) The **v1 shadow** deployed as a second InferenceService `nyc-taxi-eta-shadow` (registry version **1**, resolved BY VERSION through the F-009 two hops, feature set `v1` derived from the version's own `feature_set` tag). It answers on its OWN KServe-generated host and takes **zero rider traffic**; the champion's host is untouched. Torn down and redeployed from scratch once, to prove both the teardown and F-037's route-wait fix. (2) The **canary spike** (`make canary-spike`): a hand-authored canary Ingress at weight 50 applied and removed twice, plus a dedicated canary Service, plus `mirror-target` added to and removed from the Ingress KServe owns. (3) The **shadow run** (`make shadow-run`), a reader: 1,016 dual-sent requests across both endpoints. **No alias moved, no version was minted, no gate ran, `configs/train.yaml` untouched.** | 2 (`@champion`, read before AND after every mutation; the shadow is version 1 and is deliberately NOT aliased) | 100% champion at start and at end. **Deliberately split to 50/50 for two 200-request batches during the spike, then restored** — the only traffic this program has ever intentionally moved off the champion | `make shadow DRY_RUN=1` · `make shadow` · `make shadow TEARDOWN=1` · `make shadow` · `make canary-spike` · `make shadow-run` · `make parity` · `make verify-m5` | **Spike PASS 7/7** (`automation/runs/m6-spike/canary_spike.json`). The measured split: **0 of 200 moved** with the canary pointed at a Service KServe's own Ingress also claims (`{weight: 0, weightTotal: 0}` — silent, no error anywhere), and **100 of 200 moved** with a dedicated Service (`noServer: true`, `{weight: 50, weightTotal: 100}`). Canary-routed requests returned **404, not 500** — the V2 model name is in the URL path — and `rewrite-target` on the canary changed the share by **0 points**. `mirror-target` on the KServe-owned Ingress survived a reconcile AND produced a real nginx `mirror` directive. Decisions in **ADR-011**; conditions 1 and 2 bind M6-S4. **One unintended outage, recorded rather than hidden**: the spike's FIRST run annotated the InferenceService to force a reconcile and thereby rolled the champion's only predictor twice — **174 of 200 requests returned 502** in that run's end-state batch (F-038, gotcha #82). The service self-healed, `@champion` never moved, and the run's record is kept unedited at `automation/runs/m6-spike/attempt1-no-dedicated-service/`. End state verified three ways: **200 of 200 requests returned 200 with `model_version: 2`**, `make parity` still **0.000e+00 over 16 hazard rows**, `make verify-m5` **GREEN 49/49**. The shadow was LEFT RUNNING for M6-S4/S5 to read; `make shadow TEARDOWN=1` removes it and takes its Deployment, Service and Ingress with it. |
| 2026-08-19 | M6-S2 **the CPU request re-sized on the wire, and the alert rules provisioned** — two deliberate mutations, both recorded. (1) `spec.predictor.model.resources.requests.cpu` **200m -> 1500m** (limit unchanged at 2, memory request unchanged at 1Gi so the before/after comparison has ONE cause). Argued from M5-S4's measured 1.31 cores at the SLO's own load shape, plus ~15% for the per-request variance, and deliberately BELOW the limit so the pod stays Burstable rather than reserving the saturation ceiling. This is the one open recommendation M5's PRR routed to M6. (2) Seven alert rules (A-1, A-2, A-3, A-5x2, A-6, A-7) provisioned into Prometheus from `infra/monitoring/alerting_rules.yml`, plus M6-S2's fix to the InferenceService readiness wait (F-036). | 2 (`@champion`, read before AND after, never written) | 100% champion (no canary — Standard mode) | `make load LOAD_ARGS="--rate 4 --seconds 60 --concurrency 8"` (before) · `make serve` · `make load …` (after) · `make deploy-monitoring` · `make alert-fire-drill` · `make verify-m5` | **The roll cost 0.5 s of route unavailability — one 502 of 400 samples** (`automation/runs/m6-slo/cpu-request-roll.json`, anchored first-failure -> first-success per gotcha #75), NOT the ~15 s the SLO document's first draft predicted: a rolling update at one replica has `maxUnavailable: 25%` floor to **zero**, so a surge pod must be ready before the old one goes (docs/slo_serving.md §4.1 — the corrected prediction). p50 29.4 -> 29.5 ms, p95 84.4 -> 112.7 ms (inside the run-to-run spread of this shape; M5-S4 measured 104.2 ms), and on the SLO's own instrument at most 2 of 240 requests beyond 250 ms before vs **0 of 240** after. `make alert-fire-drill` **GREEN 11/11** — A-3 fired at T+150.5 s (predicted 150), A-2 at T+330.6 s (predicted 330), in the predicted order, all five must-not-fire alerts inactive, both received by Alertmanager, an ordinary quote succeeding throughout, both cleared 315.1 s after the injection stopped. `make verify-m5` **GREEN 49/49** after every mutation; the served version's `feature_set` still equals `configs/train.yaml`'s. Records: `automation/runs/m6-slo/`. |
| 2026-08-19 | **M6-S1 — the monitoring stack, and the first deliberate mutation of the route.** `make backup` ran FIRST (the M4-S2/M5-S1 precedent): destination `2026-08-19T05-59-36Z`, **6 databases + 331 MinIO objects, 1.6 GiB**, every dump verified by `gzip -t` over every byte plus pg_dump's own completion marker; restore still NOT rehearsed. Then Prometheus 29.27.0 (v3.14.0) + Alertmanager + kube-state-metrics + Grafana 10.5.15 (12.3.1) into `monitoring`, reached through the **existing** 8081 ingress by host (`prometheus.local`, `grafana.local`) — **no new hostPort, no kind-config edit** (M6 law 1). **The wire change:** `controller.metrics.enabled: true` on ingress-nginx, which rolls the only route into the cluster. It also FORCED `updateStrategy: Recreate` — with `hostPort` + 1 replica + a single-node nodeSelector the default RollingUpdate deadlocks (`1 node(s) didn't have free ports for the requested pod ports`; the surge pod sat Pending for 10 minutes while the old one served, and the helm upgrade would have timed out at 20m) | **2** (`@champion`) — **never read by this story.** `deploy_monitoring.sh` and the values files cannot name `champion`/`models:/` at all, asserted by test over code. The accept check asks the ENDPOINT for one quote (a read) | unchanged — 100% to the single predictor replica; no canary (Standard mode, ADR-004). **The route's own outage is measured: 15.0 s** (`automation/runs/m6-monitoring/ingress-metrics-roll.json`, 30 failed of 600 samples at 2/s, anchored first-failure → first-success per gotcha #75). It corroborates the two numbers M5 already owns — 14.53 s for a killed predictor pod, 18.24 s for a stop/start | `make backup` · `make deploy-monitoring` (`DRY_RUN=1` first, mutating nothing) · `make monitoring-accept ACCEPT_ARGS=--json` · `make probe-mlserver-metrics` · `make verify-m5` | **`make monitoring-accept` GREEN 10/10.** Targets: `kserve-predictors` 1/1, `kubernetes-service-endpoints` 6/6, `kubernetes-nodes-cadvisor` 3/3 — **all up, none permanently red.** The pipeline is proved by MEASUREMENT, not by a target list: one real `make quote` (`zone 132 -> 48 -> 39.0019 minutes`) moved the inference counter **17 → 18** across a scrape. All **11 board queries** on the provisioned `crosstown-serving` dashboard returned live series. **Idempotent re-run: every pod 9–11 min old, 0 restarts** (the ingress Service-annotation change touched no Deployment). **`make verify-m5` re-run GREEN 49/49 after every mutation** — the wire is where M5 left it |
| 2026-08-19 | **M5-S5 — the runbook, the Production Readiness Review, and the M5 gate.** The only mutation this row records is a **deliberate stop and start of the InferenceService** (`serving.kserve.io/stop` annotation, added and removed), run so the runbook's §3 quotes an observation instead of a hope. New artifacts: `docs/runbooks/serving.md` (8 sections) · `docs/rituals/2026-08-19_prr-m5.md` (the PRR minutes, four boxes, every box carrying pasted evidence) · `make verify-m5` + `make verify-m5-redteam` · `make stop-start-drill`. **The rollback to version 1 is TYPED and NOT REHEARSED** — M5 is legislated alias-neutral, so rehearsing it would cost the one thing the milestone forbids; the runbook says so in its own §4 block quote, in §8's list, and here | **2** (`@champion`, `auto-lgbm-v2`) — **READ, never written.** The gate never types a version: §2 compares the response's `model_version` stamp with what the alias resolves to, and §7 asserts the alias still points at the run the M3 bake-off recorded as its winner. Version **1** (feature set `v1`) is confirmed present as the rollback target | unchanged — 100% to the single predictor replica; **no canary** (Standard mode, ADR-004). A stop/start is therefore a full outage, measured at **18.24 s** | `uv run python scripts/serving_stop_start_rehearsal.py` (= `make stop-start-drill`) · `make verify-m5` · `make verify-m5-redteam` | **GREEN 49/49 sub-checks in 7 sections, 5.762 s, exit 0** — the route (`/healthz` 200, controller on the node the kind config names) · the champion on the wire, **asked for one real prediction** whose `model_version` equals the alias and whose value reproduces the parity record's row at **0.000e+00** · parity re-read from the record against `parity.TOLERANCE_MINUTES` **on disk** · the load shape with its rate/window/concurrency/mix and the 90%-CPU clause replayed (gotcha #74) · self-heal with the **outage anchors re-derived** (`recovered_at 40.03 − first_error 25.5 = 14.53`, where the error-span anchor would say 14.251 — gotcha #75) · the runbook's every quoted number checked against the record it cites, and every `make` target it types checked against the Makefile · the PRR's four boxes each carrying evidence. **Stop/start REHEARSED: the route stopped answering 3.12 s after the annotation and answered again 18.24 s after it was removed**, on a new pod (`…-qrd6f` → `…-xj2q6`); `spec.replicas` goes **absent**, not `0`, which is why the runbook forbids `kubectl scale`. **The new coherence check is the story's finding**: the served version's `feature_set` tag must equal `configs/train.yaml: features.version`, because version 1 eats 5 features and version 2 eats 24 — so an alias-only rollback yields a 500 on every quote from a system whose every condition says `Ready`, and `make verify-m5` §2 is what catches it. Red team **PASSED**: one recorded number rewritten (`selfheal.json`'s `outage_seconds` **14.53 → 14.251**, the record's own error-window span — gotcha #75's mistake re-made, wrong by 0.28 s) → **RED, exit 1**, the anchor leg and the runbook-quotation leg both firing, **47 sub-check lines still passing**, byte-identical sha256 restore (`f1712acf9f80…`), **GREEN again**. Records (tracked): `automation/runs/m5-s5/stop-start.json` |
| 2026-08-19 | **M5-S4 — what the endpoint costs under load, and what losing it costs.** No deployment changed and nothing was reconfigured: this row records a MEASUREMENT of the deployment M5-S2 made, plus one pod deliberately destroyed and replaced by its own controller. New: `make load` (an open-loop load client, stdlib only — arrivals scheduled at `t0 + k/rate` whether or not a worker is free, so the headline percentile is measured from the SCHEDULED instant and a client that falls behind cannot hide it) and `make load-drill` (preflight → ramp → headline → kill the predictor mid-load). Request mix is `parity.HAZARDS`, the sixteen committed rows M5-S3 declared | **2** (`@champion`, `auto-lgbm-v2`) — **READ, never written**, and read off the TIMED RESPONSES themselves: mlserver stamps `model_version` on every answer, so all 240 headline requests and all 662 successful self-heal requests say `2`, across the kill | unchanged — 100% to the single predictor replica; **no canary exists in Standard mode** (ADR-004's honest cost), which is also why a lost pod is a full outage and not a shift | `make load-drill DRILL_ARGS="--ramp 2,4,6,8 --ramp-seconds 20 --seconds 60 --concurrency 8 --selfheal-seconds 180 --kill-at 25"` (×2 — see the RED first attempt) · the ~40 s probe form with `--skip-selfheal` | **GREEN, 7/7 self-heal checks.** **Headline: p50 17.2 ms · p95 104.2 ms · p99 107.2 ms · max 115.4 ms at 4 req/s for 60 s, concurrency 8, hazard mix, 0 errors in 240 requests** (achieved 4.02 req/s; `service_ms` differs from `latency_ms` by 0.1 ms, i.e. no queueing). **Capacity: 1.31 of 2 CPU cores (0.326 core-s/request), 236 MiB against a 1 GiB request** — so **the CPU *request* of `200m` understates real usage by ~6×**, recorded for S5's PRR box and deliberately NOT changed here (a measurement story does not edit what is on the wire). Ceiling measured, not guessed: 6 req/s = 96% of the limit, 8 req/s = 101% and p50 jumps 18 → 115 ms. **Self-heal: the predictor pod deleted at T+25 s of a 180 s window, from inside the load client's own clock → 14.53 s unavailable** (first refusal T+25.5 → answering again T+40.03; 14 seconds with no successful response at all), **58 failed requests of 720 (56 × `503`, 2 × `502`)**, then **559 requests with 0 errors** against **0 errors in the 100 before the kill**. The replacement is a **different pod object by UID** (`f6bf83df…` → `2ba0096c…`, identity never name — the M4-S5 lesson) **on a different node** (`mlops-taxi-worker` → `worker2`), which is M5-S2's `kind load` to all three nodes being paid back exactly as its note predicted. The prediction was written to disk BEFORE the kill (`kill-prediction.json`). **The first attempt went RED and is kept unedited** at `automation/runs/m5-load/attempt1-at-the-ceiling/`: its ramp rule chose 8 req/s — the CPU throttle ceiling — so its p95 measured the quota and its self-heal leg reported a **182-second "outage"** that was really 13 seconds of downtime plus a saturation-induced error tail. Both were fixed as QUANTITIES, not thresholds (gotchas **#74**, **#75**). Records (tracked): `automation/runs/m5-load/*.json`. `docs/serving_load_m5.md` |
| 2026-08-19 | **M5-S3 — train/serve skew is MEASURED, and a whole class of rider stopped getting a parse error.** No deployment changed: this row records a MEASUREMENT of the deployment M5-S2 made, plus the client-side fix F-030 forced. `make parity` builds ONE 16×24 matrix through `taxi_mlops.features` and scores it TWICE — the registry-loaded champion in this process, and the live InferenceService — so the delta is the model bytes + the serving runtime + the wire and nothing else. **F-030 (HIGH, closed same session): every request whose zone had no centroid had been answering `HTTP 422 json_invalid` since the endpoint existed**, because `json.dumps` writes NaN as a bare token that is not JSON; ~1% of every split, including 264→264, the largest single OD route in the data. **F-031/gotcha #73 (closed same session): the red team's first arm went green under its own tampering**, proving the client's documented "the V2 payload is positional" false — this runtime pairs by NAME through the logged signature | **2** (`@champion`, `auto-lgbm-v2`, run `92b73bd4f77d…`) — **READ, never written**: version 2 before and after both commands, checked by the drill itself | unchanged — 100% to the single predictor; no canary exists in Standard mode (ADR-004's honest cost, landing at M6) | `make parity` · `make parity-redteam` · `make quote QUOTE_ARGS="--at 2019-03-11T00:30:00 --pu 264 --do 264"` (the F-030 reproduction, before and after) | **GREEN: `max \|offline − online\| = 0.000e+00` minutes over 16 hazard rows against a 1e-6 bar** — identical, not merely within tolerance, including both no-geometry rows and the unseen OD pair `55 → 148`. M5-S2's spot-check row reproduces at **39.001937154**. **Red team PASSED, 7 checks / 0 failures**: arm A (every feature under its own name and dtype carrying its neighbour's values) → **4.210e+01 minutes** of skew, a 48-minute trip quoted at 6; arm B (version 1 loaded offline, a READ) refused at the feature-set guard before a number existed. Nothing was deployed, restarted or promoted; the untampered run is GREEN again at the end of the drill. Record: `automation/runs/m5-parity/parity.json` (tracked). Transcripts: `docs/parity_m5.md` §5 |
| 2026-08-19 | **M5-S2 — the champion FIRST goes on the wire.** One KServe InferenceService (`serving/nyc-taxi-eta`, Standard/RawDeployment per ADR-004) on a ClusterServingRuntime this repo declares and pins (`taxi-mlserver` -> `taxi-mlops-predictor:mlserver-1.7.1-lgb-4.7.0`, built from `docker.io/seldonio/mlserver:1.7.1-mlflow@sha256:492c8bba…` + `lightgbm==4.7.0`, delivered to all 3 nodes by `kind load`). Model store read as a NEW least-privilege MinIO identity `serving` under the custom `serving-readonly` policy — read-only AND scoped to `mlflow-artifacts` alone | **version 2** (`models:/nyc-taxi-eta@champion`, run `92b73bd4f77d…`, feature set v2, 24 features) — RESOLVED from the alias at deploy time, never typed; read before AND after, unmoved | **100% to version 2** (one replica; Standard mode has no canary — ADR-004's honest cost, M6's spike) | `make serve` (×4, incl. `DRY_RUN=1`) · `make quote` · `make quote QUOTE_ARGS="--at 2026-08-19T09:15:00"` · `make quote QUOTE_ARGS="--at 2031-08-19T09:15:00"` · `uv run python scripts/resolve_champion_storage.py --check` | GREEN. InferenceService **Ready True**, url `http://nyc-taxi-eta-serving.local`, predictor `…-7ff5ccd649-pxkwr` on **mlops-taxi-worker2**, 0 restarts. Route: `GET localhost:8081/` -> 404 without the Host header, `GET /v2/health/ready` -> **200** with it, and the accept check is a **PREDICTION** and not a health probe (gotcha #59): `2019-07-04T09:15:00, zone 132 -> 48 -> 39.0019 minutes`, mlserver stamping **`model_version: "2"`** on the response itself. It matches the locally-loaded champion **bit for bit** (absolute delta 0.000e+00, ONE row — the 1e-6 gate is M5-S3's). **Idempotent re-run: `unchanged`/`configured`, the SAME pod uid, 0 restarts, 2m1s old** (the M4-S2 shape). `DRY_RUN=1` mutates nothing. **F-009 CLOSED** (option b: serving needs the same alias -> logged-model -> artifact_location hops, and a deploy trusting the registry's `source` would download an EMPTY prefix and succeed) and **F-019 CLOSED** (table derived to 2030 + a typed 422 refusal past the horizon). Three defects fixed, the one worth remembering being a **false green**: `kubectl wait --for=condition=Ready inferenceservice` is satisfied by the pod being REPLACED, so the accept check interrogated the predecessor — now `rollout status` first. **No canary, no rollback rehearsal, no load numbers** — M5-S4/S5. `docs/champion_on_the_wire_m5.md` |
| 2026-08-19 | **M5-S1 — the serving PLATFORM lands, and the two gates' evidence enters review.** `make deploy-serving` (`scripts/deploy_serving.sh`) installs **ingress-nginx chart 4.15.1 / app 1.15.1** into namespace `ingress-nginx` (the DECLARED route: host **8081**←container 80 on the control-plane node, pre-provisioned at cluster creation — hostPort + a `kubernetes.io/hostname` nodeSelector + a toleration for that node's `NoSchedule` taint, with the node name DERIVED from the kind config's cluster name and the values file asserted against it), **cert-manager v1.21.1** into `cert-manager` (KServe's webhook certificates; no ClusterIssuer, no ACME, no DNS solver), and **KServe v0.20.0** (`kserve-crd` + `kserve-resources`, OCI charts, digests `92deb742d22a…`/`956c4860374f…`) into `kserve` in **Standard/RawDeployment mode per ADR-004**. Separately: **F-029's mechanics** — `automation/runs/**/*.json` un-ignored and committed (32 records), logs and `.status` still ignored | n/a — **no model touched, and by construction**: the deploy reads no `.env`, passes no `--set`, and a unit test asserts it cannot name `champion`/`models:/`/`mlflow` in code. `@champion` is version **2** and nothing this session ran read it | n/a — **nothing is on the wire yet**; the champion goes on at M5-S2 | `make backup` · `make ports` · `make deploy-serving DRY_RUN=1` · `make deploy-serving` (×2) · `make verify-m3` · `make verify-m4` · `make verify-m3-redteam` · `make verify-m4-redteam` | **GREEN.** Backup first (the M4-S2 precedent, and the existing snapshot predated the marts-tail state): `2026-08-19T02-54-59Z`, **6 databases and 331 MinIO objects where M4-S2 had 5 and 105** — nobody edited a list, the script enumerates from the server, which is the first time that design has been tested by a changed cluster; 1.6 GiB, every dump verified by gzip CRC + pg_dump's completion marker; **restore still NOT rehearsed and every artifact says so**. Install: four releases at REVISION 1 in **3m13s**, controller on **mlops-taxi-control-plane** (the R2 failure mode — a controller on a worker answers nothing and looks like a KServe fault — checked by printing `-o wide`, not assumed), `serving-cert True` issued, `defaultDeploymentMode: RawDeployment` **read back off the live `inferenceservice-config` ConfigMap** rather than off the values submitted. **Idempotence proved by pod AGE**: the re-run took every release to REVISION 2 with the pods 4m44s / 3m35s / 2m35s old and zero restarts. `ingressclass nginx (default)`, **six KServe CRDs on Kubernetes v1.36.1** — risk R1 did not materialise, **ADR-004's plain-mlserver fallback armed and unspent**. Accept: `GET localhost:8081/` -> **404** (the pass — route up, nothing behind it) AND `GET /healthz` -> **200**. **The first accept check went RED over a perfectly good install** by demanding a `Server: nginx` header ingress-nginx suppresses on purpose — gotcha **#70**, fixed by asking the server which endpoint it serves. `DRY_RUN=1` verified to leave `helm list -A` and the namespace list untouched (gotcha #30). F-029 half: **verify-m3 GREEN 46/46 · verify-m4 GREEN 39/39 · both red teams PASSED** over the now-tracked records, sha256-verified byte-identical restores, and `git status` empty after each — **a clean drill leaves a clean tree**, a property that could not be stated while the files were invisible to git. **The cluster never went down**: nodes 2d, every pre-existing pod Running, no kind config edit, no new hostPort |
| 2026-08-18 | **M4-S4 — F-023 broken open and the project pipeline runs on the cluster.** Flyte re-released with `configuration.inline.storage.signedUrl.stowConfigOverride` (the split-horizon lever the 2.x chart renders no value for but the 2.x binary carries); a named PodTemplate `flyte-task-defaults` (`infra/manifests/flyte-task-podtemplate.yaml`) now supplies EVERY task pod with its MinIO identity, its MLflow route, `imagePullPolicy: IfNotPresent` and the data volume; a `taxi-data` PVC staged with the DVC-pinned trees (`make stage-data`); two new Secrets in namespace `flyte` (`flyte-task-storage`, `flyte-task-mlflow` — 11 converged, D-002's additive shape); and MLflow re-released with `serverAllowedHosts` (**F-025**) | n/a — **no model touched**; `@champion` read before AND after every run by the runner itself: version **2**, unchanged, and a move is a hard failure (exit 2) rather than a warning | n/a (orchestrator, not serving) | `make deploy-flyte` (x3) · `make stage-data` · `make image-load` (x2) · `make flyte-hello` · `make deploy-platform` (x2) · `make pipeline MONTH=2019-01 TRAIN_MONTHS=2019-01` | **GREEN for the plumbing; the full-data run is NOT done and is the next session's** (see `docs/pipeline_m4.md` §8). `make flyte-hello` returns `ActionOutputs(o0="HELLO CROSSTOWN FROM A FLYTE TASK")` with three pods `Completed` — **F-023 CLOSED**. `make stage-data` moved **1.8G across raw/processed/rejected** and verified it by per-tree FILE COUNTS (8/16/8, host == volume), then deleted its own stager so no pod holds the RWO volume open. `make pipeline` run `r5kzpr785rt8m6tn9b7l`: six stages on-cluster, ingest **7,696,617 -> 7,584,656 rows, 1.4547% rejected** — M4-S1's host rehearsal reproduced TO THE ROW by the same code in a container — validate 20 columns, features set v2 / 24 features, train `lightgbm-v1` run `e17ce5846aaf4f90bee8a2609b208c94` in **869.7 s**, and register returning **`decision=NO_VERDICT promoted=false`** as DATA: a sampled run produced a GREEN pipeline and no verdict (F-008 honored by construction). **The cluster never went down** (no kind delete/create, no config edit, no new hostPort). **Five defects found, four of them in the checkers** — including `make pipeline` printing `ok … six stages on-cluster` over a run that had died on ErrImagePull, because `flyte run --follow` exits 0 for a FAILED run; the assertion is now positive (the outputs must carry a `"decision"`) and it caught the next three failures instead of painting them green |
| 2026-08-18 | **M4-S2 — the platform's first lifeboat, and Flyte 2 becomes the fifth tenant.** `make backup` (`scripts/platform_backup.sh` + `scripts/backup_minio.py`) landing outside the repo at `/home/longt/dvc-remote/nyc-taxi-platform-backups/<UTC stamp>/`; **F-021 closed** in `scripts/port_precheck.sh`; **Flyte `flyteorg/flyte-binary` v2.0.42** (image `cr.flyte.org/flyteorg/flyte-binary-v2:v2.0.42`, console `ghcr.io/unionai-oss/flyteconsole-v2` pinned by tag AND digest `@sha256:3cea5ec7…`) in namespace `flyte`, its database via D-002's additive path (**fourth** consumer — Flyte 2 unifies flyteadmin/datacatalog into ONE `runs.database`, so the kickoff's budgeted two became one), its blob store the existing MinIO in a NEW bucket `flyte-data` under a NEW identity `flyte` | n/a — **no model touched**; `@champion` read before and after: version **2**, run `92b73bd4f77d…`, versions [1, 2], identical | n/a (orchestrator, not serving) | `make backup` · `make ports` · `make deploy-flyte` (×2) · `bash scripts/flyte_console.sh --check` · `make flyte-hello` (FAILED — F-023) | **GREEN except the hello-run.** Backup: 5 databases enumerated FROM THE SERVER (marts 1.2GiB/210s · metabase 295.6KiB · mlflow 53.9KiB · optuna 27.0KiB · postgres 389B) + 105 MinIO objects / 352.3 MiB, **1.5GiB total**, every dump verified host-side by `gzip -t` over every byte AND pg_dump's own completion marker, both legs **proven against a deliberately truncated copy** first. **RESTORE IS NOT REHEARSED — stated in the script header, in every MANIFEST.txt and here; it is an M6-gameday candidate, and until then "these files restore a working platform" is a hypothesis.** *[Dated note added 2026-08-19 by M6-S5 leg 2; the sentence above is kept as the record of what was true when this row was written. The M6-gameday candidacy was taken up: restore is now **SCRATCH-REHEARSED (2026-08-19)** and a **full restore over a dead platform is still NOT rehearsed** — see the M6-S5 row and `automation/runs/m6-restore/restore_drill.json`.]* Same-disk limit identical to the DVC remote's. Honest cost recorded for M7: `marts` is 1.2GiB and 210s of the ~4 minutes and is the ONE database already provably rebuildable from DVC pins (M1-S5's fresh-volume proof); the other four are 377KiB and under 2s and are the irreplaceable ones. Flyte: first install failed `context deadline exceeded` at `--wait --timeout 10m` **while all three pods were healthy** — the 99 MB console image took 9m49s to pull; timeout raised to 20m with the measurement beside it. Second run: `STATUS: deployed REVISION: 2`, all three deployments rolled out **with every pod 17 minutes old** — a clean upgrade that restarted nothing. `[pg-db] flyte: before = role absent, database absent` -> `ok flyte owner=flyte`, `5 database(s) converged`. API reachable from WSL: `GET /healthz -> 200` (the path was asked of the server — `/healthcheck`, the 1.x path, is 404). **No hostPort, and that is the recorded deviation**: kind publishes host ports at cluster-CREATE only, the cluster is stateful, and no ingress controller exists until KServe at M5 — so 8080 stays RESERVED and access is `make flyte-console`. No secret on a command line (mode-600 temp overlay, deleted on EXIT). **Cluster never went down**: nodes 27h, every pre-existing pod Running. **Not green: `make flyte-hello`** — reaches the control plane, creates the project, resolves the image, builds the bundle, then fails uploading it (`ConnectError: Name or service not known`) because the client is handed the in-cluster MinIO DNS name. **Wall recorded, attempts 5. Filed as F-023. ADR-002's fallback NOT executed** — its trigger is deployment or MLflow interop, and deployment succeeded |
| 2026-08-17 | **Error-segment board published (M2-S4)** — the third dashboard, and the first that renders MODEL behaviour rather than data health. New mart `error_segments` (grain segment x split, 1,151 rows) published into the same `marts` database by the existing publish path; no new port, no new service, no new database — the BI seat M1-S5 built absorbed a whole milestone's new subject with a JSON file and a dbt model. | `nyc-taxi-eta` version 1 (`@champion`), scored not re-fitted | n/a (BI, not serving) | `make predictions` -> `make duckdb` -> `make marts` -> `make boards`; read-only twin `uv run python scripts/metabase_boards.py --verify` | `Error segments (M2)` created with **11 cards** (dashboard id 4), every card citing a KPI id and querying the `marts` warehouse. `--verify` GREEN across all 3 dashboards, including a card RUN: `card 'KPI-13 · what the booster buys, by hour of day (test)' RAN and returned 24 row(s)`, plus `no card claims KPI-09/KPI-10 (gotcha #15)` on every board. Publish printed `COPY 1151` for `error_segments` beside the unchanged `COPY 56127878` / `44792` / `8` / `80`. Idempotence by NAME held for the two existing boards — all 17 of their cards printed `card updated`, ids 2 and 3 stable. |
| 2026-08-17 | M1-S5 **planned cluster rebuild + Metabase, the BI seat** — the rebuild existed to publish ONE new host port (3030→30300), because kind publishes host ports at cluster-CREATE time only. Metabase v0.63.13 (image pinned by tag AND digest `@sha256:6e188e70…`), plain manifest, app-db in the one Postgres as D-002's **third** consumer | n/a (no model until M2) | n/a | `make ports` · `make cluster-down` · `make cluster-up` · `make deploy-platform` · `make marts` · `make deploy-metabase` (×3) · `make verify-m1` | GREEN. `docker port mlops-taxi-control-plane` → `30300/tcp -> 0.0.0.0:3030` alongside the four existing pairs. **The marts came back from the recipe alone** on a fresh 8Gi volume — `dbt build` PASS=39 ERROR=0 in 3.64s, then `COPY 56127878` · 44,792 · 8 · 80, identical to M1-S4's counts to the row. **D-002's fresh-volume path exercised**, and the evidence is unambiguous: the initdb ConfigMap contains exactly one `CREATE DATABASE` (`mlflow`), PGDATA's `PG_VERSION` is stamped `2026-08-17 02:36:53`, and `pg_database` holds mlflow (oid 16385) → marts (16387) → metabase (16389) — the two initdb cannot create arrived by step [5/7] in the same run. Re-run printed `before = role present, database present` for all three. **F-003's remaining condition discharged**: on a FRESH object `kubectl apply -f infra/manifests/postgres.yaml` said `statefulset.apps/postgres unchanged` on the first apply and again on the second — the M1-S4 fix generalises, finding stays closed. Metabase: first `deploy-metabase` FAILED at its own host-route check (`000`) — the check raced a cold JVM's first external request; fixed to retry with a bounded wait, and the two following runs printed `service/metabase unchanged` · `deployment.apps/metabase unchanged` with every card **updated** rather than created (dashboard ids 2 and 3 stable across runs). Boards: **17 cards over 2 dashboards**, converged from `analytics/metabase/boards/*.json`. |
| 2026-08-16 | M1-S4 **gold marts published into the one Postgres** — database `marts`, schema `marts`, 4 tables (dbt-core 1.12.2 / dbt-duckdb 1.11.0). Plus D-002's database/role recipe wired into the deploy path as step [5/7] | n/a (no model until M2) | n/a | `make deploy-platform` (now runs `scripts/postgres_databases.sh`) · `make marts` (twice) · `make marts-redteam` | GREEN. `dbt build` PASS=39 / ERROR=0 in 3.24s. Publish printed `COPY 56127878` for `trips_clean` (exactly the ingest total), 44,792 · 8 · 80 for `zone_hourly_stats` · `monthly_kpis` · `rejections_by_rule`; counts read back identical from BOTH engines. Second `make marts` = **220.4s, exit 0, identical counts** — the atomic swap watched live (`trips_clean` still serving 56,127,878 while `trips_clean__staging` filled). Red-team: ERROR=1 naming `accepted_range_trips_clean_trip_duration_minutes__120__1` (`Got 2 results`), **SKIP=19** — the aggregates never got built from a failing fact — nothing published, local layer auto-restored to green. Footprint, stated: **13 GB** for `marts`, **23 GB peak** mid-swap (old + staging), node disk 783 G free after. No port was published: the data went DuckDB -> CSV -> `kubectl exec -i` -> `psql \copy`. D-002 proven on the EXISTING volume (PGDATA initialised 15:47:03, `marts` created 17:44): `before = role absent, database absent` -> `ok marts owner=marts`; re-run `before = role present, database present`, `mlflow` a no-op on both. |
| 2026-08-16 | M0 platform first deploy — MinIO 5.4.0 + Postgres 16.11 + MLflow chart 1.11.4 (app 3.15.1) onto kind `mlops-taxi` | n/a (no model until M2) | n/a | `make cluster-down && make cluster-up` (kind config gained the 5000/9000/9001 host-port mappings) · `make deploy-platform` · `make verify-m0` | GREEN: 18/18 verify-m0 sub-checks, exit 0. First attempt FAILED — `helm upgrade --wait` hit `context deadline exceeded` because MLflow was OOMKilled (exit 137) under its default 4 uvicorn workers; fixed with `extraArgs: {workers: "1"}` (gotcha #28). Re-run of `deploy-platform` on the live stack = clean upgrade AND repaired a deliberate `kubectl scale --replicas=0`. |
| 2026-08-16 | M0-S4 **destroy → rebuild cycle** — the whole platform deleted and rebuilt from the recipe alone, on the same pins (kind 0.32.0, node image `kindest/node:v1.36.1@sha256:3489c767…`, MinIO 5.4.0, Postgres 16.11, MLflow chart 1.11.4 / app 3.15.1) | n/a (no model until M2) | n/a | `make destroy` · `make cluster-up` · `make deploy-platform` · `make verify-m0` | GREEN, and boring: 3/3 nodes Ready in ~27s, both helm releases installed at REVISION 1 (a genuinely fresh cluster, not an upgrade), verify-m0 exit 0 with all 18 sub-checks. What survived vs what did not, measured: `.env` sha256 `34cde86f…` IDENTICAL before and after, so the rebuilt Postgres/MinIO came back on the SAME credentials; the tracking DATA did not — experiment `m0s4-pre-destroy-witness` (id 1), created before the destroy, returns `RESOURCE_DOES_NOT_EXIST` after it, and the store holds only `Default`. PVCs `data-postgres-0` (8Gi) and `minio` (20Gi) went with the cluster, by design. |
