# Sign-off ledger — every gate crossing. What this ledger doesn't hold didn't happen.
# Rule: Producer role ≠ Approver role on every row (ORG.md independence rule 2).
| Date | Gate | Producer (role) | Approver (role) | Verdict | Evidence link | Conditions |
|---|---|---|---|---|---|---|
| 2026-08-31 | **CLEANUP close** (`docs/milestones/CLEANUP_KICKOFF.md` — the followability cleanup, PO directive 2026-08-29-2: prune the bloat, keep every guarantee) | EXEC (Opus 5) — slices CU-S1…S5, PRs #82–#87 (role:MLOps per slice; MLE on CU-S2) | ARCH/Fable 5 (cleanup boundary session, dn) | **PASS** | **All ten gates re-run by the approver 2026-08-31 → GREEN, exit 0 each** (`verify-m0`…`verify-m9`; `verify-m9`'s banner citing the CLOSED-and-cited observed box); host suite **1,408 passed, 0 skipped** (`67.47s`) — the exact count the README declares and `readme-check` checks from the other side; `make readme-check` GREEN (before AND after this boundary's own row flip); `uv run ruff check src tests pipelines` clean; the executor's close-out battery **19/19 red teams RED-on-plant then GREEN-on-restore, 1,593.6 s** (`automation/runs/m-cleanup/battery.json` — deliberately NOT re-run by the approver: re-running rewrites the two records F-086 documents, which is that finding's own lesson); `docs/cleanup_report.md` reviewed against the directive's floor — §5's twelve re-derived guards each carry old property → new property (nine moved from reading a file to RUNNING a behaviour, none widened, none deleted) and §6's guarantee table verified live (`@champion` 2 / `feature_set v2`, no version 3, `uv.lock` byte-identical to `lock-rebaselined-m9-publish`, all 5 DVC pins up to date, thresholds/bars/knobs unmoved); lineage `git merge-base --is-ancestor dd4464f origin/main` → yes (PR #87's merge); tag **`cleanup-closed`**; close section `docs/milestones/PROGRAM_CLOSE.md` §8 | **F-086 and F-087 raised AND CLOSED by CU-S5** (the battery's record-rewrite + ordering constraint; the transformer route wait's silent fall-through — closed by the consolidation taking the STRICTEST behaviour, named as a behaviour change). Open at close: **F-001 only** (standing, non-blocking). Debt register CLOSED. **The lint gap (report §9) DECLINED to charter with the deciding measurement taken live**: `ruff check scripts` = 42 E501 and ZERO correctness-class findings; the F821 was CU-S4's and is fixed; one small slice if the PO directs it. **The heal path's fixed role RE-DECLINED with the second occurrence priced** (session dm: one read-only executor session against an empty queue; the POST_PUBLISH reasoning stands, and the park below stands the heal path down entirely). **AWAITING_PO 2026-08-30-2** stays OPEN and the PO's. Nothing chartered — program CLOSED, queue EMPTY. Chain re-parked via `automation/STOP` (the PP-S1 precedent — the PO's own resting state, lifted by them 2026-08-31 for this triage and restored at its close); resume `rm automation/STOP && automation/next_session.sh architect 120` |
| 2026-08-29 | **PP-S1 landing triage** (post-publish boundary — the one story `POST_PUBLISH_KICKOFF.md` chartered on the PO's 2026-08-29-1 answer: the two-way demo link) | EXEC (Opus 5) — story PP-S1, PR #78 (role:MLOps) | ARCH/Fable 5 (post-publish boundary session, dc) | **PASS** | `make verify-m9` re-run by the approver 2026-08-29 AFTER the landing → **GREEN** (closing line verbatim `[verify-m9] GREEN — every M9 sub-check passed.`, banner citing the CLOSED-and-cited observed box; `@champion` 2 / `feature_set v2`; no registry version created after `m7-closed`; `uv.lock` byte-identical to `lock-rebaselined-m9-publish`; all 5 DVC pins up to date); `make readme-check` GREEN (incl. the corrected `1,320 tests` claim — the twin went RED unplanted on 1,319 and was fixed in the story); lineage `git branch -r --contains 3f6f232` → `origin/main`, merge `70347c9`; the accept record's two sha pins moved `b1edd074…` → `f5f0bde9…` exactly as the charter priced, `po_observed_run` CLOSED-and-cited carried forward intact (F-067 held); the link proven TWO-WAY from both ends through the route (`GET /demo/` 200 carrying `analytics.html` · `GET /demo/analytics.html` 200 carrying the back-link) | **F-081 raised AND CLOSED by the story itself** (four record fields moved where the charter priced two; the fourth was six days of `--no-write` drift from M9-S7's F-062 wording — nothing wrong with code, pod or gate; accounted in `demo/README.md` §7.1). Open at close: **F-001 only** (standing, non-blocking by the PO's 2026-08-24 answer 7). Debt register CLOSED. Nothing chartered — program CLOSED, inbox EMPTY (deviation reasoned in `POST_PUBLISH_KICKOFF.md` §0.1). Chain paused via `automation/STOP` (the PO's own 2026-08-26 resting state restored; no watchdog code touched); resume `rm automation/STOP && automation/next_session.sh architect 120` |
| 2026-08-29 | **PR #77 triage** (post-close, PO-directed: the analytics companion page, held for ARCH per the PO's flip note of 2026-08-29 in AWAITING_PO 2026-08-25-3) | PO (Phu-Hong-Duong, viewing-copy session — PR #77, 2 commits; chain-hot files deliberately untouched per the viewing-copy protocol) | ARCH/Fable 5 (post-publish triage session) | **PASS — merged `31e4c48`, with one repair pushed to the branch first** | Triage verified before the merge: all five bake-off rows on the page reproduce `automation/runs/m3s5/bakeoff.json` EXACTLY (values, within-rates, verdicts, winner) · the 2020-01-29 daily row (223,287 trips · MAE 3.1449 · bias +0.2355) re-derived LIVE from the analyst layer's `scoring_predictions` view · self-heal anchors 25.5 → 40.03 = 14.53 s and 58/720 match the M5 records · PSI 0.0217 / volume 0.3913 / gate bar 3.2848 match · JS hygiene clean (no storage/cookies/eval/fetch), pure ASCII. The repair (`30c5345`): the Google Fonts `<link>` removed — a third-party call from every viewer's browser under a header claiming the page "calls nothing", against `index.html`'s zero-external-refs precedent; every font stack already named system fallbacks. After merge, live: `make deploy-demo` GREEN incl. the NEW analytics-sha leg · `make demo-accept --no-write` 9/9 (M9's tracked record untouched, F-063's rule) · `make verify-m9` GREEN · `make readme-check` GREEN after the follow-up README row · `tests/unit/test_demo_page.py` 23/23 | Follow-ups from the PR body dispositioned: root-README row **LANDED** in the boundary commit (readme-check owns that file — the PR correctly declined to touch it) · the optional two-way link from `index.template.html` **DECLINED with reason** (`index.html` is bound into M9's closed accept record and byte-identity contract; a cosmetic link does not justify re-measuring a closed milestone's artifact chain; the shipped one-way back-link stands). The PO's public flip of 2026-08-29 committed to the inbox verbatim. Queue EMPTY; chain RE-PARKED at AWAITING_PO **2026-08-29-1**; resume `automation/next_session.sh architect 120` |
| 2026-08-25 | **M9 PUBLISH close** (`docs/milestones/M9_PUBLISH_KICKOFF.md` §Exit — the phase that landed the PO's three letters of 2026-08-24-5 plus 2026-08-24-4 before the flip) | EXEC (Opus 5) — stories M9-S10…S13, PRs #73/#74/#75/#76 (roles MLE/MLOps/SRE/MLOps per story) | ARCH/Fable 5 (publish boundary session) | **PASS** | The five gates the kickoff §Exit names, re-run by the approver 2026-08-25 and counted live (`grep -cE "ok  "`): `verify-m2` GREEN **57** · `verify-m3` GREEN **47** · `verify-m7` GREEN **63** (was 62 — S10's added era-stable sub-check, re-derived per gotcha #50) · `verify-m8` GREEN **51** against the S11 anchor `lock-rebaselined-m9-publish` · `verify-m9` GREEN **46**, banner citing the CLOSED-and-cited observed box; `make readme-check` GREEN after the row flip; host suite **1,319 passed, no skips**; lineage: M9-S13's merge `24364ab` (PR #76) reaches `origin/main`; tag **`m9-publish-closed`**; close section `docs/milestones/PROGRAM_CLOSE.md` §7 | **F-074…F-080 all CLOSED by their own stories** (F-074 the empty-diff resolver, F-075/076/077/078 the rotation's four checking defects, F-079 the leaked env var, F-080 the hook-vs-drill collision — every one closed in the session that raised it). One boundary repair by the approver (F-065 precedent): the M9 Publish row joined `readme_check.py`'s `STATUS_ROWS` pin (12 → 13, test re-derived) in the flip commit. Open at close: **F-001 only** (the standing session-allowlist friction note, AWAITING_PO 2026-08-16-2, non-blocking since M0). Debt register CLOSED. The chain RE-PARKS on the flip entry **2026-08-25-3** — the one live item is the PO's click; resume `automation/next_session.sh architect 120` |
| 2026-08-25 | **M9 EPILOGUE close** (`docs/milestones/M9_EPILOGUE_KICKOFF.md` §Exit — the chartered ARCH touch; no new milestone follows, so this row closes the chain's last crossing) | EXEC (Opus 5) — stories M9-S5/S7/S8/S9, PRs #66/#68/#69/#70 (roles MLOps/SRE per story); S6's measurement PR #67 (MLE) | ARCH/Fable 5 (epilogue boundary session) | **PASS — with M9-S6 explicitly PARKED on the PO, not landed** | `make verify-m9` re-run by the approver 2026-08-25 → **GREEN, 45 sub-checks across 7 sections, exit 0**, banner citing the CLOSED-and-cited box and the F-019-both-states leg; `make readme-check` GREEN after the row flip (and it caught this session's own diff first: the host suite moved 1,244 → **1,246** with F-072's two tests, and the checker went RED naming the claim before anyone looked — the M9-S8 instrument doing its job on its author's approver); host suite **1,246 passed, no skips**; lineage `git merge-base --is-ancestor m9-closed origin/main` → yes and M9-S9's merge `fe0fca2` reaches `origin/main`; tag **`m9-epilogue-closed`**; close section `docs/milestones/PROGRAM_CLOSE.md` §6 | **F-072 and F-073 raised AND closed at this boundary** (the F-065 precedent: one-line-class repairs landed by the approver with demonstrations — F-072's load-bearing test RED against the old scheduler, F-073's re-derived leg RED on a planted citation-free closure with 44 sub-checks still passing, both restored byte-identically). Open at close, none silent: **F-016 + F-068 parked at AWAITING_PO 2026-08-24-4** (S6's replay wall — the identity case needs the PO's letter; the ONLY open findings in the register) · **the publish flip + `sqlparse` fork at 2026-08-24-5** (the PO's click and letter). Debt register CLOSED (D-001…D-004). The chain is deliberately RE-PARKED — no successor scheduled; the resume for any answer is `automation/next_session.sh architect 120` |
| 2026-08-24 | **M9 gate + PROGRAM CLOSE** (BLUEPRINT §9/M9 — the last milestone; no ◆ at M9; this boundary closes the program, so the sweep ran ALL TEN gates) | EXEC (Opus 5) — stories M9-S1…S4, PRs #61–#64 (roles MLOps/SRE per story; DA C on the demo) | ARCH/Fable 5 (M9 boundary = program-close session) | **PASS — with §9/M9's one human accept line recorded OPEN, not green** | `make verify-m9` re-run by the approver at the close 2026-08-24 → **GREEN, 45 sub-checks across 7 sections, exit 0**, closing line verbatim `[verify-m9] GREEN — every M9 sub-check passed.` with the banner printing its own OPEN ITEM (`OPEN BY DESIGN: §9/M9 asks for one non-technical person to complete a query unassisted, OBSERVED`) — the gate is chartered to assert that box is recorded honestly, never to render it green; **the program-close sweep then ran every predecessor gate live the same session: verify-m0 · m1 · m2 · m3 · m4 · m5 · m6 · m7 · m8 ALL GREEN** (m2 after the one repair recorded as **F-065** — its root-stray allowlist predated M8-S2's `.venv-feast` and had been RED-on-run since 2026-08-21, gotcha #50's shape, found only because a close sweeps gates nothing else re-runs; one line + dated reason, `make verify-m2` GREEN 55/55, 15 gate tests passing, host suite 1151 passed); red team `make verify-m9-redteam` PASSED at M9-S4 (planted `expected_keys.total` 57,688 → 57,425 — a correct-looking expectation of the wrong POPULATION, short by exactly `zone_static`'s 263 keys — RED exit 1 with 3 FAILs from three artifacts, 42 sub-check lines still passing, no-number and ordering legs deliberately still green, sha256 restore `b875049f8289…` → GREEN 45/45); §9/M9 accept-when against the quoted text: "the zone list renders from the lookup" — 530 `<option>` elements == 2 × the CSV's 265 zones, derived and checked · "a submitted trip returns a live prediction with model version visible" — 39.00193715359812 at `model_version: '2'` through the page's own request path, `\|Δ\| = 0.000e+00` against the transformer-parity record · "one non-technical person completes a query unassisted, observed" — **OPEN, AWAITING_PO 2026-08-23-3, only the PO can close it and this ledger says so rather than pretending**; lineage spot-check `git branch -r --contains bf2d553` (M9-S4) → `origin/main`; tag `m9-closed`; close document `docs/milestones/PROGRAM_CLOSE.md` | **F-065 raised + CLOSED this triage** (the verify-m2 allowlist repair — the row records why ARCH landed one line rather than chartering a session for it). Open at close, none silent, all in `PROGRAM_CLOSE.md` §2 and AWAITING_PO 2026-08-24-2: **F-062 → PO fork** (a dead online store billed to the caller as 4xx — options (a)/(b)/(c) restated, recommendation (b); a live-wire behaviour change with three parity records behind it) · **the PO-observed demo run** (2026-08-23-3, the one §9/M9 line only a human closes) · **F-016 standing** (2026-08-18-1, dormant — nothing fits until the PO reactivates anything) · **the stretch opt-in menu** (2026-08-23-2 — any opt-in gets an ARCH-chartered M9-S5+, the chain resumes on `automation/next_session.sh architect`) · **publish-the-repo** (§12's queued question, now formally with the PO) · error memo §7 row 2 stays open as documented future work (three measurements, named candidate reader) · D-001's registry-pattern deferral stands, landing only at a PO-sanctioned rebuild. Debt register: **CLOSED** (D-001…D-004). The chain is deliberately PARKED — no successor scheduled; everything that remains needs the PO's hands |
| 2026-08-23 | **M8 gate** (BLUEPRINT §9/M8, all legs — the boundary crossing; no ◆ at M8) | EXEC (Opus 5) — stories M8-S1…S5, PRs #52–#56, #58–#60 (S1 in two PRs, S4 in three legs; roles SRE/MLE/DE/MLOps/DA per story) | ARCH/Fable 5 (M8 boundary triage session) | **PASS** | `make verify-m8` re-run by the approver at the boundary 2026-08-23 → **GREEN, 51 `ok` sub-checks across 7 sections, exit 0** (counted live twice: `grep -cE "ok  "` → 51; paste in M9 kickoff §0; closing line verbatim `[verify-m8] GREEN — every M8 sub-check passed.`), incl. §5's five live questions (the transformer answering a hazard from four RAW fields at `\|Δ\| = 0.000e+00` with `X-Taxi-Lookups` naming the two groups that did not cross · 57,688 keys at `noeviction` read off the running Redis · F-043's PromQL question reading `up==1 (2 target(s))` with F-061's fix holding) and §7's alias law in its strong form (not one of the 2 registry versions created after the `m7-closed` tag); `make verify-m7` re-run the same session → **GREEN** (§9/M8's accept inherits it: "v1's M7 gate AND the comparison page exists" — the page is `docs/feast_side_by_side.md`, 12 rows, 3 ADOPT · 4 DIFFER · 5 SURPASS; Show: the parity table `docs/feast_online_parity_table.md` + the page, both committed); the four seams all at 0.000e+00 against bars of EXACT, each bar's document git-ADDED before the record it judges (678 s · 356 s · 320 s · 546 s — law 4 from git); red team `make verify-m8-redteam` PASSED at M8-S5 leg 2 (planted `both_missing` 13 → 0 — a correct-looking measurement of the wrong population — RED exit 1 with 3 FAILs from three artifacts, 48 sub-check lines still passing, sha256-verified restore `153c4399deab…` → GREEN 51/51); lineage spot-check `git branch -r --contains 9e39d2a` (M8-S5, PR #60) → `origin/main`; tag `m8-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M9_KICKOFF.md` §0: **F-058 DECIDED (a), CLOSED this triage** (proof trigger stays inactive; proof banked; M9 legislates no new Flyte trigger) · **F-057 INTAKEN → M9-S3** (normalize, regenerate alone, round-trip test, rebuild re-proved) · **F-054 DECIDED (a), INTAKEN → M9-S3** (the records are git-tracked, so the stated fresh-clone cost is void — verified live) · **F-016 → standing at AWAITING_PO 2026-08-18-1, DORMANT in M9** (nothing fitted, no alias move) · **R-1 DECLINED with recorded reason** (re-measuring three parity records + a redeploy to replace a twice-pinned constant, in the final milestone) · **R-2 INTAKEN → M9-S2** with its own headroom leg (law 4, ninth application) · **R-3 noted, unscheduled** · error memo §7 row 2 stays open in the memo with its three measurements and its named candidate (`airport_regime_flag`, catalog-only) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing, non-blocking; **2026-08-23-2 raised this triage** (the M9 stretch opt-in menu — Ray/CI/trivy/README are PO opt-in per §9/M9, only the demo is committed). Debt: **NONE DUE — register fully closed**; D-001's registry-pattern deferral stands (trigger: image churn; landing: next PO-sanctioned rebuild) — M9 does not trigger it |
| 2026-08-21 | **M7 gate** (BLUEPRINT §9/M7, all legs — the boundary crossing; the ◆ REV monitoring review is the row below, delivered 2026-08-21) | EXEC (Opus 5) — stories M7-S1…S5, PRs #40–#49 (S4 in four PRs, S5 in two legs; #50 boundary reconciliation and #51 REV's review are not stories; roles DE/MLE/SRE/DA per story) | ARCH/Fable 5 (M7 boundary triage session) | **PASS** | `make verify-m7` re-run by the approver at the boundary 2026-08-21 → **GREEN, 62 sub-checks across 7 sections, exit 0** (counted live; paste in M8 kickoff §0; closing line verbatim `[verify-m7] GREEN — every M7 sub-check passed.`), incl. §7's live prediction reproducing the parity record's row at 0.000e+00 with `model_version='2'` == the alias, §6's alias law in its strong form (not one of the retrain's 8 runs is a registry version; the REFUSE stayed a run), and §5's F-050 restarted-branch accounting (the finding's third observation, priced into the boundary decision); §9/M7 accept-when green against the quoted text ("v1's M6 gate" · "the predictions table for the scored month exists and the DA memo cites it" — `marts.scoring_daily`, 15,413,352 rows reconciled three ways, cited through 4 monitoring ids · "the memo explains the drift in domain terms with numbers" — every number re-derived by REV to the row · "Show: the two failure signatures + the predictions table + the memo" — the gate's own closing block prints exactly those three); red team `make verify-m7-redteam` PASSED at M7-S5 leg 2 (planted `volume_ratio` 0.3913 → 0.4021 — F-045 itself — RED exit 1 with 3 FAILs from three artifacts, 59 sub-check lines still passing, sha256-verified restore → GREEN 62/62); ◆ REV APPROVE WITH CONDITIONS (row below), both conditions dispositioned with quoted landings in M8 kickoff §0; lineage spot-check `git merge-base --is-ancestor 86a3cf2 origin/main` (M7-S5's merge, PR #49) → ancestor confirmed, deliberately a different merge than REV's; tag `m7-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M8_KICKOFF.md` §0: **F-051 intaken → M8-S1 leg 1** (denominator fix + monotonicity property test; bar unchanged, not a PO fork) · **F-052 DECIDED option (a) → M8-S1 leg 1** · **F-050 DECIDED (a)+(b) → M8-S1 leg 1** (recurrence measured at once per host restart decides it; the board becomes a STANDING surface, honoring REV's F-051 condition by ordering) · **F-048 DECIDED (c)+(a) → M8-S1 leg 2** (provenance ON the version; backfill inside `registry.py`; (b) refused) · **F-047 DECIDED (a) → M8-S1 leg 2** · **F-046 DECIDED (a)** — accept monthly grain, rely on A-9, the §8.1 sentence lands M8-S1; daily window named-not-scheduled (no quoted M8/M9 scope covers it, gotcha #19) · **F-045 CLOSED** by evidence (its option (a) is what M7-S3 shipped) · **F-016 → standing at AWAITING_PO 2026-08-18-1, DORMANT in M8** (law 3: no gate invocation) · error memo §7 row 2 stays open, M8-S2's catalog is the named reader (`airport_regime_flag` as candidate, nothing fitted) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing, non-blocking. Debt: **NONE DUE — register fully closed**; D-001's registry-pattern deferral stands — M8 does not trigger it (two image builds is not churn) |
| 2026-08-21 | **M7 review** (◆ ritual: fresh-session effective challenge of the finished milestone; NOT the M7 gate crossing, which is ARCH's at the boundary) | EXEC (Opus 5) — stories M7-S1…S5, PRs #40–#49 (S4 in four PRs, S5 in two legs; #50 is the boundary reconciliation after a host reboot, not a story; roles DE/MLE/MLOps/DA per story) | **REV/Opus 5 (fresh session, zero builder context; committed artifacts read before any narrative)** | **APPROVE WITH CONDITIONS** | Findings **F-051 (S2) · F-052 (S2)** in `ledgers/findings.md`; re-derivation committed as `scripts/rev_rederive_m7.py` (read-only, 4 sections, re-runnable — the `error_memo_numbers.py`/`drift_memo_numbers.py` twin-script precedent, because a reviewer's "I recomputed it and it matched" is worth what its transcript is worth unless the recomputation is a file). Re-derived from committed artifacts, **not** from transcripts and **without importing the module under review**: (1) **PSI for all six monitored columns**, 2020-03 vs `trips_train`, re-implemented from the formula in `drift.py`'s docstring against the analyst layer — `hour 0.009790060767709 · dayofweek 0.021666901450342 · PULocationID 0.014301722476401 · DOLocationID 0.015114526743265 · passenger_count 0.017085618281808 · trip_duration_minutes 0.012484979364001`, equal to `automation/runs/m7-drift/drift-2020-03.json` **to 15 significant figures on every column**, max input PSI 0.0217 confirmed below the 0.10 bar; (2) **volume_ratio for all three months re-derived exactly** — `0.8335556483513884 · 0.8776340568603828 · 0.3913368667803675`, matching each record to 16 decimals, off a reference of 43,987,422 rows / 181 days = 243,024.4309 trips/day; (3) **KPI-14/15/16 recomputed straight off `data/scoring_predictions/**.parquet`** (the published rows, never the mart): `3.0295 / 2.9802 / 3.3227` · `83.226 / 83.768 / 80.569%` · `+0.2836 / −0.1703 / +0.5468`, all three matching the manifest, the mart and the memo; (4) the memo's **March cut re-derived to the row** — Mar 01-10 **68.231%** at KPI-14 3.0463, Mar 11-21 **28.448%** at 3.7534, Mar 22-31 **3.321%** at 5.3128 / KPI-15 62.118% / KPI-16 +4.1412 — with worst days 2020-03-26 **6.3693** against January's 2020-01-03 **3.5757**, and `model_version` exactly `['2']` in every month. `make verify-m7` **re-run by the approver 2026-08-21 → GREEN, 62/62 sub-checks across 7 sections, exit 0**, closing line verbatim `[verify-m7] GREEN — every M7 sub-check passed.`, incl. §7's live endpoint answering **10.665224 minutes stamped `model_version='2'`** reproducing the parity record to 0.000e+00 and the F-032 coherence leg (served `feature_set` v2 == `configs/train.yaml`'s `features.version` v2); registry read and left untouched (`@champion` → 2, versions `['1','2']`, **no version 3** — the retrain's REFUSE stayed a run); lineage spot-check `git merge-base --is-ancestor 45d8949 origin/main` (M7-S3's merge, PR #42) → ancestor confirmed | **Nothing is blocked and no path parks — no S1 was found, so no AWAITING_PO entry is raised by this review and the chain continues to ARCH.** M7's numbers hold under independent recomputation: every published drift figure, every monitoring KPI and every period split in the memo re-derives from raw materials, the champion never moved, the retrain's REFUSE is honestly recorded as a refusal, and the milestone's uncomfortable results (A-8 correctly silent through a catastrophe; F-020's correction worth 54 ms) are reported at the size they happened. **The one thing this review wants read out loud: M7's central claim is that A-9 is the marginal A-8 is structurally blind to — and F-051 is a blind spot inside that marginal.** Conditions, both for ARCH's M7→M8 boundary triage, each needing a QUOTED landing (gotcha #19): (1) **F-051** — A-9's ratio is non-monotonic in collapse severity and fails QUIET; the fix is one line plus the monotonicity property test that no current test asserts, and it must land before any milestone treats this board as a standing operational surface rather than a drill-populated one (it interacts with F-050's open (a)/(b) fork, which is about the same board's trustworthiness on this machine — the boundary should dispose of them together). (2) **F-052** — `configs/drift.yaml` is F-013 recurring: a dead second home naming the wrong reference month and the wrong bar, cited as "Sources of truth" by `docs/m7_flow.html`; the config half and the doc half must be fixed together, and the F-013 test's knob tuple widened so the next stale bar cannot land. Neither is closable by prose (this register's F-008 precedent), and **REV closes none of its own findings** |
| 2026-08-20 | **M6 gate** (BLUEPRINT §9/M6, all legs — the boundary crossing; no ◆ at M6, the next ◆ is M7's REV monitoring review) | EXEC (Opus 5) — stories M6-S1…S5, PRs #33, #35–#39 (S5 in two legs; #34 is the PO's front-door refresh, not a story; roles SRE/MLOps/DA per story) | ARCH/Fable 5 (M6 boundary triage session) | **PASS** | `make verify-m6` re-run by the approver at the boundary 2026-08-20 → **GREEN, 63 sub-checks across 7 sections, exit 0** (counted live; paste in M7 kickoff §0; closing line verbatim `[verify-m6] GREEN — every M6 sub-check passed.`), incl. §7's live prediction reproducing the parity record's row at 0.000e+00 with `model_version='2'` == the alias, F-044's compound-label check over every artifact INCLUDING the line the backup prints at runtime, and the alias law in its strong form (M6's two sanctioned alias moves round-tripped; the pointer derived from the record, never typed); §9/M6 accept-when green against the quoted text ("the shadow comparison table exists … before the first traffic shift" — 1,016 rows at segment grain, DA memo verdict NO-GO for v1, the ordering checked on the two records' own clocks · "canary 90/10 observed" — from TWO counters, ingress 9.76% vs the predictors' own 9.33% · "rollback <2min under load" — traffic revert 0.37 s; the alias rollback 35.35 s/34.38 s both ways, F-040 measured · "alert fired in red-team" — A-3 then A-2 end to end to Alertmanager, prediction written first · "at least one prediction wrong and investigated" — TWO wrong, F-041 and the saturation 502s, neither engineered); red team `make verify-m6-redteam` PASSED at M6-S5 leg 2 (planted 13.75 → 13.501, gotcha #75's wrong anchor — RED exit 1 with 2 FAILs from two different artifacts, 61 sub-check lines still passing, sha256-verified restore → GREEN 63/63); lineage spot-check `git merge-base --is-ancestor 9a514a5 origin/main` (M6-S5 leg 2's merge, PR #39) → ancestor confirmed; tag `m6-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M7_KICKOFF.md` §0: **F-043 direction DECIDED (option c — document the ~90%-CPU in-pod instrument limit), intaken → M7-S3** · **F-042's threshold recommendation DECLINED** (shortening a sustain on one drill's authority is gotcha #63 in bar-form; the annotation already carries the measured order) · **F-020 → M7-S4, F-022 → M7-S4** (both DUE at their quoted landing, §9/M7 "scheduled Flyte retrain landing a challenger" — mandatory intake honored) · **F-035's pushgateway landing → M7-S3** · **F-040's remedy stays named-and-unproven deliberately** (M7's alias law reserves moves for the gate; the runbook's do-not-substitute label is the guard) · **the v1 shadow torn down at M7-S1 entry** (boundary decision; its F-043 control work is committed) · **F-016 → standing at AWAITING_PO 2026-08-18-1, now ACTIVE**: M7 proceeds with the gate as pre-registered per the M6 kickoff's standing rule · error memo §7 row 2 stays open in the memo (M7-S5's drift memo is the named next reader) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing, non-blocking. Debt: **NONE DUE — register fully closed** (D-001…D-004); D-001's registry-pattern deferral stands (trigger: image churn; landing: next PO-sanctioned rebuild) — M7 does not trigger it |
| 2026-08-19 | **M5 gate** (BLUEPRINT §9/M5, all legs — the boundary crossing; no ◆ at M5) | EXEC (Opus 5) — stories M5-S1…S5, PRs #28–#32 (roles MLOps/MLE/SRE per story) | ARCH/Fable 5 (M5 boundary triage session) | **PASS** | `make verify-m5` re-run by the approver at the boundary 2026-08-19 → **GREEN, 49/49 sub-checks across 7 sections, exit 0** (paste in M6 kickoff §0; closing line verbatim `[verify-m5] GREEN — every M5 sub-check passed.`), incl. §2's live prediction whose `model_version` equals what the alias resolves to and whose value reproduces the parity record's row at 0.000e+00, §2's F-032 coherence check (served `feature_set` tag == `configs/train.yaml`), and §7's strong form of the alias law (`@champion` → version 2 = the M3 bake-off's recorded winner, derived never typed; rollback target version 1 exists carrying `feature_set: v1`); §9/M5 accept-when green against the quoted text ("KServe Standard, mlserver, storage-config" — live, RawDeployment read off the ConfigMap · "THE parity test 1e-6" — 0.000e+00 over 16 hazard rows · "p95 measured" — p50 17.2 / p95 104.2 / p99 107.2 ms at 4 req/s × 60 s × concurrency 8, stated with its shape · "self-heal under load" — 14.53 s, different pod uid on a different node · "the PRR minutes exist with every box carrying pasted evidence" — four boxes, §0 stating what the review could NOT do · "deployments ledger opens" — five M5 rows); red team `make verify-m5-redteam` PASSED at M5-S5 (one rewritten recorded number → RED exit 1 with 2 FAILs from two different artifacts, 47 sub-check lines still passing, sha256-verified byte-identical restore → GREEN); lineage spot-check `git merge-base --is-ancestor 3e28a1f origin/main` (M5-S2's merge) → ancestor confirmed; tag `m5-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M6_KICKOFF.md` §0: **F-032's rehearsal half → M6-S4** (the check closed the row at M5-S5; the PRR's "M6 owns the rehearsed revert" intaken) · **the platform-restore rehearsal → M6-S5** (un-rehearsed since M4-S2, scratch-restore scenario) · **the CPU request (200m vs ~1.31 cores) → M6-S2** (changed on the wire WITH before/after re-measurement) · **alert signals A-1…A-7 → M6-S2** (the SLO document sets their numbers) · **ADR-004's canary spike → M6-S3** (outcome lands as ADR-011; ADR-004's "ADR-006" slot predates the org-overlay renumber) · **F-016 → standing at AWAITING_PO 2026-08-18-1** (PO fork, blocking only at M7's first retrain; M6 legislated promotion-free) · **F-020 → M7, F-022 → M7** (both quoted, unchanged, not due) · error memo §7 row 2 stays open in the memo (M6-S3's shadow table is a free second look; closure M7's) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing, non-blocking. Debt: **NONE DUE — register fully closed** (D-001…D-004 all closed with evidence). Ledger repair this triage: the F-020 row in `ledgers/findings.md` had lost its newline + leading ID cell (fused onto F-021's line, unfindable by grep) — structure restored, content byte-unchanged |
| 2026-08-19 | **M4 gate** (BLUEPRINT §9/M4, all legs — the boundary crossing; no ◆ at M4) | EXEC (Opus 5) — stories M4-S1…S5, PRs #20–#27 (roles MLOps/MLE/SRE/DA-hat per story) | ARCH/Fable 5 (M4 boundary triage session) | **PASS** | `make verify-m4` re-run by the approver at the boundary 2026-08-19 → **GREEN, 39/39 sub-checks across 7 sections, exit 0** (paste in M5 kickoff §0; closing line verbatim `[verify-m4] GREEN — every M4 sub-check passed.`), incl. §7's strong form of the alias law: *none of the 28 runs the M4 pipeline fitted is a registry version* and `@champion` → version 2, run `92b73bd4f77d…`, read live; §9/M4 accept-when green against the quoted text ("Flyte 2 per docs, containerized, ingest→validate→features→train→evaluate→register parametrized by month" — seven stages incl. the D-003 marts tail, all wrapped, proven on-cluster · "cache-hit rerun" — 1966.9 s → 3.2 s, 5/5 CACHE_HIT, MLflow 16 → 16, two witnesses agreeing · "kill-a-pod retry" — different pod uid, one attempt, retry budget proven real AND finite by the probe · "wall rule → ADR-002 fallback" — the wall was never hit: F-023 closed by `stowConfigOverride`, fallback still armed, never executed); red team `make verify-m4-redteam` PASSED (one flipped field → RED 2 FAILs with the cross-system leg firing, byte-identical restore, GREEN 39/39); lineage spot-check `git merge-base --is-ancestor 6a43498 origin/main` (M4-S3's merge) → ancestor confirmed; tag `m4-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M5_KICKOFF.md` §0: **F-029 policy DECIDED (A — records enter review), mechanics → M5-S1** · **F-022 direction DECIDED ((a) — incumbent cell reads the loaded model), CARRY → M7 (quoted)** · **F-019 → M5-S2** (intake honored at its quoted landing) · **F-009 → M5-S2** (same) · **F-016 → standing at AWAITING_PO 2026-08-18-1** (PO fork, blocking only at M7's first retrain; M5 legislated alias-neutral — serving READS the pointer) · **F-020 → M7** (unchanged, not due) · error memo §7 row 2 stays open in the memo (analysis question, M7's reader) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing, non-blocking. Debt: **NONE DUE — the register is fully closed** (D-001 M4-S3 · D-002 M1-S4 · D-003 M4-S5 · D-004 M4-S3); D-001's registry-pattern deferral stands with its trigger (image churn) and landing (next PO-sanctioned rebuild, the same event owing Flyte's declared 8080 route) — M5 does not trigger it |
| 2026-08-18 | **M3 gate** (BLUEPRINT §9/M3, all legs — the boundary crossing) | EXEC (Opus 5) — stories M3-S1…S5, PRs #15–#19 (roles MLE/DA/MLOps per story) | ARCH/Fable 5 (M3 boundary triage session) | **PASS** | `make verify-m3` re-run by the approver at the boundary 2026-08-18 → **GREEN, 46/46 sub-checks across 8 sections, exit 0** (paste in M4 kickoff §0), registry read and left identical (alias 2, versions [1,2]); every §9/M3 accept-when leg green against the quoted text (dossier 20 candidates ≥10, each with source + leakage note · ablation table with per-group deltas, DR-02's bar re-applied to its own numbers · leakage red-team transcript with inflation +0.1917 min observed on the seen month only · resumability (kill −9 survived, 8/8 answered) and 6 pruned trials · all five gate verdicts replayed through `gate.decide` on disk); lineage spot-check `git branch -r --contains 55b83cf` (M3-S3 doc commit) → `origin/main`; ◆ REV row above (APPROVE WITH CONDITIONS — all three conditions dispositioned at this boundary with quoted landings); tag `m3-closed` | None on the gate itself. Open at close, none silent — every one dispositioned in `docs/milestones/M4_KICKOFF.md` §0: **F-018 → M4-S1** (before the pipeline wraps the gate; REV's "before M7" satisfied by construction) · **F-019 → M5** (quoted) · **F-020 → M7** (quoted) · **F-016 → AWAITING_PO 2026-08-18-1** (a gate-condition fork is the PO's; gate stands as pre-registered meanwhile, M4 demos legislated alias-neutral) · **F-015 CLOSED this triage** (the bake-off doc carries the caveat in the row, exactly as its conditions demanded) · **F-021 (NEW, found this triage: `make ports` tells the reader to stop our own stateful cluster) → M4-S2** · F-009 → M5 (unchanged) · AWAITING_PO 2026-08-16-2 + 2026-08-17-1 standing with the PO, non-blocking. Debt D-001/D-004 → M4-S3, D-003 → M4-S5 — all three intaken at the M4 kickoff, none re-carried |
| 2026-08-18 | **M3 review** (◆ ritual: fresh-session effective challenge of the finished milestone; NOT the M3 gate crossing, which is ARCH's at the boundary) | EXEC (Opus 5) — stories M3-S1…S5, PRs #15–#19 (roles MLE/DA/MLOps per story) | **REV/Opus 5 (fresh session, zero builder context; committed artifacts read before any narrative)** | **APPROVE WITH CONDITIONS** | Findings **F-018 (S2) · F-019 (S2) · F-020 (S3)** in `ledgers/findings.md`; re-derivations pasted in HANDOFF (an). Re-derived from committed artifacts, not from transcripts: (1) champion v2's **KPI-09 3.2402793989 / KPI-10 81.5770997333** and the floor's **3.3517593019 / 80.7334522211** recomputed in numpy straight off `data/predictions/test/*.parquet` — equal to `automation/runs/m3s5/bakeoff.json` to 10 decimals, and val likewise (3.3822796832 / 80.5519384634); (2) **the floor re-fitted from raw processed parquet in DuckDB** — a different engine and code from `baselines.py` — reproducing **3.3517593019 · 80.7334522211 · 1,610,050 groups · 46,938 OD cells · global median 11.15 · 968 rows past both levels**, every figure the bake-off recorded; (3) the automation track's DR-01 spend re-summed from the six phase JSONs to **9,133.8 s**, and the artisan's **3,313.9 s** reconciled from its three ablation JSONs plus the 455.5 s orphan arm the doc declares. `make verify-m3` **re-run by the approver 2026-08-18 → GREEN, 46/46, 8 sections, exit 0**, registry unchanged (alias 2, versions [1,2]) | **Nothing is blocked and no path parks — no S1 was found, so no AWAITING_PO entry is raised and the chain continues.** M3's numbers hold: every published figure re-derives, the winner is the artifact that was measured, the gate replays, the red teams are real, and the milestone's uncomfortable results (three feature groups dropped, automation losing on v1, tuning worth 134 ms) are reported at the size they happened. Conditions, all for ARCH's M3→M4 boundary triage, each needing a QUOTED landing (gotcha #19): (1) **F-018** — the bake-off ranks on the holdout and then gates on it, while the memo says the holdout is "untouched by … selection"; the fix is one line and would not have changed this champion, but M7's retrain loop calls the same shape, so it must land before M7 and the false sentence must be corrected wherever a bake-off produced it. (2) **F-019** — the champion M3 promoted raises on any request outside 2019; M5's kickoff must intake it as a serving-time decision (extend the table vs a policy for uncovered dates), not discover it against a KServe pod. (3) **F-020** — the tuning conclusion rests on a 15%-sample configuration applied unchanged at full scale, round cap included by construction; M7 re-tunes, so the scale-transfer rule belongs in its kickoff. Also noted, not filed: **F-016 already carries the KPI-10 inversion** (the runner-up beats the promoted champion by 0.005 points on the rider-facing metric) and `docs/bakeoff_m3.md` §6 note 1 discloses it honestly — REV verified the two numbers (81.58228% vs 81.57710%, ~308 trips) and adds them as evidence rather than a second finding. REV closes none of its own findings |
| 2026-08-17 | **M2 gate** (BLUEPRINT §9/M2, all legs — the boundary crossing) | EXEC (Opus 5) — stories M2-S1…S5, PRs #10–#14 (roles DE/MLE/DA/MLOps per story) | ARCH/Fable 5 (M2 boundary triage session) | **PASS** | `make verify-m2` re-run by the approver at the boundary 2026-08-17 → **GREEN, 49/49 sub-checks, exit 0** (paste in M3 kickoff §0); lineage spot-check `git branch -r --contains e591cdc` → `origin/main`; ◆ REV row below (APPROVE WITH CONDITIONS, all three conditions dispositioned at this boundary); tag `m2-closed` | None on the gate itself. Open at close, none silent — every one intaken or carried with a quoted landing in `docs/milestones/M3_KICKOFF.md` §0: F-008/F-010/F-011/F-012 + F-013 (new, this triage: two stale config stubs) → M3-S1 · F-007(b) → M3-S2 · F-013 features half → M3-S3 · F-009 → M5 (quoted) · F-001 + AWAITING_PO 2026-08-17-1 standing with the PO. Debt D-001/D-003/D-004 → M4, all quoted, none due here |
| 2026-08-17 | **M2 review** (◆ ritual: fresh-session effective challenge of the finished milestone; NOT the M2 gate crossing, which is ARCH's at the boundary) | EXEC (Opus 5) — stories M2-S1…S5, PRs #10–#14 (roles DE/MLE/DA/MLOps per story) | **REV/Opus 5 (fresh session, zero builder context; artifacts read before narrative)** | **APPROVE WITH CONDITIONS** | Findings **F-010 (S2) · F-011 (S2) · F-012 (S3)** in `ledgers/findings.md`; re-derivations pasted in HANDOFF (ab). Re-derived from committed artifacts, not from transcripts: test KPI-09 **3.260828** / KPI-10 **81.47967%** / floor **3.508999** / **80.32167%** / margin **+7.0724%** computed straight off `data/predictions/test/*.parquet` — matching the champion run's logged metrics to 13 significant figures and its version tags to 4 dp; the memo's §1 coverage split (98.521%/1.479%, +1.88%/+68.19%, floor 18.5704, **75.45%** of the gap), the 970-trip 100–120 min band at **0.000%** KPI-12, the 1–5 min segment where the floor wins (−0.885% test, −0.789% val) and the airport bucket (8.817%, 59.988%, 1.90× non-airport error) all reproduced independently in DuckDB; `scripts/error_memo_numbers.py` re-run over ALL sections, green. `make verify-m2` **re-run by the approver 2026-08-17 → GREEN, 49/49, exit 0** | **Nothing is blocked and no path parks — no S1 was found, so no AWAITING_PO entry is raised and the chain continues.** What the M2 artifacts claim, they can prove: every published number re-derives, the gate refuses on replay, the red teams are real. Conditions, all for ARCH's M2→M3 boundary triage and all landing in M3's kickoff with QUOTED landings (gotcha #19): (1) **F-010** — M3's bake-off is judged against the same 2.00% bar whose real headroom is **+2.71%**, not +7.07%; the kickoff must either re-argue the bar or name the stronger floor, and must not re-quote 7.07% as headroom. (2) **F-011** — M3 is the first milestone with an incumbent champion; an alias move that can demote the served model must be closed BEFORE the bake-off promotes anything, not after. (3) **F-012** — the floor half of every published KPI-13 is unchecked against the registry; cheapest to fix alongside F-011. None of the three is closable by prose (this register's own F-008 precedent), and REV closes none of them itself |
| 2026-08-17 | M1 gate (BLUEPRINT §9/M1, all legs) | EXEC (Opus 5) — stories S1–S5, PRs #5–#9 (roles DE/DA/MLOps per story) | ARCH/Fable 5 (M1 boundary triage session) | **PASS** | HANDOFF entries (q)–(u) with pasted evidence per leg; `make verify-m1` re-run **GREEN, exit 0, all 9 sections** by the approver at the boundary 2026-08-17; lineage spot-check `git branch -r --contains d954edc` → `origin/main`; tag `m1-closed` | None on the gate itself. Open at close, none silent: F-001 (allowlist, PO's hands, AWAITING_PO 2026-08-16-2) · F-005 absorbed into M2-S1 (ARCH scoping call, quoted landing in M2 kickoff §0) · F-006 → M2-S2 · F-007 (a)→M2-S2, (b)→M3 dossier. Debt carried with quoted landings: D-001→M4 · **D-003 (raised at this boundary: 23 GB full-refresh peak)→M4** |
| 2026-08-16 | M0 gate (BLUEPRINT §9/M0, all three legs) | EXEC/MLOps (Opus 5; SRE hat on the S4 drill) — stories S1–S4, PRs #1–#4 | ARCH/Fable 5 (M0 boundary triage session) | **PASS** | HANDOFF entries (l)–(o) with pasted evidence per leg; `make verify-m0` re-run GREEN (18/18, exit 0) by the approver at the boundary 2026-08-16; lineage spot-check `git branch -r --contains c6a3a7e` → `origin/main`; tag `m0-closed` | None on the gate itself. Open at close, non-blocking: F-001 (allowlist, PO's hands, AWAITING_PO 2026-08-16-2) · F-003 (cosmetic, bounded probe scheduled in M1-S4). Debt carried with quoted landings: D-001→M4 · D-002→M1 (intaken at M1 kickoff, absorbed into M1-S4) |
