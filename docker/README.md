# docker/ — written at M3 (train) and M4 (serve if custom needed; mlserver may
# suffice via runtime). uv-based, slim, versions from CLAUDE.md pin table.
# Remember gotcha #3: kind load docker-image after every build.
